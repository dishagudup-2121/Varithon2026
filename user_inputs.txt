<USER_REQUEST>
Before modifying ANY VARI OS project files, verify whether the current
Antigravity agent has outbound network access.

I have enabled Full Access in Settings → Permissions.

Run only these diagnostic checks:

1. Check DNS resolution for:
   overpass-api.de

2. Test:
   curl -I https://overpass-api.de

3. Test:
   curl -I https://www.openstreetmap.org

4. Test:
   curl -I https://tile.openstreetmap.org

5. If curl is unavailable, use another read-only HTTP method available
   in the environment.

DO NOT:
- modify any project files
- install packages
- change dependencies
- modify routes.json
- modify zones.json
- modify the frontend
- modify the backend

Report:

A. DNS result
B. HTTP status for each URL
C. Whether outbound Internet works from the terminal
D. Whether the sandbox is still blocking network access
E. If blocked, identify exactly what permission is causing the block

This is ONLY a network connectivity test.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-11T22:42:00+05:30.
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from None to Gemini 3.1 Pro (High). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>
<USER_REQUEST>
VARI OS — PHASE 1 FINAL DATA CORRECTION

Network connectivity has now been verified.

The Antigravity terminal has:

- DNS access
- outbound Internet access
- successful HTTPS access to overpass-api.de
- successful HTTPS access to www.openstreetmap.org

Therefore proceed with the FINAL remaining task of Phase 1.

IMPORTANT:
The existing Phase 1 frontend architecture is APPROVED.

DO NOT redesign the UI.
DO NOT redesign DigitalTwinMap.
DO NOT modify the dashboard layout.
DO NOT modify Sidebar/Header/KPI/Alerts/Recommendation/Simulation panels.
DO NOT start Phase 2.
DO NOT implement moving pilgrims.
DO NOT implement WebSockets.
DO NOT implement NetworkX simulation yet.

==================================================
CURRENT PROBLEM
==================================================

The current routes.json was manually constructed because network
access was previously unavailable.

We need to replace that manually constructed route geometry with
verified OpenStreetMap-derived road geometry.

The application architecture must remain unchanged.

==================================================
OBJECTIVE
==================================================

Create a verified local geographic dataset:

OpenStreetMap
      ↓
Overpass API
      ↓
OSM road data
      ↓
Local processing
      ↓
GeoJSON
      ↓
existing RouteLayer

The application must NOT query Overpass at runtime.

The downloaded/processed geographic data must be stored locally
inside the repository.

==================================================
IMPORTANT: DO NOT FABRICATE GEOMETRY
==================================================

Do NOT manually invent coordinates.

Do NOT generate "realistic-looking" roads.

Do NOT approximate road geometry if OSM data is available.

Use actual geometry returned by OpenStreetMap/Overpass.

If the exact Wari route cannot be identified confidently from
OpenStreetMap data, DO NOT pretend that an arbitrary road is the Wari.

Instead:

1. obtain the actual OSM road network for the selected geographic
   demonstration area;
2. identify the relevant road corridor using available OSM metadata;
3. document any ambiguity.

==================================================
GEOGRAPHIC SCOPE
==================================================

Do NOT attempt to download the entire Maharashtra Wari route.

Use the same Alandi/Pune-side demonstration area currently displayed
by the Digital Twin.

Preserve the existing geographic scope as much as possible so the
existing zones and dashboard remain meaningful.

First inspect the current routes.json and zones.json to determine
their current geographic extent.

Then choose a reasonable bounding box covering the existing
demonstration area.

==================================================
OVERPASS QUERY
==================================================

Use Overpass API to retrieve the relevant road network.

Retrieve appropriate OSM highway features for the demonstration area.

The query should include road classes useful for:

- primary roads
- secondary roads
- tertiary roads
- residential roads
- unclassified roads
- service roads where operationally useful

Do not blindly include every OSM object.

Exclude irrelevant features such as:

- footways
- paths
- cycleways
- tracks

unless there is a specific reason they are operationally important
for the Wari demonstration.

Preserve:

- OSM way ID
- highway type
- road name where available
- geometry
- relevant metadata

==================================================
DATA PROCESSING
==================================================

Convert the Overpass result into local GeoJSON.

Preferred structure:

FeatureCollection
    |
    +-- road feature
    +-- road feature
    +-- road feature
    +-- ...

Each feature should retain useful source metadata.

Example:

{
  "type": "Feature",
  "properties": {
    "osm_way_id": "...",
    "highway": "primary",
    "name": "...",
    "source": "OpenStreetMap"
  },
  "geometry": {
    "type": "LineString",
    "coordinates": [...]
  }
}

Do not throw away the OSM way ID.

We will need stable identifiers later for NetworkX.

==================================================
ROUTE MODEL COMPATIBILITY
==================================================

The existing React RouteLayer already expects the current route
data model.

Do not break it.

If necessary, create a preprocessing step that converts:

OSM GeoJSON
    ↓
VARI route GeoJSON/model

The frontend should continue consuming the same clean application-level
route model.

The goal is:

OSM data can change
without requiring a frontend rewrite.

==================================================
ROUTE IDs
==================================================

Create deterministic route/segment IDs.

For example:

R_OSM_<osm_way_id>

or another stable scheme.

Do NOT generate random IDs every time the dataset is processed.

Stable IDs are critical for Phase 2.

Later we need:

route_id
segment_id
NetworkX edge
pilgrim movement

to refer to the same geographic objects.

==================================================
ZONES
==================================================

DO NOT replace the existing zones automatically.

Inspect zones.json.

If the existing zones are intentionally synthetic operational zones,
keep them.

Document that:

- road geometry = OSM-derived
- operational zones = VARI MVP-defined geometry

Do not pretend the zones themselves came from OSM unless they actually
did.

==================================================
PHASE 1 DATA ARCHITECTURE
==================================================

The final structure should conceptually be:

data/
    geospatial/
        osm/
            source/
        routes/
            routes.geojson
        zones/
            zones.geojson

If the existing repository has a different approved structure,
preserve it instead of creating duplicate data directories.

Do not duplicate geographic datasets unnecessarily.

==================================================
SOURCE DOCUMENTATION
==================================================

Create/update a small metadata file for the geographic dataset.

For example:

geospatial_metadata.json

It should contain:

{
  "source": "OpenStreetMap",
  "retrieval_method": "Overpass API",
  "retrieved_at": "...",
  "bounding_box": {
    "south": ...,
    "west": ...,
    "north": ...,
    "east": ...
  },
  "feature_count": ...,
  "license_note": "OpenStreetMap data",
  "processing": "OSM highway features converted to local GeoJSON"
}

Do not invent values.

Populate them from the actual acquisition process.

==================================================
VALIDATION
==================================================

After downloading and processing the data, validate:

1. GeoJSON is syntactically valid.
2. Every LineString has valid coordinates.
3. Coordinates are in WGS84 longitude/latitude order.
4. OSM way IDs are preserved.
5. Route IDs are deterministic.
6. No duplicate route IDs.
7. Dataset is inside the intended geographic bounding box.
8. Existing zones still align spatially with the displayed area.
9. Existing RouteLayer renders the new data.
10. The map still opens without Internet after the data has been
    downloaded, except for the basemap itself.

==================================================
CRITICAL VERIFICATION
==================================================

Before claiming success, report:

1. Exact Overpass endpoint used.
2. Exact Overpass query used.
3. Bounding box.
4. Number of OSM ways retrieved.
5. Number of usable road features after filtering.
6. Number of GeoJSON features generated.
7. Number of unique OSM way IDs.
8. Number of unique VARI route IDs.
9. Geographic min/max latitude.
10. Geographic min/max longitude.
11. Data retrieval timestamp.
12. Files created.
13. Files modified.
14. Whether any geometry was manually fabricated.

The answer to item 14 MUST be explicit:

"None" if no geometry was fabricated.

==================================================
VISUAL VALIDATION
==================================================

Open the application after replacing the dataset.

Confirm that:

- the OSM basemap still renders
- roads appear in geographically correct locations
- the existing zones remain visible
- route layer toggle works
- zone layer toggle works
- pilgrim layer remains static
- dashboard remains unchanged

Do not begin moving pilgrims.

==================================================
FINAL BUILD CHECK
==================================================

Run:

npm run lint

npm run build

Fix any errors.

Do not add unnecessary dependencies.

==================================================
FINAL REPORT
==================================================

At the end, provide a concise report with:

PHASE 1 FINAL STATUS

Map:
PASS/FAIL

OSM basemap:
PASS/FAIL

Verified OSM road geometry:
PASS/FAIL

Zones:
PASS/FAIL

Map controls:
PASS/FAIL

Static pilgrim layer:
PASS/FAIL

Frontend build:
PASS/FAIL

Lint:
PASS/FAIL

Then provide the geographic dataset statistics listed above.

IMPORTANT:

Do not claim Phase 1 is complete unless the route geometry has
actually been obtained from OpenStreetMap/Overpass.

Do not proceed to Phase 2.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-11T22:46:09+05:30.
</ADDITIONAL_METADATA>
<USER_REQUEST>
Antigravity's report says:

"All routes.json geometries now contain exactly the LineStrings returned by the OpenStreetMap API."

So the technically correct statement is:

"All route geometries were constructed from the OSM way/node geometry returned by Overpass; no coordinates were manually fabricated."

That's more defensible in a judge Q&A.

Don't touch Phase 1 anymore

Seriously.

Freeze it.

Don't let Antigravity now decide:

"I'll improve the map."

That is how hackathon projects break working components.

Create a mental checkpoint:

================================
VARI OS
PHASE 1 — FROZEN
================================

OSM data              ✅
routes.json           ✅
zones.json            ✅
Leaflet               ✅
Layer controls        ✅
Static pilgrims      ✅
Dashboard             ✅
Lint                  ✅
Build                 ✅

DO NOT MODIFY
================================

Commit this state to Git before Phase 2.

Something like:

git add .
git commit -m "feat(digital-twin): complete phase 1 geographic foundation"

Then Phase 2 happens on top of that checkpoint.


before making any commit i want to inform you that we have create a branch name common and within that i have my branch named digital_twin, the reason to do like this is to avoid the failur of project at main branch, we will first merge our work on the branch common and if everything works well we will push that in main branch, so now i want to commit and push my changes to the branch digital_twin which is already present in branch named "common".

give me steps to do so
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-11T23:11:28+05:30.

The user's current state is as follows:
Browser State:
  Page B787362B8A9775AAD170AFD4E98435EB (VARI OS) - http://localhost:5173/ [ACTIVE]
    Viewport: 1470x750, Page Height: 750
</ADDITIONAL_METADATA>
<USER_REQUEST>
the branch exist on the repo because i have created in through github interface and not through terminal now what to do
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-11T23:19:43+05:30.

The user's current state is as follows:
Browser State:
  Page B787362B8A9775AAD170AFD4E98435EB (VARI OS) - http://localhost:5173/ [ACTIVE]
    Viewport: 1470x750, Page Height: 750
</ADDITIONAL_METADATA>
<USER_REQUEST>
VARI OS — PHASE 2A
ROAD NETWORK GRAPH FOUNDATION

Phase 1 has been completed and committed to Git.

DO NOT modify Phase 1 behavior unnecessarily.

Phase 1 is now frozen.

==================================================
OBJECTIVE
==================================================

Build the backend graph foundation for the Digital Twin.

Convert the verified OpenStreetMap road dataset produced in Phase 1
into a NetworkX graph that will later support:

- synthetic pilgrim movement
- route selection
- route closure
- rerouting
- congestion simulation
- Digital Twin state updates

THIS PHASE DOES NOT IMPLEMENT PILGRIM MOVEMENT.

==================================================
CURRENT PHASE 1 DATA
==================================================

Inspect the existing repository first.

Locate and inspect:

- routes.json
- osm_data.json
- geospatial_metadata.json
- existing frontend geographic types
- existing backend structure
- package/dependency configuration

Do not assume paths.

Use the actual repository structure.

==================================================
IMPORTANT ARCHITECTURAL RULE
==================================================

Do NOT construct the graph from visual Leaflet geometry alone.

Prefer the raw OSM way/node relationships from osm_data.json where
available.

The graph should preserve the relationship:

OSM way
   ↓
OSM node sequence
   ↓
graph edges

routes.json may be used as the application-level geographic
representation and metadata source.

==================================================
GRAPH LIBRARY
==================================================

Use NetworkX.

Do not introduce another graph library.

The graph must be created in Python/backend code.

Do not implement graph construction in React.

==================================================
GRAPH TYPE
==================================================

Use a directed graph:

networkx.DiGraph

Reason:

Road directionality must eventually support:

- one-way roads
- route closure
- directional movement
- realistic rerouting

If a road is bidirectional, create both directed edges.

Example:

N1 → N2
N2 → N1

If an OSM way is one-way:

N1 → N2

only.

==================================================
NODE MODEL
==================================================

Each graph node must preserve:

- stable node ID
- latitude
- longitude
- OSM node ID where available

Example conceptual structure:

node_id:
"N_123456"

attributes:

{
    "osm_node_id": 123456,
    "lat": 18.6712,
    "lng": 73.8951
}

Do not generate random node IDs.

Use deterministic identifiers.

==================================================
EDGE MODEL
==================================================

Each edge must preserve, where available:

- stable edge ID
- route ID
- OSM way ID
- highway type
- road name
- geometry
- length in meters
- oneway information
- relevant OSM metadata

Example:

{
    "edge_id": "E_R_OSM_123456_01",
    "route_id": "R_OSM_123456",
    "osm_way_id": 123456,
    "highway": "secondary",
    "name": "Example Road",
    "length_m": 142.6,
    "oneway": false
}

Do not invent missing OSM attributes.

Use null/None where appropriate.

==================================================
DISTANCE
==================================================

Calculate edge length from geographic coordinates.

Use an appropriate geographic distance calculation.

Do NOT calculate length as:

sqrt((lat2-lat1)^2 + (lng2-lng1)^2)

because latitude/longitude are geographic coordinates.

Use a proper geodesic/haversine calculation or an established
geospatial utility already available in the project.

Do not add a heavy dependency unless necessary.

==================================================
OSM WAY STRUCTURE
==================================================

Inspect the actual osm_data.json before implementation.

Determine exactly how OSM ways and nodes are represented.

Do not assume the JSON schema.

Build the graph from the actual downloaded data.

Preserve OSM IDs.

==================================================
ONE-WAY HANDLING
==================================================

Inspect the actual OSM tags.

Handle:

oneway=yes
oneway=true
oneway=1

as directed travel.

Handle explicit bidirectional roads as two directed edges.

If the OSM source contains other one-way conventions, document how
they are handled.

Do not silently guess.

==================================================
ROAD FILTERING
==================================================

Do not download or recreate another dataset.

Use the verified Phase 1 dataset.

The Phase 1 dataset already contains the selected highway classes.

Do not remove roads arbitrarily at this stage.

The complete graph is the source of truth.

Any simulation-specific filtering will happen later.

==================================================
INTERSECTIONS
==================================================

Preserve actual OSM node connectivity.

If multiple OSM ways reference the same OSM node:

they must connect in the graph.

Do not create separate graph nodes for the same OSM node.

This is critical for routing.

==================================================
EDGE SEGMENTATION
==================================================

Do not collapse an entire OSM way into one artificial edge if the
way contains intermediate OSM nodes that are important for network
connectivity.

Represent the OSM node sequence appropriately.

For example:

N1 → N2 → N3 → N4

should remain traversable as connected segments.

Preserve the original OSM way ID across those segments.

==================================================
GRAPH MODEL
==================================================

Create a clean graph-building module.

Preferred conceptual structure:

backend/
    graph/
        __init__.py
        graph_builder.py
        graph_model.py
        graph_validator.py

Use the repository's existing backend structure if one already exists.

Do not create duplicate backend architectures.

==================================================
GRAPH BUILDER
==================================================

Implement a deterministic graph-building function.

Conceptually:

build_graph(source_data) -> nx.DiGraph

It should:

1. load OSM nodes
2. load OSM ways
3. create graph nodes
4. create directed edges
5. preserve OSM metadata
6. calculate edge lengths
7. validate connectivity
8. return the graph

Do not expose this through an API yet unless the existing backend
architecture requires it.

==================================================
GRAPH VALIDATOR
==================================================

Implement validation checks for:

1. node count > 0
2. edge count > 0
3. every edge references valid nodes
4. every node has valid coordinates
5. every edge has positive length
6. every edge has stable ID
7. every edge has OSM way ID
8. no duplicate edge IDs
9. no invalid geographic coordinates
10. directed edge consistency
11. expected geographic extent
12. graph is not accidentally empty/disconnected

IMPORTANT:

Do not require the entire OSM graph to be one connected component.

Real road datasets can contain disconnected components.

Instead report:

- total connected components
- largest connected component size
- isolated node count if any

Do not delete components merely to make validation pass.

==================================================
GRAPH STATISTICS
==================================================

Create a diagnostic output containing:

- total nodes
- total directed edges
- unique OSM ways
- unique OSM nodes
- bidirectional road count
- one-way road count
- total road length
- largest connected component
- number of connected components
- min latitude
- max latitude
- min longitude
- max longitude

Do not invent values.

Calculate them from the actual graph.

==================================================
TESTS
==================================================

Add focused tests for:

1. simple two-node bidirectional road
2. one-way road
3. shared intersection node
4. edge length calculation
5. deterministic IDs
6. invalid coordinates
7. disconnected components

Use the project's existing test framework if one exists.

Do not introduce a large testing framework unnecessarily.

==================================================
PERSISTENCE
==================================================

Do NOT replace routes.json.

Do NOT modify Phase 1 geographic data.

For this phase, the NetworkX graph can be built at backend startup
or through a deterministic builder.

If persistence is useful, create a derived graph artifact.

Do not commit unnecessary generated binary/cache files.

The source of truth remains the OSM-derived geographic dataset.

==================================================
API
==================================================

Do NOT implement:

- WebSockets
- pilgrim APIs
- movement APIs
- simulation endpoints
- routing endpoints

Those belong to later Phase 2 stages.

If necessary, expose only a development/diagnostic graph statistics
endpoint, but prefer keeping the builder internal unless the
existing architecture requires an API.

==================================================
FRONTEND
==================================================

Do NOT modify:

- Dashboard UI
- DigitalTwinMap visual design
- RouteLayer
- ZoneLayer
- PilgrimLayer

The current Phase 1 map must continue working exactly as before.

This phase is backend graph infrastructure.

==================================================
PERFORMANCE
==================================================

The Phase 1 dataset contains approximately 10,000+ OSM road ways.

Do not perform expensive graph reconstruction on every frontend render.

Graph construction must happen:

- once at backend startup
or
- through an explicit initialization step.

Do not create the graph repeatedly for every request.

==================================================
DEPENDENCIES
==================================================

Use the previously approved project stack.

Do not change frontend technology.

Do not introduce unnecessary libraries.

If NetworkX is not already installed in the backend environment,
add the pinned version according to the project's established
Python dependency strategy.

Do not install packages globally.

==================================================
LANGUAGE
==================================================

This is backend infrastructure.

Do not modify English/Marathi UI strings in this phase.

Bilingual UI remains a frontend concern.

==================================================
GIT SAFETY
==================================================

Phase 1 has already been committed.

Before making changes:

1. inspect git status
2. inspect current branch
3. do not reset or overwrite Phase 1 commits
4. do not modify unrelated files

==================================================
VALIDATION
==================================================

Run the appropriate backend tests.

Run:

npm run lint
npm run build

if these are the existing frontend validation commands.

Also run the Python graph tests.

Do not leave generated temporary files in the repository.

==================================================
FINAL REPORT
==================================================

At completion report:

PHASE 2A STATUS

Graph construction: PASS/FAIL
OSM node preservation: PASS/FAIL
OSM way preservation: PASS/FAIL
One-way handling: PASS/FAIL
Bidirectional handling: PASS/FAIL
Edge length calculation: PASS/FAIL
Connectivity validation: PASS/FAIL
Tests: PASS/FAIL
Frontend unchanged: PASS/FAIL
Build: PASS/FAIL
Lint: PASS/FAIL

Then provide actual calculated statistics:

- OSM nodes
- graph nodes
- OSM ways
- graph edges
- one-way roads
- bidirectional roads
- total road length
- connected components
- largest component
- geographic extent

Do not proceed to Phase 2B.

STOP after Phase 2A.

One hard rule for Phase 2

Don't let Antigravity turn this into a generic GPS simulator.

The chain must remain:

Real OSM geography → NetworkX graph → synthetic pilgrim groups → constrained movement → Digital Twin.

The pilgrims are simulated. The geography isn't.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-11T23:48:11+05:30.

The user's current state is as follows:
Browser State:
  Page B787362B8A9775AAD170AFD4E98435EB (VARI OS) - http://localhost:5173/ [ACTIVE]
    Viewport: 1470x750, Page Height: 750
</ADDITIONAL_METADATA>
<USER_REQUEST>
Approved with the following mandatory changes before implementation.

The overall Phase 2A architecture is approved:

backend/app/graph/
    __init__.py
    graph_model.py
    graph_builder.py
    graph_validator.py

and:

backend/tests/test_graph.py

The NetworkX DiGraph approach, deterministic OSM IDs, Haversine
distance calculation, diagnostics script, and isolated backend
implementation are approved.

Before implementation, incorporate these requirements:

1. HANDLE oneway=-1

If an OSM way has:

oneway=-1

reverse the node sequence when constructing directed edges.

Example:

OSM:
N1 -> N2 -> N3
oneway=-1

Graph:
N3 -> N2 -> N1

Do not treat oneway=-1 as bidirectional.

2. SHARED OSM NODES

The same OSM node ID appearing in multiple OSM ways MUST map to the
same NetworkX graph node.

This must be explicitly tested.

3. ADD TEST: oneway=-1

Expected direction must be reversed.

4. ADD TEST: missing node coordinates

Validation must fail with a clear error rather than silently
creating an invalid edge.

5. ADD TEST: duplicate/invalid edge handling

Ensure deterministic edge construction and no corrupt duplicate
edge IDs.

6. PRESERVE EDGE GEOMETRY

Every graph edge must retain the ordered geographic geometry of the
original OSM way segment, in addition to:

- edge_id
- route_id
- osm_way_id
- highway
- name
- length_m
- oneway

This geometry will later be required for road-constrained pilgrim
movement and position interpolation.

7. DO NOT over-engineer graph_model.py.

Use NetworkX attributes and lightweight typing/helpers instead of
creating unnecessary Node/Edge class hierarchies.

8. DO NOT modify Phase 1 frontend behavior.

9. DO NOT implement Phase 2B or later.

10. After implementation, STOP and provide the graph statistics and
test results.

Do not proceed beyond Phase 2A.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-11T23:55:59+05:30.

The user's current state is as follows:
Browser State:
  Page B787362B8A9775AAD170AFD4E98435EB (VARI OS) - http://localhost:5173/ [ACTIVE]
    Viewport: 1470x750, Page Height: 750
</ADDITIONAL_METADATA>
<USER_REQUEST>
had you implemented the plan you have given to me?
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-12T00:09:03+05:30.

The user's current state is as follows:
Browser State:
  Page B787362B8A9775AAD170AFD4E98435EB (VARI OS) - http://localhost:5173/ [ACTIVE]
    Viewport: 1470x750, Page Height: 750
</ADDITIONAL_METADATA>
<USER_REQUEST>
 PHASE 2A — FINAL VALIDATION ONLY

Do not modify the graph implementation.

Inspect the coordinate validation/fallback logic in graph_builder.py
and graph_validator.py.

Confirm exactly how missing or invalid OSM coordinates are handled.

Specifically verify:

1. Is a coordinate (0, 0) ever treated as a "missing coordinate"
   using a simple truthiness check?

2. Are missing latitude/longitude values rejected explicitly?

3. Are NaN/infinite coordinates rejected?

4. Are coordinates outside valid geographic ranges rejected?

5. Is any coordinate ever silently replaced with a fabricated
   fallback coordinate?

Do not change code.

Report the exact behavior.

STOP after reporting.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-12T00:13:58+05:30.

The user's current state is as follows:
Browser State:
  Page B787362B8A9775AAD170AFD4E98435EB (VARI OS) - http://localhost:5173/ [ACTIVE]
    Viewport: 1470x750, Page Height: 750
</ADDITIONAL_METADATA>
<USER_REQUEST>
VARI OS — PHASE 2B
SYNTHETIC PILGRIM GROUP MODEL

Phase 2A is COMPLETE and FROZEN.

Do NOT modify Phase 1 geographic data.
Do NOT modify the Phase 2A graph builder.
Do NOT modify the Phase 2A graph validation behavior.
Do NOT implement movement.
Do NOT implement WebSockets.
Do NOT modify the frontend Digital Twin UI.

==================================================
OBJECTIVE
==================================================

Create the backend domain model and deterministic generator for
synthetic pilgrim GROUPS that will later move through the Phase 2A
NetworkX road graph.

IMPORTANT:

One simulation marker represents an AGGREGATED PILGRIM GROUP.

It does NOT represent one individual pilgrim.

Example:

G001 = 250 pilgrims
G002 = 180 pilgrims
G003 = 420 pilgrims

==================================================
DATA MODEL
==================================================

Create a lightweight typed model containing:

group_id
count
current_edge_id
progress
speed_mps
status
destination_zone_id
route
created_at
updated_at

Use the project's existing Python typing/model conventions.

Do not introduce unnecessary model frameworks.

If Pydantic is already part of the approved backend stack, use it
where appropriate.

Otherwise use lightweight dataclasses/TypedDicts consistent with the
existing project.

==================================================
FIELD RULES
==================================================

group_id:

Stable deterministic identifier.

Example:

G001
G002
G003

count:

Positive integer.

Represents the number of simulated pilgrims in the group.

current_edge_id:

Must refer to an actual edge in the Phase 2A NetworkX graph.

progress:

Float in:

0.0 <= progress <= 1.0

Meaning:

0.0 = beginning of current edge
1.0 = end of current edge

speed_mps:

Positive simulated movement speed.

This is a simulation parameter.

Do NOT claim it represents measured Wari data.

status:

Initially support:

"moving"
"stopped"
"congested"

Do not implement automatic status transitions yet.

destination_zone_id:

Must reference one of the existing VARI operational zones.

Do not invent a new zone dataset.

route:

Initially an empty list is acceptable.

Do not implement route calculation in this phase.

created_at / updated_at:

Use consistent UTC timestamps according to the project's existing
backend conventions.

==================================================
INITIAL POSITIONING
==================================================

Do NOT place groups at random latitude/longitude coordinates.

Do NOT generate coordinates independently of the road network.

Every group must initially occupy a valid graph edge.

Use the Phase 2A NetworkX graph.

Prefer edges belonging to the largest connected component.

==================================================
DESTINATION SELECTION
==================================================

Use existing zones from Phase 1.

Each generated group must have a valid destination_zone_id.

Do not create fake zones.

Do not assume that a zone centroid is a traversable road position.

The destination is a logical operational target for now.

Actual graph routing will be implemented in Phase 2C.

==================================================
GROUP GENERATOR
==================================================

Create a deterministic synthetic generator.

It should accept a seed.

Example conceptual API:

generate_pilgrim_groups(
    graph,
    zones,
    count=30,
    seed=42
)

The same seed should produce the same initial groups.

This is important for reproducible demos and testing.

==================================================
GROUP SIZE
==================================================

Generate varied group sizes.

Use configurable simulation parameters rather than hardcoded
"real-world Wari statistics."

Example configuration ranges may be used, but document clearly that
they are synthetic demonstration parameters.

Do not present synthetic distributions as historical Wari data.

==================================================
SPEED
==================================================

Generate varied simulated speeds within a configurable range.

Again:

These are simulation parameters.

Do not claim they are measured Wari pedestrian speeds.

Keep the configuration centralized so Phase 2C can later modify
speed dynamically.

==================================================
INITIAL EDGE SELECTION
==================================================

Select valid edges from the largest connected component.

Ensure:

- edge exists
- edge has geometry
- edge has positive length
- edge has valid source/target nodes

Do not choose disconnected or invalid edges.

==================================================
DISTRIBUTION

Avoid putting all groups on the exact same edge.

Spread the initial groups across valid graph edges.

Avoid pathological clustering unless explicitly configured.

However, do not implement crowd-congestion logic yet.

==================================================
NO MOVEMENT

This is critical.

Phase 2B MUST NOT:

- move groups
- update progress continuously
- calculate routes
- run a simulation clock
- open WebSockets
- update the React frontend
- animate Leaflet markers

Those belong to later phases.

Phase 2B only creates and validates the initial state.

==================================================
STATE VALIDATION

Create validation for:

1. unique group IDs
2. positive counts
3. valid edge IDs
4. progress range
5. positive speed
6. valid status
7. valid destination zone
8. valid timestamps
9. edge belongs to graph
10. edge has valid geometry

Invalid groups must fail clearly.

Do not silently repair invalid state.

==================================================
TESTS

Add tests for:

1. deterministic generation with same seed
2. different seed produces different valid groups
3. unique group IDs
4. positive pilgrim counts
5. valid graph edge assignment
6. progress range
7. valid speed
8. valid destination zone
9. invalid edge rejected
10. invalid progress rejected
11. invalid count rejected

Use the existing backend test framework.

==================================================
PERFORMANCE

The initial target is approximately:

30–100 pilgrim groups.

Do not create one object per pilgrim.

A group with:

count = 500

is ONE simulation object.

==================================================
ARCHITECTURE

Keep the model independent from:

- FastAPI
- WebSocket
- React
- Leaflet

The model should be usable by the future simulation engine.

Conceptually:

Graph
  ↓
Pilgrim Group Generator
  ↓
Pilgrim Group State
  ↓
Future Phase 2C Movement Engine

==================================================
FRONTEND

Do not modify the frontend.

The existing static pilgrim visualization remains frozen.

Phase 2B is backend-only.

==================================================
VALIDATION

Run:

pytest

and the existing:

npm run lint
npm run build

Do not modify unrelated files.

Check git diff before completion.

==================================================
FINAL REPORT

Report:

- files created
- files modified
- model schema
- number of groups generated
- total simulated pilgrims
- min/max group size
- min/max speed
- number of destination zones used
- number of source edges used
- random seed
- test results
- frontend build/lint results

Explicitly confirm:

"MOVEMENT NOT IMPLEMENTED"

"WEBSOCKET NOT IMPLEMENTED"

"ROUTING NOT IMPLEMENTED"

"FRONTEND NOT MODIFIED"

STOP after Phase 2B.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-12T00:17:20+05:30.

The user's current state is as follows:
Browser State:
  Page B787362B8A9775AAD170AFD4E98435EB (VARI OS) - http://localhost:5173/ [ACTIVE]
    Viewport: 1470x750, Page Height: 750
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2B — APPROVED WITH REQUIRED CORRECTIONS

The overall Phase 2B architecture is approved.

Proceed with:

backend/app/simulation/pilgrim_model.py
backend/app/simulation/generator.py
backend/tests/test_pilgrim_groups.py
backend/scripts/phase2b_diagnostics.py

Use the existing Pydantic dependency because it is already present
in requirements.txt.

Before implementation, incorporate these corrections:

1. LARGEST COMPONENT

Use the largest weakly connected component of the NetworkX DiGraph
for initial edge selection.

Do NOT describe this as guaranteeing directed route reachability.

Actual directed routing will be implemented in Phase 2C.

2. ZONES

Inspect the actual Phase 1 zones.json schema.

Do not assume its structure.

Extract real zone IDs from the existing dataset.

Every destination_zone_id must correspond to an existing zone.

Do not invent zone IDs.

3. DETERMINISTIC TIME

The generator must accept a deterministic simulation timestamp,
for example:

simulation_time: datetime

When the same:

- graph
- zones
- seed
- count
- configuration
- simulation_time

are provided, generated groups must be identical.

Do NOT call datetime.now() internally for deterministic generation.

created_at and updated_at should initially use the supplied
simulation_time.

Phase 2D will later provide the live simulation clock.

4. CONFIGURABLE SYNTHETIC PARAMETERS

Do not hardcode group sizes or speeds throughout the generator.

Centralize configurable defaults such as:

min_group_size
max_group_size
min_speed_mps
max_speed_mps

The values are synthetic demonstration parameters.

Do NOT claim they represent measured Wari statistics.

5. INITIAL EDGE VALIDATION

Every generated group must:

- use an edge from the largest weakly connected component
- reference an existing graph edge
- have positive edge length
- have valid edge geometry
- have progress between 0 and 1

6. GROUP PLACEMENT

Spread groups across valid edges.

Do NOT force all groups onto unique edges.

Multiple groups may occupy the same edge because future congestion
simulation requires this.

Avoid accidental extreme clustering.

7. TESTS

In addition to the proposed tests, add tests for:

- valid destination_zone_id
- invalid destination_zone_id
- deterministic timestamps
- largest-component placement
- multiple groups can legally share an edge

8. NO MOVEMENT

Do NOT implement:

- movement
- progress updates
- route calculation
- simulation clock
- WebSockets
- frontend changes
- Leaflet animation

9. NO ROUTING

Do not call NetworkX shortest_path or any routing algorithm in Phase 2B.

10. FRONTEND

Do not modify the frontend.

11. FINAL REPORT

Include:

- number of groups
- total simulated pilgrims
- min/max group size
- min/max speed
- number of destination zones
- number of source edges
- number of groups sharing edges
- random seed
- simulation timestamp
- tests passed
- frontend build/lint

Explicitly confirm:

MOVEMENT: NOT IMPLEMENTED
ROUTING: NOT IMPLEMENTED
WEBSOCKET: NOT IMPLEMENTED
FRONTEND: NOT MODIFIED

STOP after Phase 2B.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-12T00:28:47+05:30.

The user's current state is as follows:
Browser State:
  Page B787362B8A9775AAD170AFD4E98435EB (VARI OS) - http://localhost:5173/ [ACTIVE]
    Viewport: 1470x750, Page Height: 750
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2B — TEST COUNT VERIFICATION ONLY

Do not modify implementation code.

Inspect backend/tests/test_pilgrim_groups.py and determine:

1. Exact number of test functions/cases present.
2. Exact number executed by the pytest command.
3. Whether all tests passed.
4. Why the walkthrough says "8 unit tests" while the validation
   says "All 7 tests passed."

If there are actually 8 tests, run the complete test file and report
the result.

Do not change the test implementation unless there is an actual
failure.

Do not proceed to Phase 2C.

STOP after reporting.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-12T00:38:22+05:30.

The user's current state is as follows:
Browser State:
  Page B787362B8A9775AAD170AFD4E98435EB (VARI OS) - http://localhost:5173/# [ACTIVE]
    Viewport: 1470x835, Page Height: 835
</ADDITIONAL_METADATA>
<USER_REQUEST>
# VARI OS — PHASE 2C-1

## Route Planning Engine + Digital Twin Visualization Refinement

Phase 1, Phase 2A and Phase 2B are complete.

Phase 1 OSM data has been pushed to the repository.

Phase 2A NetworkX graph construction and validation are complete.

Phase 2B synthetic pilgrim-group generation is complete.

Treat Phase 1, Phase 2A and Phase 2B as FROZEN unless a compatibility fix is absolutely required.

Do not rewrite or regenerate existing geographic data.

---

# PART A — DIGITAL TWIN MAP REFINEMENT

The current Digital Twin map successfully displays the real OSM road network, zones and pilgrim groups.

However, the current visualization has excessive visual clutter because almost all OSM road geometries are rendered with the same bright blue styling.

The objective is to improve visual hierarchy WITHOUT deleting or modifying the underlying OSM data.

## A1. Preserve all geographic data

Do NOT:

* delete road features
* simplify routes.json permanently
* fabricate road geometry
* replace OSM geometry
* regenerate routes.json
* modify the Phase 1 OSM dataset

The full 10,438-road dataset must remain available.

Only the frontend rendering strategy may change.

---

# A2. Introduce zoom-dependent road visibility

Inspect the actual properties available in routes.json before implementing this.

Do not assume a property name.

If highway classification is available, use it.

At LOW zoom:

Display primarily:

* primary
* secondary
* tertiary

Hide or strongly suppress:

* residential
* service
* minor roads

At MEDIUM zoom:

Display:

* primary
* secondary
* tertiary
* selected residential roads

At HIGH zoom:

Display detailed residential/service roads where useful.

The exact zoom thresholds must be determined from the existing Leaflet implementation and tested visually.

Do not delete hidden features from memory.

They are simply not rendered at the current zoom level.

---

# A3. Separate OSM network styling from VARI operational routes

This is critical.

The current legend uses:

* Open Route = blue
* Restricted Route = orange
* Closed Route = red

Therefore, the entire OSM network must NOT be rendered as bright blue.

Use a visually subdued style for the underlying road network.

The semantic colors must be reserved for operational states:

OPEN / RECOMMENDED:
blue

RESTRICTED:
orange

CLOSED:
red

CRITICAL:
red boundary/fill

PILGRIM GROUP:
cyan

The user must be able to distinguish:

"road network"

from:

"VARI operational route"

at a glance.

---

# A4. Road visual hierarchy

Implement a hierarchy approximately like:

Major roads:
higher opacity and thicker than minor roads

Tertiary:
medium opacity

Residential:
thin and subdued

Service:
very thin / hidden until high zoom

Operational VARI routes:
clearly highlighted above the underlying network

Do not use excessive glow effects.

Do not make the map look like a gaming interface.

---

# A5. Preserve the existing legend

The existing legend contains:

* Pilgrim Groups
* Open Route
* Restricted Route
* Closed Route
* High/Critical Zone

Do not remove these items.

However, ensure the legend colors correspond to the actual rendered layers.

If the underlying OSM network receives a neutral/subdued style, do not incorrectly label it as Open Route.

If necessary, add:

* Road Network

with a subdued symbol.

---

# A6. Map controls

Keep existing Leaflet controls working.

Ensure:

* zoom controls work
* layer controls work
* route visibility works
* pilgrim visibility works
* zone visibility works

Do not reintroduce the previous React-Leaflet layer-toggle bug.

Do not wrap Leaflet layer components in invalid div structures.

---

# A7. Do NOT implement movement yet

Phase 2C-1 is route planning only.

Do NOT implement:

* animation
* moving pilgrim markers
* progress updates
* geographic interpolation
* simulation clock
* WebSockets
* real-time backend streaming

Those belong to later phases.

---

# PART B — PHASE 2C-1 ROUTE PLANNING ENGINE

Implement a backend route-planning module using the existing Phase 2A NetworkX DiGraph.

The purpose is:

PilgrimGroup
→ current edge
→ destination zone
→ target graph node
→ weighted directed shortest path
→ ordered route edges

---

# B1. Inspect the existing architecture first

Before writing code:

Inspect:

* backend/app/
* backend/app/simulation/
* Phase 2A graph builder
* graph validator
* Phase 2B PilgrimGroup model
* generator
* zones.json
* routes.json
* graph serialization/loading mechanism

Do not create duplicate graph-loading logic if an existing implementation already exists.

Follow the existing project architecture.

---

# B2. Route planner input

The route planner must work with:

* existing NetworkX DiGraph
* PilgrimGroup
* existing zones data

Do not modify the fundamental PilgrimGroup schema unless absolutely necessary.

The existing group contains information such as:

* group_id
* count
* current_edge_id
* progress
* speed_mps
* status
* destination_zone_id
* route
* timestamps

Use the existing schema.

---

# B3. Current edge → starting node

A pilgrim group currently belongs to a graph edge.

Inspect the actual Phase 2A edge representation.

Do NOT assume the current edge ID directly represents a node.

For the current directed edge:

(source_node, target_node)

determine the valid routing start.

For this MVP:

1. obtain the edge endpoints
2. determine whether each endpoint can reach the destination
3. calculate valid weighted routes
4. select the endpoint producing the shortest valid route
5. use length_m as the edge weight

Do not arbitrarily teleport the pilgrim.

Do not convert the graph to undirected.

Do not ignore one-way restrictions.

---

# B4. Destination zone → graph node

A destination zone is an operational geographic area and is not necessarily a NetworkX node.

Inspect the actual zones.json schema.

Determine the zone's geographic representative point from its existing geometry.

Then find the nearest valid graph node.

The selected target node must:

* exist in the NetworkX graph
* have valid coordinates
* be part of the usable graph
* be geographically nearest to the zone representative point

Do NOT:

* create a new graph node
* fabricate coordinates
* assume zone ID equals node ID
* route using straight-line distance

---

# B5. Routing algorithm

Use NetworkX.

Use the existing directed graph.

Use:

length_m

as the edge weight.

Use an appropriate weighted shortest-path algorithm, such as Dijkstra through NetworkX.

Conceptually:

nx.shortest_path(
graph,
source=start_node,
target=target_node,
weight="length_m"
)

The exact implementation must match the actual graph representation.

Do not create a custom routing algorithm.

---

# B6. Directionality

The existing graph preserves road directionality.

The route planner MUST respect it.

Do not convert:

DiGraph → Graph

Do not manually ignore:

* oneway=yes
* oneway=true
* oneway=1
* oneway=-1

If no directed route exists, return a structured failure.

---

# B7. Route result

Create a typed/validated route result appropriate for the existing backend architecture.

It should conceptually contain:

* group_id
* status
* start_node
* target_node
* node_ids
* edge_ids
* total_distance_m
* failure reason when applicable

Example:

{
"group_id": "G001",
"status": "routed",
"start_node": "N123",
"target_node": "N456",
"node_ids": [
"N123",
"N234",
"N345",
"N456"
],
"edge_ids": [
"E001",
"E002",
"E003"
],
"total_distance_m": 1250.5
}

Use the project's actual ID formats.

Do not fabricate IDs.

---

# B8. Edge/node consistency

If:

N1 → N2 → N3 → N4

is returned,

the corresponding edges must be:

E1 → E2 → E3

and must actually connect:

E1: N1 → N2
E2: N2 → N3
E3: N3 → N4

Do not return unordered edges.

---

# B9. Total distance

Calculate total distance using actual graph edge attributes.

Example:

E1 = 100m
E2 = 250m
E3 = 175m

total_distance_m = 525m

Do not calculate total route length using straight-line geographic distance.

---

# B10. Unreachable route handling

Because the graph contains disconnected components and is directed, some routes may be unreachable.

Do NOT crash.

Return a structured result such as:

status = "unreachable"

reason = "no_directed_path"

Use the project's conventions if an equivalent status model already exists.

Never fabricate a fallback route.

---

# B11. Invalid input handling

Handle explicitly:

* invalid group
* invalid current_edge_id
* missing current edge
* invalid destination_zone_id
* missing destination zone
* missing graph node
* invalid edge
* missing edge length
* zero/negative edge length
* unreachable destination

Do not silently replace invalid values.

Do not fabricate coordinates.

---

# B12. Route calculation must not mutate the simulation

Route calculation should preferably return a route result.

Do not automatically move the pilgrim.

Do not automatically update progress.

Do not start a simulation loop.

If route assignment to PilgrimGroup is necessary, keep it as an explicit operation separate from route calculation.

---

# B13. Tests

Create focused unit tests.

At minimum:

1. simple directed route
2. weighted shortest route
3. one-way restriction
4. directed unreachable route
5. disconnected graph
6. current-edge endpoint selection
7. destination-zone nearest-node selection
8. correct node sequence
9. correct edge sequence
10. correct total distance
11. invalid current edge
12. invalid destination zone
13. deterministic route result

Use small synthetic NetworkX graphs.

Do NOT run all 98,621 edges for every unit test.

---

# B14. Mandatory weighted-routing test

Create a test graph where:

Route A:

100m + 100m + 100m = 300m

Route B:

150m + 50m = 200m

The route planner must select Route B.

This verifies that length_m is actually being used.

---

# B15. Frontend/backend separation

The frontend must NOT depend directly on NetworkX.

The backend route planner should produce serializable route results.

The frontend can consume those results later.

Do not import backend Python logic into React.

---

# B16. Performance

Do not calculate routes for every pilgrim group automatically during module import.

Routing must be explicitly invoked.

Do not add premature caching.

First establish correctness.

Caching can be introduced later after profiling.

---

# B17. Future compatibility

The route representation must be designed so that later phases can use:

route:
[
edge_1,
edge_2,
edge_3,
...
]

for movement.

Later phases will use:

route
+
speed_mps
+
progress
+
edge geometry

to calculate geographic pilgrim positions.

Do not implement those features now.

---

# B18. Future dynamic routing compatibility

The initial routing weight is:

length_m

Later the system may support:

length_m
+
congestion penalty
+
flood risk
+
road closure
+
operational restrictions

Do not implement these dynamic weights in Phase 2C-1.

But keep the routing code structured so the weighting mechanism can be extended later.

---

# VALIDATION

After implementation run:

pytest

npm run lint

npm run build

Check:

git status

git diff

Confirm that Phase 1, Phase 2A and Phase 2B files have not been unintentionally modified.

---

# FINAL REPORT

At completion report:

1. files created
2. files modified
3. route planner architecture
4. shortest-path algorithm
5. edge weight
6. current-edge start-node selection
7. destination-zone target-node selection
8. unreachable-route handling
9. number of tests
10. tests passed
11. frontend changes
12. lint result
13. build result
14. confirmation that Phase 1/2A/2B were preserved

Explicitly state:

MOVEMENT: NOT IMPLEMENTED

GEOMETRIC INTERPOLATION: NOT IMPLEMENTED

WEBSOCKET: NOT IMPLEMENTED

REAL-TIME STREAMING: NOT IMPLEMENTED

DYNAMIC CONGESTION WEIGHTS: NOT IMPLEMENTED

FLOOD-AWARE ROUTING: NOT IMPLEMENTED

AI/ML PREDICTION: NOT IMPLEMENTED

STOP after Phase 2C-1.

Do not proceed automatically to Phase 2C-2.

</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-12T21:50:21+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/simulation/__init__.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/graph/__init__.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/fetch_osm_data.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/digital-twin/DigitalTwinMap.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/dashboard/SimulationPanel.tsx (LANGUAGE_TSX)
Running terminal commands:
- npm run dev (in /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend, running for 8m53s)

The user has uploaded 1 audio file(s). Consider them as a part of their message.
</ADDITIONAL_METADATA>
<USER_REQUEST>
Before proceeding, revise the Phase 2C-1 implementation plan with the
following correction.

CRITICAL ROUTING CORRECTION:

Do NOT test both endpoints of the current directed edge as independent
starting nodes.

A PilgrimGroup is already physically located on its current directed
edge.

If the current edge is:

u → v

the pilgrim must continue in the valid edge direction toward v.

Therefore the route planner must respect the current edge direction
and use the forward endpoint v as the route-continuation node.

Do NOT allow the planner to route backward from v to u simply because
u produces a shorter NetworkX path.

The group's existing `progress` value must be preserved because later
movement phases will need to account for the remaining portion of the
current edge.

For Phase 2C-1, route planning should therefore conceptually be:

current_edge_id
    ↓
resolve directed edge (u → v)
    ↓
validate current progress
    ↓
continue from v
    ↓
destination zone
    ↓
nearest valid graph node
    ↓
NetworkX weighted shortest path

Do not implement movement or progress updates yet.

Also revise destination-zone handling:

Do not assume `zones.json` contains a `center` field.
Inspect the actual schema first.

If `center` exists and is the established representative point,
use it.

Otherwise derive a representative point from the existing zone
geometry without modifying zones.json.

Keep the diagnostic script that runs route planning for all Phase 2B
groups.

Do not require 100% routing success if some groups are legitimately
unreachable because of directed/disconnected graph topology.

Report routed/unreachable counts and reasons.

Everything else in the existing Phase 2C-1 plan can remain unchanged.

After updating the plan, STOP and show the revised plan.
Do not implement until the revised plan is approved.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-12T21:54:33+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/dashboard/AlertsPanel.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/data/geospatial/zones.json (LANGUAGE_JSON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/services/.gitkeep (LANGUAGE_UNSPECIFIED)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/simulation/pilgrim_model.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/common/StatusBadge.tsx (LANGUAGE_TSX)
Running terminal commands:
- npm run dev (in /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend, running for 13m5s)
</ADDITIONAL_METADATA>
<USER_REQUEST>
APPROVED — proceed with Phase 2C-1 implementation.

The revised plan is accepted.

Before implementation, add these two small safeguards:

1. CURRENT EDGE VALIDATION

When resolving `current_edge_id`, verify that it resolves to exactly
one valid directed graph edge `(u, v)`.

The planner must:

current_edge_id
    ↓
resolve exact directed edge
    ↓
validate u and v exist
    ↓
use v as the routing continuation node

If the current edge cannot be resolved unambiguously, return a
structured invalid/unreachable result with an appropriate reason such
as:

reason = "invalid_current_edge"

Do not silently select another edge.

2. CURRENT EDGE vs CONTINUATION ROUTE

The returned Phase 2C-1 route represents the continuation from the
forward endpoint `v` of the current edge.

Do not include the already-occupied current edge as another future
edge in the continuation route.

The current edge's remaining distance based on `progress` will be
handled later in Phase 2C-2 movement.

Do NOT implement that movement logic now.

Everything else in the revised plan is approved.

Implement ONLY:

- Digital Twin map visualization refinement
- Phase 2C-1 route planning
- RouteResult
- route planner tests
- phase2c1 diagnostics

DO NOT IMPLEMENT:

- pilgrim movement
- progress updates
- geographic interpolation
- simulation clock
- WebSockets
- real-time streaming
- dynamic congestion weights
- flood-aware routing
- AI/ML prediction

After implementation run:

pytest
npm run lint
npm run build

Then inspect:

git status
git diff

Confirm Phase 1, Phase 2A and Phase 2B were not unintentionally
modified.

STOP after Phase 2C-1 and provide the final implementation report.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-12T21:57:18+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/store/dashboardStore.ts (LANGUAGE_TYPESCRIPT)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/digital-twin/layers/ZoneLayer.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/schemas/.gitkeep (LANGUAGE_UNSPECIFIED)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/main.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/graph/graph_validator.py (LANGUAGE_PYTHON)
Running terminal commands:
- npm run dev (in /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend, running for 15m50s)
</ADDITIONAL_METADATA>
<USER_REQUEST>
i want to push this work to repo, give me the command, and you know where i am. pushing all this code right?
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-12T22:22:16+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/simulation/__init__.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/graph/__init__.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/fetch_osm_data.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/digital-twin/DigitalTwinMap.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/dashboard/SimulationPanel.tsx (LANGUAGE_TSX)
Running terminal commands:
- npm run dev (in /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend, running for 40m48s)
</ADDITIONAL_METADATA>
<USER_REQUEST>
We have completed and pushed Phase 2C-1.

Before implementing Phase 2C-2, perform an inspection-only analysis of
the existing repository.

DO NOT modify any files.

Inspect the actual implementations of:

1. backend/app/simulation/pilgrim_model.py
2. backend/app/simulation/generator.py
3. backend/app/simulation/route_planner.py
4. Phase 2A graph construction modules
5. Phase 2C-1 RouteResult/model definitions
6. Existing Phase 2B and Phase 2C-1 tests

Determine exactly:

- PilgrimGroup schema and every field/type
- meaning and valid range of `progress`
- meaning and format of `current_edge_id`
- meaning and valid range of `speed_mps`
- graph type (DiGraph/MultiDiGraph/etc.)
- exact graph node schema
- exact graph edge schema
- edge `length_m` representation
- how RouteResult stores continuation edges
- how unreachable routes are represented
- whether a route contains node IDs, edge IDs, or `(u,v)` tuples
- existing simulation-related abstractions
- existing test conventions

Then propose a minimal Phase 2C-2 Movement Engine design that fits
the existing code.

The movement engine must eventually support:

- deterministic simulation ticks
- movement based on `speed_mps`
- progress updates
- remaining distance on current edge
- crossing one or multiple edge boundaries in one tick
- transition to the next route edge
- correct current_edge_id updates
- progress reset when entering a new edge
- arrival/destination state
- unreachable/no-route state
- no teleportation
- no WebSocket
- no frontend changes
- no AI/ML
- no congestion calculation
- no flood-aware routing

For now, ONLY return the inspection findings and proposed design.

Do not create or modify files.
STOP after the analysis.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-12T22:29:03+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/.gitignore (LANGUAGE_UNSPECIFIED)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/scripts/phase2b_diagnostics.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/graph/graph_builder.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/locales/mr/translation.json (LANGUAGE_JSON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/App.tsx (LANGUAGE_TSX)
Running terminal commands:
- npm run dev (in /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend, running for 47m35s)
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-2 — DETERMINISTIC PILGRIM MOVEMENT ENGINE

The Phase 2C-1 route planner is complete and pushed to the repository.

You have already inspected the actual repository structures.

Implement ONLY the deterministic backend movement engine described below.

DO NOT modify the frontend.

DO NOT implement WebSockets.

DO NOT implement real-time streaming.

DO NOT implement congestion calculation.

DO NOT implement flood-aware routing.

DO NOT implement AI/ML.

DO NOT modify Phase 1, Phase 2A, Phase 2B, or Phase 2C-1 behavior
unless a direct compatibility fix is absolutely necessary.

==================================================
1. CORE MOVEMENT
==================================================

Create a dedicated movement module under:

backend/app/simulation/

Use the existing PilgrimGroup model and existing NetworkX DiGraph.

Movement must be deterministic.

Core input:

- graph
- PilgrimGroup
- delta_time_s

Movement distance:

distance_to_move_m =
    group.speed_mps * delta_time_s

Validate delta_time_s:

- must be finite
- must be >= 0
- reject invalid values explicitly

==================================================
2. CURRENT EDGE
==================================================

Resolve:

group.current_edge_id

to exactly one directed graph edge:

(u, v)

Retrieve:

length_m

from the existing graph edge.

Do NOT fabricate a fallback edge.

Do NOT teleport the pilgrim.

If the current edge is missing, invalid, or has non-positive
length_m, return a structured movement failure.

Do not silently interpret this as successful arrival.

==================================================
3. PROGRESS
==================================================

Use the existing PilgrimGroup.progress semantics:

0.0 = beginning of current edge
1.0 = end of current edge

Remaining distance:

remaining_m =
    (1.0 - progress) * length_m

Movement must remain within [0.0, 1.0].

Do not round progress in a way that causes visible discontinuities.

==================================================
4. MULTI-EDGE TRAVERSAL
==================================================

A single simulation tick may cross multiple edges.

Therefore use a loop while movement distance remains.

Example:

speed = 1.5 m/s
delta_time = 60 s

distance_to_move = 90 m

If:

Edge A remaining = 20m
Edge B length = 35m
Edge C length = 100m

the engine must:

1. consume 20m on A
2. transition to B
3. consume 35m on B
4. transition to C
5. consume remaining 35m on C
6. set progress on C accordingly

Do NOT stop after the first edge transition.

Do NOT teleport.

==================================================
5. EDGE TRANSITION
==================================================

When the current edge is completed:

current edge:

(u, v)

If group.route contains another edge:

next_edge_id = first future route edge

Resolve next_edge_id to:

(next_u, next_v)

CRITICAL:

Require:

next_u == v

The next edge MUST begin exactly at the forward endpoint of
the current edge.

This is a hard no-teleportation invariant.

If:

next_u != v

return a structured movement failure with a reason such as:

"route_edge_disconnected"

Do not silently choose another edge.

Do not reroute.

Do not teleport.

Only the Phase 2C-1 route planner is responsible for producing
the route. Phase 2C-2 must consume and validate it.

==================================================
6. ROUTE CONSUMPTION
==================================================

The existing `route` contains ONLY future continuation edges.

It does NOT contain the current edge.

When transitioning:

current_edge_id = next_edge_id
progress = 0.0

Then consume that edge's remaining distance.

Do not append the old current edge to route.

Do not duplicate edges.

Avoid inefficient `pop(0)` if practical; preserve the existing
Pydantic model semantics.

==================================================
7. ARRIVAL
==================================================

If the current edge is completed and there are no future route
edges:

The group has reached the end of its planned route.

Set:

progress = 1.0
status = "stopped"

Mark the movement result as:

arrived = true

Do NOT automatically calculate geographic distance to the zone again.

Phase 2C-1 already selected the destination graph node.

==================================================
8. MOVEMENT RESULT
==================================================

Do not return only PilgrimGroup.

Create a small structured MovementResult model appropriate for
the existing backend architecture.

It should expose at minimum:

- group / updated group state
- distance_moved_m
- edges_traversed
- arrived
- success
- reason

Use clear reason strings for failures, for example:

- "invalid_current_edge"
- "invalid_current_edge_length"
- "invalid_route_edge"
- "route_edge_disconnected"
- "invalid_delta_time"

Normal arrival should NOT be represented as an error.

==================================================
9. STATUS HANDLING
==================================================

If:

group.status != "moving"

do not move the group.

Return a successful no-op movement result with:

distance_moved_m = 0
edges_traversed = 0
arrived = appropriate existing state
reason = "group_not_moving"

Do not invent new PilgrimGroup status literals.

Use only the existing schema.

==================================================
10. TIME
==================================================

Use the existing `updated_at` field and type.

Advance simulation time according to:

delta_time_s

not according to distance travelled.

Do not introduce a separate simulation clock yet.

==================================================
11. SAFETY
==================================================

The movement loop must be guaranteed to terminate.

Explicitly protect against:

- zero/negative edge length
- invalid progress
- invalid delta time
- missing edge
- disconnected route edge
- empty route
- excessive floating-point boundary errors

Do not fabricate coordinates.

Do not fabricate graph edges.

==================================================
12. TESTS
==================================================

Create comprehensive pytest tests.

At minimum test:

1. movement within a single edge
2. progress calculation
3. exact edge-boundary crossing
4. transition to next edge
5. multi-edge traversal in one tick
6. arrival when route becomes empty
7. stationary/non-moving group no-op
8. invalid current edge
9. invalid route edge
10. disconnected next edge
11. zero/negative edge length
12. invalid delta_time
13. progress remains within [0,1]
14. deterministic repeated execution
15. distance moved equals speed × delta_time,
    except where the group arrives before consuming the full
    requested distance

Use small synthetic NetworkX DiGraph fixtures rather than the
full OSM graph for unit tests.

==================================================
13. DIAGNOSTICS
==================================================

Create:

backend/scripts/phase2c2_diagnostics.py

The diagnostic should load the existing Phase 2B groups and route
data and perform deterministic movement ticks.

Report:

- number of groups
- moving groups
- stopped groups
- successful movements
- movement failures
- arrivals
- total distance moved
- number of edge transitions
- failure reasons

Do NOT require all groups to arrive.

Do NOT modify the stored Phase 2B data.

==================================================
14. SCOPE FREEZE
==================================================

This phase ends after deterministic backend movement works.

Explicitly DO NOT implement:

- WebSockets
- frontend animation
- browser timers
- real-time streaming
- crowd density
- congestion scoring
- flood conditions
- weather
- route recalculation
- dynamic graph weights
- AI
- ML
- LLM
- resource optimization

==================================================
15. VALIDATION
==================================================

Run:

cd backend
source .venv/bin/activate
pytest

Then:

cd ../frontend
npm run lint
npm run build

Inspect:

git status
git diff

Confirm:

Phase 1 unchanged
Phase 2A unchanged
Phase 2B unchanged
Phase 2C-1 unchanged except unavoidable integration imports

Do not commit or push automatically.

STOP after implementation and provide a final report containing:

- files created
- files modified
- movement algorithm
- MovementResult schema
- test count
- test results
- diagnostic results
- lint result
- build result
- confirmation of scope exclusions
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-12T22:52:04+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/layout/Header.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/store/dashboardStore.ts (LANGUAGE_TYPESCRIPT)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/digital-twin/layers/ZoneLayer.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/schemas/.gitkeep (LANGUAGE_UNSPECIFIED)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/main.tsx (LANGUAGE_TSX)
Running terminal commands:
- npm run dev (in /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend, running for 1h10m36s)
</ADDITIONAL_METADATA>
<USER_REQUEST>
APPROVED WITH THREE REQUIRED CORRECTIONS.

Proceed with Phase 2C-2 implementation after updating the plan/implementation
with the following safeguards.

==================================================
1. PROGRESS VALIDATION
==================================================

Before calculating remaining distance, explicitly validate:

0.0 <= group.progress <= 1.0

Do not silently clamp invalid progress.

If invalid, return:

success = false
reason = "invalid_progress"

==================================================
2. DELTA TIME VALIDATION
==================================================

Validate delta_time_s as:

- finite
- >= 0

Reject:

- NaN
- +infinity
- -infinity
- negative values

Return:

success = false
reason = "invalid_delta_time"

==================================================
3. CRITICAL ROUTE MUTATION RULE
==================================================

When transitioning from the current edge:

current edge:
(u, v)

and the first future route edge:

next_edge_id

DO NOT pop/remove the route edge before validating it.

Use this order:

1. Peek the next edge ID.
2. Resolve it in the graph.
3. Verify it is valid.
4. Verify its starting node satisfies:

   next_u == v

5. Verify its length_m is valid and positive.
6. ONLY AFTER all validation succeeds, consume/remove the next
   route edge and update:

   current_edge_id = next_edge_id
   progress = 0.0

If validation fails, do not silently discard the invalid route edge.

Return:

success = false
reason = "invalid_route_edge"

or:

reason = "route_edge_disconnected"

as appropriate.

==================================================
4. STATE SAFETY
==================================================

Do not fabricate edges, coordinates, or fallback routes.

Do not reroute inside the movement engine.

Phase 2C-1 owns route generation.

Phase 2C-2 only consumes and validates the existing route.

The movement engine must preserve the no-teleportation invariant:

current edge:
u → v

next edge:
v → x

Valid.

current edge:
u → v

next edge:
x → y

Invalid.

==================================================
5. EVERYTHING ELSE
==================================================

The existing Phase 2C-2 plan is approved.

Implement:

- movement_engine.py
- MovementResult
- deterministic tick_movement()
- multi-edge traversal
- progress updates
- edge transitions
- arrival handling
- movement tests
- phase2c2 diagnostics
- simulation package exports

Do NOT implement:

- WebSockets
- frontend animation
- real-time streaming
- congestion
- flood-aware routing
- weather
- route recalculation
- dynamic graph weights
- AI/ML
- LLM
- resource optimization

After implementation run the backend tests, frontend lint/build,
and inspect git diff.

Do not commit or push.

STOP after Phase 2C-2 and provide the final implementation report.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-12T23:17:31+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/digital-twin/layers/ZoneLayer.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/schemas/.gitkeep (LANGUAGE_UNSPECIFIED)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/main.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/graph/graph_validator.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/dashboard/AlertsPanel.tsx (LANGUAGE_TSX)
Running terminal commands:
- npm run dev (in /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend, running for 1h36m3s)
</ADDITIONAL_METADATA>
<USER_REQUEST>
lets push the work we have done
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-12T23:29:36+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/tests/test_pilgrim_groups.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/tests/test_graph.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/graph/graph_model.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/index.css (LANGUAGE_CSS)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/i18n.ts (LANGUAGE_TYPESCRIPT)
Running terminal commands:
- npm run dev (in /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend, running for 1h48m7s)
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-3A — SIMULATION STATE API

You are working on the VARI OS Digital Twin repository.

IMPORTANT:
Phase 1, Phase 2A, Phase 2B, Phase 2C-1 and Phase 2C-2 are already implemented, tested, and pushed to GitHub.

Do NOT modify their core logic.

The objective of this phase is ONLY to expose the existing deterministic simulation state through a clean backend API.

==================================================
OBJECTIVE
==================================================

Create a read-only Simulation State API that allows the frontend Digital Twin to retrieve the current state of all simulated pilgrim groups.

The API must expose the existing backend simulation state.

It must NOT implement WebSockets yet.

It must NOT implement frontend movement/animation yet.

It must NOT introduce AI/ML.

It must NOT modify the route planner or movement engine.

==================================================
CURRENT ARCHITECTURE
==================================================

Existing pipeline:

OSM geometry
    ↓
Phase 2A NetworkX directed graph
    ↓
Phase 2B PilgrimGroup generation
    ↓
Phase 2C-1 route planning
    ↓
Phase 2C-2 deterministic movement
    ↓
[THIS PHASE]
Simulation State API
    ↓
Future frontend visualization

Existing PilgrimGroup contains the important state:

- group_id
- count
- speed_mps
- progress
- status
- current_edge_id
- route
- destination_zone_id
- updated_at

Use the existing Pydantic model.

Do NOT create a second competing pilgrim model.

==================================================
PHASE 2C-3A SCOPE
==================================================

Implement:

1. Simulation state representation
2. Simulation state manager/service
3. REST endpoint for current simulation state
4. Pydantic response schemas
5. Tests
6. Diagnostic script
7. API documentation/comments where appropriate

The API must be read-only.

==================================================
RECOMMENDED API
==================================================

Create:

GET /api/simulation/state

The exact prefix should follow the repository's existing FastAPI routing conventions if one already exists.

Do not invent a second application entry point.

==================================================
RESPONSE CONTRACT
==================================================

Return a deterministic structured response similar to:

{
  "simulation_time": "...",
  "tick": 0,
  "groups": [
    {
      "group_id": "...",
      "count": 250,
      "speed_mps": 1.05,
      "progress": 0.42,
      "status": "moving",
      "current_edge_id": "...",
      "destination_zone_id": "...",
      "updated_at": "..."
    }
  ]
}

Use proper Pydantic response models.

Do NOT return arbitrary dictionaries if a typed Pydantic schema can be used.

==================================================
IMPORTANT: COORDINATES
==================================================

Do NOT fabricate latitude/longitude.

Do NOT add fake coordinates to PilgrimGroup.

At this stage, the API should expose simulation state based on the existing graph state.

If the frontend later needs exact geographic position, that will be handled in the next visualization phase by resolving:

current_edge_id
+
progress
+
edge geometry

into a map coordinate.

Do NOT implement that interpolation in this phase unless the existing architecture already provides it as a reusable function.

==================================================
SIMULATION STATE MANAGEMENT
==================================================

Create a small dedicated service/module responsible for holding the current simulation state.

The service should:

- initialize the Phase 2B groups
- preserve their current state
- expose a snapshot
- avoid duplicating PilgrimGroup logic
- return safe copies/snapshots rather than allowing the API layer to mutate simulation state

Do NOT create a database dependency for this phase.

Use in-memory state.

This is a hackathon simulation, not production persistence.

==================================================
DETERMINISM
==================================================

Use the existing Phase 2B deterministic seed/configuration.

Do not introduce random behavior into the API.

Two fresh initialization runs with the same configuration should produce equivalent initial simulation state.

==================================================
TICKING RULE
==================================================

CRITICAL:

The GET endpoint must NOT automatically advance the simulation every time it is called.

For example:

GET /api/simulation/state
GET /api/simulation/state
GET /api/simulation/state

must not accidentally move pilgrims three times.

The API is a state reader.

Movement advancement will be introduced through a controlled simulation mechanism in a later phase.

==================================================
OPTIONAL INTERNAL SERVICE METHOD
==================================================

The state manager may expose an internal method such as:

advance(delta_time_s)

which calls the EXISTING Phase 2C-2 tick_movement() function.

However:

- do not automatically call it from GET
- do not modify tick_movement()
- do not implement scheduling
- do not implement WebSockets
- do not create a background loop

The purpose is to prepare a clean boundary for the next phase.

==================================================
VALIDATION
==================================================

Add tests covering at minimum:

1. API returns HTTP 200.
2. Response conforms to Pydantic schema.
3. All expected groups are present.
4. group_id values are unique.
5. current_edge_id is preserved.
6. progress remains within [0,1].
7. status remains one of the existing valid statuses.
8. GET does not mutate simulation state.
9. Two consecutive GET requests return equivalent state when no advance operation occurs.
10. Empty simulation state is handled safely.

If an existing FastAPI test convention exists in the repository, follow it.

==================================================
DIAGNOSTIC
==================================================

Create:

backend/scripts/phase2c3a_diagnostics.py

The diagnostic should:

1. initialize the simulation
2. retrieve the state snapshot
3. print:
   - number of groups
   - total pilgrims
   - simulation timestamp
   - moving/stopped/congested counts
   - unique current edges
4. verify that all states satisfy the expected schema
5. exit non-zero on failure

Do not modify source data as part of diagnostics.

==================================================
NO FRONTEND CHANGES
==================================================

Do NOT modify:

DigitalTwinMap.tsx
PilgrimLayer.tsx
RouteLayer.tsx
any existing frontend components

The frontend remains frozen during 2C-3A.

==================================================
STRICT EXCLUSIONS
==================================================

DO NOT IMPLEMENT:

- WebSockets
- Server-Sent Events
- polling timers
- frontend animation
- coordinate interpolation
- GPS
- real pilgrim tracking
- crowd density
- congestion
- flood-aware routing
- weather
- AI/ML
- LLM
- dynamic route recalculation
- resource optimization
- database persistence
- authentication
- SMS/WhatsApp

These belong to later phases.

==================================================
BACKWARD COMPATIBILITY
==================================================

Before editing anything:

1. Inspect the existing repository.
2. Identify the existing FastAPI app entry point.
3. Identify existing router conventions.
4. Identify existing Pydantic schemas.
5. Identify how Phase 2B groups are currently generated.
6. Identify how Phase 2C-2 tick_movement() is imported.

Do not create duplicate implementations.

After implementation:

- run backend tests
- run Phase 2C-2 tests
- run Phase 2C-1 tests
- run Phase 2B tests
- run frontend lint
- run frontend build

Inspect:

git diff
git status

Confirm that Phase 1 / 2A / 2B / 2C-1 / 2C-2 core files were not unintentionally modified.

==================================================
FILES
==================================================

Prefer a structure similar to:

backend/app/simulation/state_manager.py
backend/app/api/...              # according to existing API convention
backend/tests/test_simulation_state.py
backend/scripts/phase2c3a_diagnostics.py

Do not blindly create these paths if the repository already has an appropriate architecture.

Follow the existing repository structure.

==================================================
IMPORTANT ENGINEERING RULE
==================================================

Do not over-engineer this.

The purpose of 2C-3A is simply:

EXISTING SIMULATION STATE
        ↓
SAFE TYPED API
        ↓
FRONTEND CAN READ IT

Nothing more.

==================================================
STOP CONDITION
==================================================

After implementation:

1. Run all required tests.
2. Run diagnostics.
3. Run frontend lint/build.
4. Inspect git diff.
5. Provide a final Phase 2C-3A implementation report.

Do NOT commit.
Do NOT push.

STOP.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-12T23:34:27+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/dashboard/AlertsPanel.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/data/geospatial/zones.json (LANGUAGE_JSON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/services/.gitkeep (LANGUAGE_UNSPECIFIED)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/simulation/pilgrim_model.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/common/StatusBadge.tsx (LANGUAGE_TSX)
Running terminal commands:
- npm run dev (in /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend, running for 1h52m59s)
</ADDITIONAL_METADATA>
<USER_REQUEST>
APPROVED — proceed with Phase 2C-3A.

The proposed architecture aligns with the repository and the Phase 2C-3A requirements.

Before implementation, apply these three additional constraints:

1. SIMULATION MANAGER LIFECYCLE

SimulationStateManager must initialize once per application instance.

Do NOT rebuild the OSM graph, regenerate pilgrim groups, or rerun route
planning on every GET /api/simulation/state request.

Expected lifecycle:

application startup
    ↓
initialize manager once
    ↓
graph + groups + routes in memory
    ↓
GET /api/simulation/state
    ↓
return snapshot

2. STATE ISOLATION

get_state_snapshot() must return a deep copy / immutable-equivalent
snapshot.

The API caller must never receive references to mutable internal
simulation state.

advance() must operate only on the manager's internal state.

Do not allow the GET endpoint to mutate simulation state.

3. ADD MOVEMENT TEST

In addition to the required API tests, add a test proving that:

initial_state
    ↓
advance(60)
    ↓
state changes

Specifically verify that at least one valid moving group's movement
state changes when sufficient delta_time is provided.

Also retain the separate test proving:

GET /api/simulation/state
GET /api/simulation/state

does NOT advance the simulation.

IMPORTANT:

Do not modify Phase 2C-2 tick_movement().
Use it as-is.

Do not add WebSockets, polling, frontend animation, coordinate
interpolation, GPS, congestion, flood routing, weather, AI/ML,
dynamic weights, database persistence, or resource optimization.

Frontend remains unchanged.

After implementation:

- run Phase 2C-3A tests
- run Phase 2B tests
- run Phase 2C-1 tests
- run Phase 2C-2 tests
- run frontend lint
- run frontend build
- run diagnostics
- inspect git diff
- confirm previous phase core files were not unintentionally modified

Do NOT commit.
Do NOT push.

STOP and provide the final implementation report.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-12T23:37:03+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/App.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/dashboard/RecommendationPanel.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/types/api.ts (LANGUAGE_TYPESCRIPT)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/types/digitalTwin.ts (LANGUAGE_TYPESCRIPT)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/data/geospatial/fetch_routes.py (LANGUAGE_PYTHON)
Running terminal commands:
- npm run dev (in /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend, running for 1h55m35s)
</ADDITIONAL_METADATA>
<USER_REQUEST>
push the work 
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-13T00:41:53+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/graph/graph_model.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/index.css (LANGUAGE_CSS)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/i18n.ts (LANGUAGE_TYPESCRIPT)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/.prettierrc.json (LANGUAGE_JSON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/digital-twin/layers/BaseMapLayer.tsx (LANGUAGE_TSX)
Running terminal commands:
- npm run dev (in /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend, running for 3h0m24s)
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-3B — GEOGRAPHIC POSITION RESOLVER + PILGRIM MAP VISUALIZATION

Phase 1, Phase 2A, Phase 2B, Phase 2C-1, Phase 2C-2 and Phase 2C-3A
are complete and pushed.

DO NOT modify their core logic.

The objective of this phase is to convert the authoritative simulation
state:

    current_edge_id + progress

into a geographic position derived from the REAL OSM edge geometry,
then expose that position through the existing Simulation State API
and render the pilgrim groups at those positions on the Digital Twin.

==================================================
1. INSPECT FIRST
==================================================

Before writing code, inspect:

- routes.json
- graph_builder.py
- graph_model.py
- pilgrim_model.py
- route_planner.py
- movement_engine.py
- state_manager.py
- schemas/simulation.py
- api/simulation.py
- PilgrimLayer.tsx
- DigitalTwinMap.tsx

Determine the ACTUAL:

- edge geometry schema
- coordinate ordering
- graph edge attributes
- edge_id format
- PilgrimGroup schema
- SimulationStateResponse schema
- existing frontend pilgrim layer architecture

Do not assume schemas from previous descriptions.

Do not modify anything during inspection.

==================================================
2. AUTHORITATIVE STATE
==================================================

The authoritative pilgrim position remains:

    current_edge_id
    progress

Do NOT add lat/lng as authoritative mutable fields to PilgrimGroup.

Geographic coordinates are DERIVED state.

Conceptually:

current_edge_id + progress
            ↓
     edge geometry
            ↓
       lat/lng

==================================================
3. POSITION RESOLVER
==================================================

Create a dedicated geographic position resolver in the backend.

Follow the existing project structure.

The resolver should accept:

- graph
- current_edge_id
- progress

and return a validated geographic position or an explicit failure.

Use the ACTUAL graph edge geometry.

Do NOT fetch OSM data again.

Do NOT use an external API.

Do NOT fabricate geometry.

==================================================
4. LINESTRING INTERPOLATION
==================================================

Do NOT interpolate directly between the first and last coordinate.

For a geometry:

P0 → P1 → P2 → P3

calculate cumulative segment distances.

Then:

target_distance =
    progress × total_geometry_length

Find the segment containing target_distance.

Interpolate within that segment.

Therefore the calculated position follows the actual OSM road
geometry, including curves and intermediate vertices.

Use an appropriate geographic distance calculation consistent with
the project's existing Haversine/geospatial utilities.

Do not introduce unnecessary GIS dependencies.

==================================================
5. PROGRESS SEMANTICS
==================================================

Require:

0.0 <= progress <= 1.0

Expected:

progress = 0.0
→ first point of directed geometry

progress = 1.0
→ last point of directed geometry

Intermediate progress:
→ corresponding distance along the LineString

Do not silently clamp invalid progress.

Return an explicit failure for invalid values.

==================================================
6. GEOMETRY DIRECTION
==================================================

CRITICAL.

The graph edge is directed:

u → v

The geometry used for interpolation must correspond to:

u → v

If the stored geometry is reversed relative to the graph direction,
reverse the coordinate sequence IN MEMORY for interpolation.

Do NOT modify routes.json.

Do NOT modify the original graph geometry.

The resolver must ensure:

progress=0
→ near node u

progress=1
→ near node v

Validate this using the actual node coordinates.

Do not assume the geometry is correctly oriented without checking.

==================================================
7. INVALID DATA
==================================================

Never fabricate:

(0,0)

If:

- edge does not exist
- geometry missing
- geometry malformed
- fewer than two coordinates
- invalid latitude
- invalid longitude
- invalid progress
- geometry cannot be reconciled with graph direction

return an explicit failure / null position according to the existing
API architecture.

Do not crash the entire simulation.

==================================================
8. API RESPONSE
==================================================

Extend the existing SimulationStateResponse group representation
so each pilgrim group can expose a derived position.

Conceptually:

{
  "group_id": "...",
  "count": 250,
  "speed_mps": 1.05,
  "progress": 0.42,
  "status": "moving",
  "current_edge_id": "...",
  "destination_zone_id": "...",
  "position": {
      "lat": 18.612345,
      "lng": 73.852341
  }
}

Use the repository's existing Pydantic conventions.

Position must be derived when the state snapshot is generated.

Do NOT persist lat/lng as authoritative simulation state.

If position resolution fails:

position = null

with an appropriate structured reason if compatible with the existing
response schema.

==================================================
9. STATE MANAGER
==================================================

Integrate the position resolver into get_state_snapshot() or an
appropriate service layer.

Do NOT modify:

tick_movement()
plan_route()
PilgrimGroup movement semantics

The state manager should remain responsible for assembling the
read-only state representation.

==================================================
10. FRONTEND
==================================================

Update the existing PilgrimLayer rather than creating a duplicate
pilgrim rendering system.

The frontend should consume the position supplied by the backend.

Do NOT calculate simulation positions in React.

Do NOT use current_edge_id/progress to independently reconstruct
coordinates in the browser.

Backend is authoritative.

==================================================
11. MARKERS
==================================================

For Phase 2C-3B:

one PilgrimGroup = one geographic marker.

Use the existing pilgrim visual language where practical.

Keep the cyan pilgrim representation from the current Digital Twin.

Do not introduce clustering yet.

Do not introduce sophisticated marker animation yet.

Do not introduce 3D rendering.

==================================================
12. SIMULATION UPDATE BEHAVIOR
==================================================

Do NOT add:

- WebSockets
- polling
- browser simulation timers
- automatic backend ticking
- Server-Sent Events

GET /api/simulation/state remains read-only.

For demonstration/testing, state can change only when the existing
SimulationStateManager.advance(delta_time_s) is explicitly invoked.

==================================================
13. NO SMOOTH ANIMATION YET
==================================================

Do NOT animate markers between two backend positions.

Phase 2C-3B only establishes:

simulation state
    ↓
authoritative geographic position
    ↓
correct Leaflet marker

Smooth interpolation between snapshots will be a later concern.

==================================================
14. TESTS
==================================================

Create comprehensive backend tests.

At minimum:

1. progress=0 returns first geometry coordinate
2. progress=1 returns last geometry coordinate
3. midpoint of straight LineString
4. multi-segment LineString interpolation
5. curved geometry follows segments rather than endpoint chord
6. reversed geometry is corrected for directed edge
7. invalid edge
8. missing geometry
9. malformed geometry
10. fewer than two geometry coordinates
11. invalid latitude/longitude
12. invalid progress
13. deterministic repeated resolution
14. API returns position for valid groups
15. API returns null/structured failure for invalid geometry
16. GET state remains read-only
17. advance() changes the resulting geographic position

Use small synthetic graph fixtures for unit tests.

Do not rely only on the 10,438-road production dataset.

==================================================
15. FRONTEND VALIDATION
==================================================

Verify:

- existing map still renders
- zones still render
- routes still render
- road network still renders
- legend still works
- pilgrim markers render at backend-provided positions
- no duplicate pilgrim layers exist
- no React warnings
- no Leaflet layer-toggle regressions

==================================================
16. PERFORMANCE
==================================================

Do not resolve geometry through an OSM API per pilgrim.

Use the already-loaded graph geometry.

For the initial 85-group simulation, straightforward in-memory
resolution is acceptable.

Do not introduce Redis, database storage, or caching yet.

==================================================
17. SCOPE EXCLUSIONS
==================================================

DO NOT IMPLEMENT:

- WebSockets
- polling
- Server-Sent Events
- smooth marker animation
- GPS
- real pilgrim tracking
- crowd density
- congestion
- flood-aware routing
- weather
- dynamic route recalculation
- AI/ML
- LLM
- resource optimization
- database persistence
- clustering
- 3D map

==================================================
18. VALIDATION
==================================================

Run:

cd backend
source .venv/bin/activate
PYTHONPATH=. pytest -v

Then:

cd frontend
npm run lint
npm run build

Run a dedicated Phase 2C-3B diagnostic.

Inspect:

git status
git diff

Confirm Phase 1, Phase 2A, Phase 2B, Phase 2C-1, Phase 2C-2 and
Phase 2C-3A core behavior remains intact.

Do NOT commit.

Do NOT push.

==================================================
19. FINAL REPORT
==================================================

Report:

1. files created
2. files modified
3. actual geometry schema discovered
4. interpolation method
5. direction handling
6. invalid geometry handling
7. API response changes
8. frontend changes
9. number of tests
10. test results
11. diagnostic results
12. lint result
13. build result
14. regression verification
15. explicit scope exclusions

STOP after Phase 2C-3B.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-13T00:44:28+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/App.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/dashboard/RecommendationPanel.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/types/api.ts (LANGUAGE_TYPESCRIPT)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/types/digitalTwin.ts (LANGUAGE_TYPESCRIPT)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/data/geospatial/fetch_routes.py (LANGUAGE_PYTHON)
Running terminal commands:
- npm run dev (in /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend, running for 3h3m0s)
</ADDITIONAL_METADATA>
<USER_REQUEST>
The Phase 2C-3B plan is almost approved, but make these required
corrections before implementation.

1. GEOMETRY DIRECTION VALIDATION

Do not determine geometry orientation only by checking whether the
first point is closest to graph node u.

For directed graph edge:

u → v

compare both geometry endpoints against both graph endpoints.

Let:

P0 = first geometry coordinate
Pn = last geometry coordinate

Evaluate endpoint distances.

The correct orientation should satisfy approximately:

P0 → u
Pn → v

If the stored geometry is reversed:

Pn → u
P0 → v

reverse the coordinate sequence IN MEMORY.

Do NOT modify routes.json or stored graph geometry.

Use the existing Haversine/geospatial utility.

If geometry orientation cannot be reconciled reliably with the graph
edge endpoints, return an explicit resolution failure rather than
fabricating a position.

2. API RESPONSE MODEL

Before implementing:

Inspect the existing SimulationStateResponse and PilgrimGroup schema.

Do not blindly expose the entire internal PilgrimGroup through
inheritance if that would expose internal fields unnecessarily.

In particular, `route` is internal simulation state and does not need
to be sent to the frontend merely to render a marker.

Expose only the fields required by the Digital Twin state contract,
plus:

position: Optional[GeoPosition]

Do not duplicate or create a competing PilgrimGroup model.

3. POSITION VALIDATION

The diagnostic/tests should verify more than lat/lng existence.

For every valid resolved position:

- lat/lng are finite
- lat is within [-90, 90]
- lng is within [-180, 180]
- progress is within [0,1]
- position corresponds to the current edge geometry

Use an appropriate geographic tolerance when checking that the
resolved position lies on/near the current edge geometry.

4. FRONTEND SCOPE

The single GET request on component mount is APPROVED.

Do NOT add:

- polling
- timers
- WebSockets
- SSE
- automatic simulation advancement
- browser-side coordinate interpolation

The frontend should simply render the authoritative position returned
by the backend.

5. KEEP THE EXISTING ARCHITECTURE

Everything else in the existing Phase 2C-3B plan is approved.

Implement:

- position_resolver.py
- geographic interpolation
- direction-safe LineString handling
- derived GeoPosition
- API position field
- PilgrimLayer integration
- tests
- diagnostics

Do NOT modify:

- tick_movement()
- plan_route()
- PilgrimGroup movement semantics
- OSM source data
- Phase 2A graph construction

Do NOT implement WebSockets or real-time movement.

After implementation:

- run all backend tests
- run diagnostics
- run npm lint
- run npm build
- inspect git diff
- confirm previous phases remain intact

Do NOT commit.
Do NOT push.

STOP after Phase 2C-3B and provide the final report.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-13T00:47:31+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/scripts/phase2a_diagnostics.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/locales/en/translation.json (LANGUAGE_JSON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/simulation/__init__.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/graph/__init__.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/fetch_osm_data.py (LANGUAGE_PYTHON)
Running terminal commands:
- npm run dev (in /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend, running for 3h6m3s)
</ADDITIONAL_METADATA>
<USER_REQUEST>
push
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-13T00:53:40+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/graph/graph_model.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/index.css (LANGUAGE_CSS)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/i18n.ts (LANGUAGE_TYPESCRIPT)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/.prettierrc.json (LANGUAGE_JSON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/digital-twin/layers/BaseMapLayer.tsx (LANGUAGE_TSX)
Running terminal commands:
- npm run dev (in /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend, running for 3h12m11s)
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-4A — BACKEND REAL-TIME SIMULATION + WEBSOCKET

Phase 1, Phase 2A, Phase 2B, Phase 2C-1, Phase 2C-2,
Phase 2C-3A and Phase 2C-3B are complete and pushed.

The current architecture is:

OSM geometry
    ↓
NetworkX directed graph
    ↓
PilgrimGroup
    ↓
Route planner
    ↓
Movement engine
    ↓
SimulationStateManager
    ↓
Position resolver
    ↓
GET /api/simulation/state

This phase adds the BACKEND real-time synchronization layer.

==================================================
CORE OBJECTIVE
==================================================

Implement:

ONE authoritative backend simulation loop
        ↓
SimulationStateManager.advance(delta_time_s)
        ↓
position resolution
        ↓
broadcast current simulation state
        ↓
multiple WebSocket clients

The backend must remain the single source of truth.

==================================================
1. INSPECT FIRST
==================================================

Before modifying anything, inspect:

- backend/app/main.py
- backend/app/simulation/state_manager.py
- backend/app/simulation/movement_engine.py
- backend/app/simulation/position_resolver.py
- backend/app/api/simulation.py
- backend/app/schemas/simulation.py
- existing tests
- existing application startup/lifespan handling

Do NOT assume the current FastAPI architecture.

Do not modify anything during inspection.

==================================================
2. WEBSOCKET ENDPOINT
==================================================

Add a WebSocket endpoint following existing FastAPI routing conventions.

Preferred conceptual endpoint:

WS /api/simulation/ws

Do not create a second FastAPI application.

Do not create a separate simulation server.

==================================================
3. SINGLE AUTHORITATIVE SIMULATION
==================================================

There must be exactly ONE SimulationStateManager instance for the
running application.

There must be exactly ONE simulation loop.

All WebSocket clients observe the same simulation state.

Do NOT create:

- one SimulationStateManager per client
- one simulation loop per client
- one simulation clock per client

Expected architecture:

                 SimulationStateManager
                          │
                    ONE simulation loop
                          │
                 current simulation state
                          │
             ┌────────────┼────────────┐
             ↓            ↓            ↓
          Client A     Client B     Client C

==================================================
4. SIMULATION LOOP
==================================================

Implement a controlled backend simulation loop.

Initial tick interval:

1 second

Each tick must call:

state_manager.advance(1.0)

Use the EXISTING Phase 2C-2 movement engine.

Do NOT rewrite tick_movement().

Do NOT calculate movement independently inside the WebSocket layer.

Do NOT use datetime.now() to calculate movement distance.

The movement engine must continue receiving explicit delta_time_s.

==================================================
5. SIMULATION CLOCK
==================================================

Maintain a simulation tick counter.

Example:

tick = 0
advance(1.0)
tick = 1
advance(1.0)
tick = 2

The simulation state should expose:

- simulation_time
- tick

Use the existing state schema conventions.

Do not introduce multiple clocks.

==================================================
6. INITIAL STATE
==================================================

When a WebSocket client connects:

1. accept the connection
2. immediately send the CURRENT simulation state
3. add the connection to the subscriber set
4. wait for subsequent broadcasts

The client must not have to wait for the next simulation tick to
receive its first state.

==================================================
7. BROADCAST
==================================================

After each successful simulation tick:

1. advance the simulation
2. obtain a fresh state snapshot
3. serialize the existing SimulationStateResponse
4. broadcast it to all connected clients

All clients must receive the same state for a given tick.

Do not generate a separate state for each client.

==================================================
8. DISCONNECT HANDLING
==================================================

If a client disconnects:

- remove its WebSocket from the subscriber collection
- do not stop the simulation
- do not affect other clients

Handle WebSocketDisconnect safely.

A broken/stale connection must not crash the simulation loop.

==================================================
9. BROADCAST FAILURE
==================================================

If sending to one client fails:

- remove that client
- continue broadcasting to remaining clients
- do not stop the simulation loop

The simulation must be independent of client health.

==================================================
10. CONCURRENCY / LOCKING
==================================================

Protect simulation state transitions against concurrent access.

Do not allow:

advance()
and
get_state_snapshot()

to observe partially mutated state.

Use an appropriate asyncio/thread-safe mechanism compatible with the
existing architecture.

Do NOT introduce Redis or an external state store.

For this hackathon MVP, in-memory synchronization is sufficient.

==================================================
11. REST API COMPATIBILITY
==================================================

Keep:

GET /api/simulation/state

working exactly as before.

It remains a read-only snapshot endpoint.

The WebSocket implementation must not change its semantics.

==================================================
12. STATE SCHEMA
==================================================

Reuse the existing SimulationStateResponse / group response schema
from Phase 2C-3A/3B.

Do NOT create a duplicate WebSocket response model unless technically
required.

The WebSocket should send the same conceptual state:

{
  "simulation_time": "...",
  "tick": 42,
  "groups": [...]
}

Each group must retain its authoritative:

- group_id
- count
- speed_mps
- progress
- current_edge_id
- destination_zone_id
- status
- derived position

Do not expose internal `route` state unnecessarily.

==================================================
13. ERROR HANDLING
==================================================

The simulation loop must not terminate because:

- one group has an invalid state
- one WebSocket disconnects
- one WebSocket send fails

Handle errors explicitly.

However, do NOT silently fabricate simulation state.

Use structured logging for unexpected failures.

==================================================
14. LIFECYCLE
==================================================

The simulation loop must start and stop with the FastAPI application
lifecycle.

Do NOT create an uncontrolled background thread at import time.

Prefer FastAPI lifespan/startup/shutdown mechanisms already used by the
repository.

On application shutdown:

- stop the simulation loop
- close/cleanup resources
- do not leave orphan tasks

==================================================
15. TESTING
==================================================

Add backend tests covering at minimum:

1. WebSocket endpoint accepts connections.
2. Newly connected client receives an initial state immediately.
3. Initial state has expected schema.
4. Simulation tick advances state.
5. Tick counter increments deterministically.
6. Connected client receives subsequent state updates.
7. Multiple clients receive equivalent state.
8. Disconnecting one client does not stop simulation.
9. Failed client send does not stop simulation.
10. REST GET /state still works.
11. REST GET does not itself advance simulation.
12. Simulation loop does not create multiple simulation instances.
13. Application shutdown stops the simulation task cleanly.
14. State does not expose internal route data.
15. Positions remain valid after movement ticks.

Use FastAPI's testing utilities / WebSocket test client according to
the existing repository conventions.

Do not rely exclusively on timing-sensitive sleeps.

Where possible, make the simulation tick manually triggerable in tests
so tests remain deterministic.

==================================================
16. TESTABILITY
==================================================

Separate:

simulation logic
from
wall-clock scheduling

The core operation should remain conceptually:

advance(delta_time_s)

The scheduler should merely call:

advance(1.0)

This allows deterministic unit tests without waiting one real second.

==================================================
17. DIAGNOSTICS
==================================================

Create:

backend/scripts/phase2c4a_diagnostics.py

The diagnostic should:

1. initialize the application simulation
2. connect one or more test WebSocket clients if practical
3. capture initial tick/state
4. execute controlled simulation ticks
5. verify tick progression
6. verify pilgrim positions change
7. verify multiple subscribers receive equivalent states
8. report connected clients
9. report simulation tick
10. report number of moving groups
11. report total pilgrims
12. report any failures

Do not depend on an external API.

==================================================
18. FRONTEND
==================================================

DO NOT MODIFY the frontend in Phase 2C-4A.

The frontend will subscribe to the WebSocket in Phase 2C-4B.

Keep the current one-shot REST behavior untouched.

==================================================
19. PERFORMANCE
==================================================

Initial target:

1 simulation tick per second.

Do not optimize prematurely.

Do not introduce:

- Redis
- Kafka
- Celery
- external message brokers
- databases
- multiple worker processes

The MVP should remain a single FastAPI process with one
authoritative simulation.

==================================================
20. STRICT EXCLUSIONS
==================================================

DO NOT IMPLEMENT:

- React WebSocket client
- frontend animation
- browser interpolation
- polling
- SSE
- GPS
- real pilgrim tracking
- congestion
- flood-aware routing
- weather
- AI/ML
- LLM
- dynamic routing
- scenario simulation
- resource optimization
- authentication
- persistence

==================================================
21. REGRESSION SAFETY
==================================================

Run:

cd backend
source .venv/bin/activate
PYTHONPATH=. pytest -v

Then:

cd frontend
npm run lint
npm run build

Run Phase 2C-4A diagnostics.

Inspect:

git status
git diff

Confirm:

Phase 1 unchanged
Phase 2A unchanged
Phase 2B unchanged
Phase 2C-1 unchanged
Phase 2C-2 unchanged
Phase 2C-3A unchanged
Phase 2C-3B unchanged except unavoidable integration changes

Do NOT commit.

Do NOT push.

==================================================
22. FINAL REPORT
==================================================

Report:

1. files created
2. files modified
3. WebSocket endpoint
4. simulation loop architecture
5. lifecycle implementation
6. concurrency/state protection
7. client management
8. broadcast behavior
9. test count
10. test results
11. diagnostics
12. lint result
13. build result
14. regression verification
15. explicit scope exclusions

STOP after Phase 2C-4A.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-13T00:57:00+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/scripts/phase2a_diagnostics.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/locales/en/translation.json (LANGUAGE_JSON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/simulation/__init__.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/graph/__init__.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/fetch_osm_data.py (LANGUAGE_PYTHON)
Running terminal commands:
- npm run dev (in /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend, running for 3h15m32s)
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-4A PLAN REVISION REQUIRED BEFORE IMPLEMENTATION

The overall architecture is approved, but make the following changes.

==================================================
1. NEVER HOLD SIMULATION LOCK DURING NETWORK I/O
==================================================

Do NOT do:

async with lock:
    manager.advance()
    snapshot = manager.get_state_snapshot()
    broadcast_to_websockets()

This would hold the simulation lock while performing WebSocket network
I/O.

Instead:

async with lock:
    manager.advance(1.0)
    snapshot = manager.get_state_snapshot()

# LOCK RELEASED HERE

broadcast the immutable/serialized snapshot to WebSocket clients.

A slow client must never block:

- simulation advancement
- REST GET /state
- other clients

==================================================
2. ATOMIC TICK + SNAPSHOT
==================================================

The following operations must occur inside ONE critical section:

advance(1.0)
+
get_state_snapshot()

Conceptually:

LOCK
    advance()
    snapshot()
UNLOCK

Then broadcast the resulting snapshot.

This guarantees that one WebSocket broadcast represents one complete
simulation tick.

==================================================
3. WEBSOCKET MUST NOT REQUIRE CLIENT MESSAGES
==================================================

Do not use application-level receive_text() messages merely to keep
the connection alive.

This WebSocket is currently server → client state streaming.

Lifecycle:

connect
→ accept
→ send current state
→ register subscriber
→ remain connected
→ receive server broadcasts
→ handle disconnect

Do not introduce a client command protocol in Phase 2C-4A.

==================================================
4. MOVE SIMULATION LOOP OUT OF API MODULE
==================================================

Do not place the main SimulationLoop implementation inside:

backend/app/api/simulation.py

Prefer:

backend/app/simulation/simulation_loop.py

The responsibilities should be separated:

simulation_loop.py:
- simulation scheduling
- tick advancement
- subscriber/broadcast coordination if appropriate
- lifecycle

api/simulation.py:
- REST endpoint
- WebSocket transport/connection handling

main.py:
- FastAPI lifespan
- startup/shutdown of SimulationLoop

Follow existing repository conventions if an equivalent service
abstraction already exists.

==================================================
5. PRESERVE DETERMINISTIC MOVEMENT
==================================================

The scheduler may use:

asyncio.sleep(1.0)

as its wall-clock scheduling interval.

But each simulation tick must explicitly call:

manager.advance(1.0)

Do NOT calculate movement delta from wall-clock timestamps.

Do NOT modify tick_movement().

==================================================
6. SHARED SINGLE SIMULATION
==================================================

Exactly one:

SimulationStateManager
SimulationLoop
simulation clock

per FastAPI application instance.

All WebSocket clients observe the same state.

Do NOT create per-client simulation state.

==================================================
7. STATE ISOLATION
==================================================

REST GET and WebSocket broadcast must receive snapshots/copies.

Do not expose mutable internal simulation objects.

The snapshot produced inside the lock becomes the immutable state
payload for that tick.

==================================================
8. BROADCAST FAILURE
==================================================

If one WebSocket send fails:

- remove that client
- continue sending to all other clients
- do not stop simulation
- do not hold the simulation lock while handling the failure

==================================================
9. WEBSOCKET INITIAL STATE
==================================================

On connection:

1. accept
2. acquire the appropriate state synchronization
3. obtain a current snapshot
4. release synchronization
5. send initial snapshot
6. register client safely

Ensure there is no race where a client is registered twice or receives
unexpectedly inconsistent state.

If a tick occurs between snapshot and registration, this is acceptable;
the next broadcast will bring the client current.

Do not require the client to send a message.

==================================================
10. TESTING ADDITIONS
==================================================

In addition to the existing proposed tests, add tests for:

8. GET /api/simulation/state does not advance the simulation.
9. Multiple clients observe the same simulation tick/state.
10. One failed/slow client does not stop broadcasts to healthy clients.
11. Simulation loop shuts down cleanly with FastAPI lifespan.
12. One authoritative SimulationLoop exists per application.
13. Tick + snapshot are produced atomically from the simulation's point
    of view.

Avoid timing-sensitive tests where possible.

Expose a testable/manual tick mechanism so tests can advance one tick
without sleeping one real second.

==================================================
11. DIAGNOSTICS
==================================================

The diagnostic should demonstrate:

- initial tick
- tick +1
- tick +2
- position changes
- multiple clients receiving equivalent state
- healthy clients continue receiving updates if another client fails

==================================================
12. FRONTEND
==================================================

DO NOT MODIFY frontend files.

Phase 2C-4A is backend-only.

==================================================
13. STRICT EXCLUSIONS
==================================================

Do not implement:

- React WebSocket client
- marker animation
- frontend interpolation
- polling
- SSE
- GPS
- congestion
- flood routing
- weather
- AI/ML
- LLM
- dynamic routing
- scenario engine
- pause/resume controls
- simulation speed UI
- persistence
- authentication

==================================================
14. VALIDATION
==================================================

Run:

cd backend
source .venv/bin/activate
PYTHONPATH=. pytest -v

Run Phase 2C-4A diagnostics.

Then:

cd frontend
npm run lint
npm run build

Inspect:

git diff
git status

Confirm previous phase core logic remains intact.

DO NOT commit.
DO NOT push.

STOP after implementation and provide the final report.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-13T01:00:52+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/types/digitalTwin.ts (LANGUAGE_TYPESCRIPT)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/data/geospatial/fetch_routes.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/tests/test_pilgrim_groups.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/tests/test_graph.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/graph/graph_model.py (LANGUAGE_PYTHON)
Running terminal commands:
- npm run dev (in /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend, running for 3h19m24s)
</ADDITIONAL_METADATA>
<USER_REQUEST>
Before we commit Phase 2C-4A, inspect POST /api/simulation/dev/tick.

Confirm exactly:
1. Is it development/test-only?
2. Can it be called in normal production mode?
3. Does it mutate the authoritative SimulationLoop state?
4. Is it guarded by an environment/configuration check?
5. Does it expose any security or production risk?

Do not modify anything. Report only.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-13T01:12:51+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/types/digitalTwin.ts (LANGUAGE_TYPESCRIPT)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/data/geospatial/fetch_routes.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/tests/test_pilgrim_groups.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/tests/test_graph.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/graph/graph_model.py (LANGUAGE_PYTHON)
Running terminal commands:
- npm run dev (in /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend, running for 3h31m23s)
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-4A SECURITY CORRECTION — DEV TICK ENDPOINT

The implementation review found a production issue with:

POST /api/simulation/dev/tick

Current behavior:

- endpoint is registered unconditionally
- endpoint is accessible in production
- no authentication/authorization protects it
- endpoint mutates the authoritative SimulationLoop state
- endpoint broadcasts the resulting state to all WebSocket clients

This MUST be fixed before Phase 2C-4A is committed.

==================================================
REQUIRED FIX
==================================================

Make the manual development tick endpoint available ONLY when the
application is explicitly running in development/test mode.

Use the project's existing configuration/environment conventions if
they exist.

If no configuration system exists, introduce the smallest clean
configuration mechanism necessary.

Example conceptual behavior:

APP_ENV=development
    → POST /api/simulation/dev/tick exists

APP_ENV=test
    → available only if useful for automated testing

APP_ENV=production
    → endpoint MUST NOT be registered

Do NOT merely hide the endpoint from documentation or frontend code.

It must not exist as a routable production endpoint.

==================================================
SECURITY REQUIREMENT
==================================================

In production:

POST /api/simulation/dev/tick

must return a normal "not found" routing response because the route
should not be registered.

Do NOT add authentication merely to protect this development endpoint.

The preferred solution is conditional route registration.

==================================================
PRESERVE TESTABILITY
==================================================

Do NOT remove SimulationLoop.manual_tick() if existing tests or
diagnostics depend on it.

The internal method may remain available to backend tests.

Only the PUBLIC HTTP development endpoint must be conditionally
registered.

==================================================
TESTS
==================================================

Add/modify tests proving:

1. Development environment:
   POST /api/simulation/dev/tick is available.

2. Development endpoint:
   calling it advances exactly one simulation tick.

3. Production environment:
   POST /api/simulation/dev/tick is NOT registered.

4. Production request:
   endpoint returns HTTP 404.

5. Production simulation:
   normal automatic 1-second SimulationLoop continues operating.

6. WebSocket behavior remains unchanged.

7. GET /api/simulation/state remains unchanged.

8. Existing deterministic tests continue passing.

Do not weaken the tests by simply mocking away the endpoint.

==================================================
CONFIGURATION
==================================================

Inspect the existing repository configuration before introducing new
settings.

Do not introduce unnecessary configuration frameworks.

Keep the implementation minimal and consistent with the current
FastAPI architecture.

==================================================
REGRESSION
==================================================

Run:

cd backend
source .venv/bin/activate
PYTHONPATH=. pytest -v

Run Phase 2C-4A diagnostics.

Then:

cd frontend
npm run lint
npm run build

Inspect:

git diff
git status

Do NOT commit.
Do NOT push.

Report:

- exact configuration mechanism used
- development behavior
- production behavior
- test results
- diagnostics
- lint
- build
- confirmation that the authoritative SimulationLoop remains unchanged

STOP after this correction.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-13T01:13:59+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/dashboard/RecommendationPanel.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/types/api.ts (LANGUAGE_TYPESCRIPT)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/types/digitalTwin.ts (LANGUAGE_TYPESCRIPT)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/data/geospatial/fetch_routes.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/tests/test_pilgrim_groups.py (LANGUAGE_PYTHON)
Running terminal commands:
- npm run dev (in /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend, running for 3h32m31s)
</ADDITIONAL_METADATA>
<USER_REQUEST>
push
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-13T01:18:43+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/fetch_osm_data.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/digital-twin/DigitalTwinMap.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/dashboard/SimulationPanel.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/layout/AppShell.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/layout/Sidebar.tsx (LANGUAGE_TSX)
Running terminal commands:
- npm run dev (in /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend, running for 3h37m15s)
</ADDITIONAL_METADATA>
<USER_REQUEST>
We are starting Phase 2C-4B — React WebSocket Client + Live Digital Twin.

Phase 2C-4A has been completed and pushed.

DO NOT modify any files yet.

Inspect the current repository and report the existing implementation of:

BACKEND
- backend/app/main.py
- backend/app/api/simulation.py
- backend/app/simulation/simulation_loop.py
- backend/app/simulation/state_manager.py
- backend/app/schemas/simulation.py

FRONTEND
- frontend/src/components/digital-twin/DigitalTwinMap.tsx
- frontend/src/components/digital-twin/layers/PilgrimLayer.tsx
- frontend/src/types/digitalTwin.ts
- relevant API/service/type files

Determine exactly:

1. Current WebSocket endpoint and message format.
2. Exact SimulationStateResponse JSON structure sent over WebSocket.
3. How the WebSocket connection lifecycle is currently handled server-side.
4. How the current PilgrimLayer obtains simulation state.
5. Whether there is currently any WebSocket client code.
6. Current TypeScript interfaces for simulation state and pilgrim groups.
7. How PilgrimLayer is mounted/unmounted by DigitalTwinMap.
8. Whether there are existing React state-management conventions we should follow.
9. Whether any current mock pilgrim data remains.
10. Whether the existing REST /api/simulation/state endpoint should remain as fallback/initial-state support.
11. Any existing error/loading UI that can be reused.
12. Potential duplicate WebSocket connection risks.
13. Potential React StrictMode development behavior affecting WebSocket connections.
14. Any existing CORS/API-base configuration relevant to WebSocket URLs.

Also inspect the current git status and confirm the working tree is clean.

DO NOT edit files.
DO NOT create files.
DO NOT run implementation changes.

Return an inspection report only.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-13T19:39:38+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/scripts/phase2b_diagnostics.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/digital-twin/layers/BaseMapLayer.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/tests/test_graph.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/graph/graph_validator.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/fetch_osm_data.py (LANGUAGE_PYTHON)
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-4B — REACT WEBSOCKET CLIENT + LIVE DIGITAL TWIN

Phase 2C-4A is complete and pushed.

The backend currently provides:

WS /api/simulation/ws

The WebSocket sends JSON serialized SimulationStateResponse messages.

The frontend currently performs a one-time:

GET http://localhost:8000/api/simulation/state

inside PilgrimLayer.tsx.

Phase 2C-4B must replace the Digital Twin's one-shot simulation
snapshot behavior with a WebSocket-driven live state stream.

==================================================
1. INSPECTED ARCHITECTURE
==================================================

Current WebSocket payload:

{
  "simulation_time": "...",
  "tick": 0,
  "groups": [
    {
      "group_id": "...",
      "count": 100,
      "speed_mps": 1.5,
      "progress": 0.5,
      "status": "moving",
      "current_edge_id": "...",
      "destination_zone_id": "...",
      "updated_at": "...",
      "position": {
        "lat": 18.123,
        "lng": 73.123
      }
    }
  ]
}

position may be null.

Current PilgrimLayer is mounted/unmounted through the
React-Leaflet LayersControl.

Toggling the pilgrim layer therefore causes component
mount/unmount cycles.

React StrictMode may also cause development mount/unmount/remount
cycles.

==================================================
2. PRIMARY OBJECTIVE
==================================================

Implement:

WebSocket server
      ↓
React WebSocket client
      ↓
validated simulation state
      ↓
PilgrimLayer
      ↓
Leaflet markers

The backend remains the ONLY simulation authority.

The frontend must NEVER calculate pilgrim movement.

==================================================
3. API CONFIGURATION
==================================================

Do NOT hardcode:

http://localhost:8000

inside PilgrimLayer.tsx.

Create the smallest appropriate frontend API configuration utility
consistent with the current Vite project.

Use:

VITE_API_BASE_URL

with development fallback if appropriate.

Example:

VITE_API_BASE_URL=http://localhost:8000

Derive WebSocket protocol correctly:

http:// → ws://
https:// → wss://

Result:

ws://localhost:8000/api/simulation/ws

Do not introduce a large configuration framework.

Do not add unnecessary dependencies.

==================================================
4. WEBSOCKET CLIENT
==================================================

Create a clean WebSocket client implementation.

The client must:

1. construct the correct WebSocket URL
2. open the connection
3. receive the initial snapshot
4. receive subsequent snapshots
5. validate incoming data
6. update React state
7. handle errors
8. handle close
9. clean up correctly

Do NOT use polling.

Do NOT use setInterval.

Do NOT use fetch repeatedly.

==================================================
5. REACT LIFECYCLE
==================================================

PilgrimLayer is mounted/unmounted when the map layer is toggled.

Therefore:

mount
    ↓
open WebSocket

unmount
    ↓
close WebSocket

The useEffect cleanup MUST call:

ws.close()

Prevent leaked connections.

React 18 StrictMode must also be handled correctly:

Mount
→ WebSocket A
→ cleanup
→ WebSocket A closed
→ remount
→ WebSocket B

There must NOT be two active connections caused by StrictMode.

Use a useRef if necessary to maintain the current WebSocket instance.

==================================================
6. MESSAGE VALIDATION
==================================================

Do not blindly trust:

JSON.parse(event.data)

Validate the minimum required structure.

At minimum verify:

- message is an object
- tick is numeric
- groups is an array
- group_id exists
- count is numeric
- position is either null or an object
- position.lat is finite
- position.lng is finite

Invalid messages must be ignored safely and logged appropriately.

Do not crash the component.

Do not fabricate coordinates.

==================================================
7. TYPESCRIPT TYPES
==================================================

Do not keep the simulation response interfaces unnecessarily
localized inside PilgrimLayer.tsx.

Create/reuse appropriate shared TypeScript types in the existing
frontend type structure.

Define types corresponding to the actual backend response:

SimulationState
PilgrimGroupState
GeoPosition

Do not create conflicting duplicate interfaces.

Do not expose backend-only internal route data beyond what the
backend actually sends.

==================================================
8. REST ENDPOINT
==================================================

KEEP:

GET /api/simulation/state

Do not delete it.

The WebSocket becomes the primary source for the live Digital Twin,
but REST remains useful for:

- debugging
- health verification
- future consumers
- fallback architecture

Do NOT add REST polling.

Do NOT fetch REST repeatedly.

==================================================
9. PILGRIM STATE
==================================================

Replace the current one-shot fetch-driven pilgrim state with
WebSocket-driven state.

The state should be:

groups from the most recent valid WebSocket message.

Do not maintain a second independently simulated state.

Do not mutate individual group positions manually.

==================================================
10. LEAFLET MARKERS
==================================================

Render:

one PilgrimGroup
    =
one CircleMarker

Use:

group.group_id

as the React key.

Use backend-provided:

position.lat
position.lng

as the Leaflet position.

If position is null:

do not render that marker.

Do not use fallback coordinates.

Do not calculate coordinates from:

current_edge_id
progress

in the frontend.

The backend Position Resolver remains authoritative.

==================================================
11. MAP INTEGRATION
==================================================

Preserve all existing map behavior:

- OSM basemap
- zones
- route layers
- road network
- map controls
- legend
- existing layer visibility

Do not modify unrelated DigitalTwinMap functionality.

Only modify PilgrimLayer and the minimal shared frontend types/config
required for WebSocket integration.

==================================================
12. CONNECTION STATES
==================================================

Track enough connection state to support debugging and future UI.

At minimum distinguish:

connecting
connected
error/closed

Do not build a large UI for this phase.

Console logging is acceptable for development.

Do not add noisy logs every second.

==================================================
13. RECONNECTION
==================================================

Implement a conservative reconnect strategy if appropriate.

Do NOT create an aggressive infinite reconnect loop.

If reconnect is implemented:

- use a small bounded/backoff delay
- clear pending reconnect timers during cleanup
- do not reconnect after intentional component unmount
- do not create multiple simultaneous sockets

If the current project does not require automatic reconnect yet,
a clean connection-error/close state is acceptable.

Prioritize correctness over complexity.

==================================================
14. IMPORTANT: NO FRONTEND SIMULATION
==================================================

Absolutely do NOT implement:

setInterval()
requestAnimationFrame()
advance()
speed × delta_time
progress calculations
route traversal
coordinate interpolation

The browser only renders backend state.

==================================================
15. NO SMOOTH ANIMATION YET
==================================================

When backend sends:

tick 10 → position A

then:

tick 11 → position B

the marker may update directly from A → B.

Do NOT add smooth animation in this phase.

Smooth visual interpolation will be implemented separately after
raw WebSocket synchronization is proven reliable.

==================================================
16. TESTING
==================================================

Add appropriate frontend tests if the repository has a frontend test
framework.

If there is no existing frontend test framework, do not introduce a
large testing framework solely for this phase.

At minimum manually/diagnostically verify:

1. WebSocket opens.
2. Initial backend state appears.
3. Subsequent backend ticks update markers.
4. Marker position changes after simulation advancement.
5. Multiple groups render.
6. Group with null position is not rendered.
7. Invalid WebSocket message does not crash the component.
8. Component unmount closes the WebSocket.
9. React StrictMode does not leave duplicate connections.
10. Toggling the pilgrim layer OFF/ON does not leak sockets.
11. Existing map layers remain functional.
12. No mock pilgrim coordinates remain.

==================================================
17. BROWSER VERIFICATION
==================================================

Run the application and verify:

1. Open Digital Twin.
2. Enable Synthetic Pilgrims.
3. WebSocket connection opens.
4. Pilgrim markers appear on actual roads.
5. Backend simulation advances.
6. Markers move to subsequent backend positions.
7. Disable Synthetic Pilgrims.
8. WebSocket closes.
9. Enable it again.
10. A fresh WebSocket connection opens.
11. No duplicate marker sets appear.
12. No duplicate WebSocket connections remain.

Use browser developer tools if useful.

==================================================
18. BACKEND REGRESSION
==================================================

Do NOT modify the backend simulation algorithm.

Do NOT modify:

- SimulationStateManager
- SimulationLoop
- MovementEngine
- PositionResolver
- RoutePlanner
- graph builder
- OSM data

unless an unavoidable API-contract compatibility issue is discovered.

If such an issue is discovered, STOP and report it instead of
silently modifying backend simulation logic.

==================================================
19. VALIDATION
==================================================

Run backend:

cd backend
source .venv/bin/activate
PYTHONPATH=. pytest -v

Run frontend:

cd frontend
npm run lint
npm run build

Inspect:

git status
git diff

Confirm:

Phase 1 unchanged
Phase 2A unchanged
Phase 2B unchanged
Phase 2C-1 unchanged
Phase 2C-2 unchanged
Phase 2C-3A unchanged
Phase 2C-3B unchanged
Phase 2C-4A backend unchanged

Do NOT commit.
Do NOT push.

==================================================
20. FINAL REPORT
==================================================

Report:

1. files created
2. files modified
3. WebSocket client architecture
4. API URL configuration
5. TypeScript state models
6. connection lifecycle
7. cleanup behavior
8. StrictMode handling
9. message validation
10. marker rendering behavior
11. reconnection behavior
12. tests/verification
13. lint result
14. build result
15. browser verification
16. regression verification
17. explicit scope exclusions

STOP after Phase 2C-4B.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-13T19:51:01+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/digital-twin/layers/ZoneLayer.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/schemas/.gitkeep (LANGUAGE_UNSPECIFIED)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/scripts/phase2c3a_diagnostics.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/common/StatusBadge.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/dashboard/KpiCard.tsx (LANGUAGE_TSX)
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-4B FINAL VERIFICATION — DO NOT MODIFY IMPLEMENTATION

Before Phase 2C-4B is committed, perform an inspection-only
verification.

1. GIT / ENVIRONMENT

Run:

git status --short

git check-ignore -v frontend/.env

Confirm whether frontend/.env is ignored.

If it is not ignored, DO NOT modify anything yet. Report this as a
blocking issue.

2. WEBSOCKET LIFECYCLE

Inspect PilgrimLayer.tsx and verify:

- WebSocket is created exactly once per component mount.
- cleanup calls ws.close().
- layer OFF causes the connection to close.
- layer ON creates a new connection.
- React StrictMode does not leave two active sockets.
- old WebSocket callbacks cannot mutate state belonging to a newer
  WebSocket connection.

Pay particular attention to stale onmessage/onclose/onerror callbacks.

Do not assume useRef alone guarantees this.

3. BROWSER VERIFICATION

Actually run the backend and frontend.

Open the Digital Twin.

Enable Synthetic Pilgrims.

Verify:

- WebSocket connects.
- initial state arrives.
- pilgrim markers appear.
- at least one specific group_id changes geographic position across
  multiple backend simulation ticks.
- markers remain positioned on/near the corresponding OSM road.
- multiple groups render.
- groups with null positions are not rendered.

Record concrete observations:

initial tick
later tick
group_id
initial lat/lng
later lat/lng

4. LAYER LIFECYCLE

Verify:

Synthetic Pilgrims ON
→ one WebSocket connection

Synthetic Pilgrims OFF
→ connection closes

Synthetic Pilgrims ON again
→ new connection

Confirm there are no duplicate active connections or duplicate marker
sets.

5. FRONTEND CONFIGURATION

Verify:

VITE_API_BASE_URL

is read from Vite environment configuration and is not hardcoded
inside PilgrimLayer.tsx.

Verify:

http:// → ws://
https:// → wss://

6. BACKEND REGRESSION

Do NOT modify backend code.

Run:

cd backend
source .venv/bin/activate
PYTHONPATH=. pytest -v

7. FRONTEND

Run:

cd frontend
npm run lint
npm run build

8. DIFF

Run:

git diff
git status

Do not commit.
Do not push.

Return a verification report only.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-13T21:35:53+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/scripts/phase2c4a_diagnostics.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/tests/test_position_resolver.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/tests/test_simulation_state.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/dashboard/AlertsPanel.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/tests/test_simulation_ws.py (LANGUAGE_PYTHON)
Browser State:
  Page 91EFD520943CA7B08DB4713736A54416 (VARI OS) - http://localhost:5173/ [ACTIVE]
    Viewport: 1470x835, Page Height: 835
</ADDITIONAL_METADATA>
<USER_REQUEST>
Now I would **approve Phase 2C-4B for commit/push**.

The verification addressed the important blockers.

### Final 4B status

| Check                                       | Result                 |
| ------------------------------------------- | ---------------------- |
| WebSocket connects                          | ✅                      |
| Initial state received                      | ✅                      |
| Actual pilgrim movement observed            | ✅                      |
| Backend → frontend position synchronization | ✅                      |
| Layer OFF closes socket                     | ✅                      |
| Layer ON creates fresh socket               | ✅                      |
| `.env` ignored                              | ✅                      |
| Hardcoded API URL removed                   | ✅                      |
| Backend regression                          | ✅ 48 passed, 2 skipped |
| Frontend lint                               | ✅                      |
| Frontend build                              | ✅                      |
| Backend simulation untouched                | ✅                      |
| Frontend simulation logic                   | ❌ none                 |
| Polling                                     | ❌ none                 |
| Animation/interpolation                     | ❌ none                 |

The most important evidence is this actual transition:

```text
G001

Tick 1589
lat = 18.661820364
lng = 73.868169217

        ↓ WebSocket

Tick 1590
lat = 18.661825859
lng = 73.868169047
```

That proves we're no longer looking at a static frontend mock. The browser is receiving successive authoritative backend positions.

## One correction to the report

This statement:

> "`ws.close()` ... terminates synchronously"

isn't technically something I'd rely on.

`close()` initiates the WebSocket closing procedure; the actual close event/network teardown is asynchronous.

The **implementation is still fine** because the cleanup is present and the component lifecycle prevents the old socket from being intentionally retained. Just don't use "synchronously terminated" as a technical claim in your documentation.

Also, the report says:

```text
48 passed, 2 skipped, 4 warnings
```

So don't describe this as "all tests passed" without qualification.

Correct wording:

> **48 tests passed, 2 tests skipped, with 4 warnings.**

The skipped tests should eventually be understood/fixed, but they are not a blocker for this phase based on the verification you've shown.

---

# Push it

Before committing, I recommend one final:

```bash
git diff --stat
git status
```

Make sure the changes are only:

```text
frontend/src/components/digital-twin/layers/PilgrimLayer.tsx
frontend/src/config/api.ts
frontend/src/types/simulation.ts
```

and that `frontend/.env` is **not** staged.

Then:

```bash
git add \
  frontend/src/components/digital-twin/layers/PilgrimLayer.tsx \
  frontend/src/config/api.ts \
  frontend/src/types/simulation.ts

git commit -m "feat(digital-twin): stream pilgrim simulation over websocket"

git push origin digital_twin
```

Don't use `git add .` here. You've got a clean, tightly scoped phase; keep it that way.

---

# Your Digital Twin now has a major milestone

You've gone from:

```text
OSM roads
   ↓
static map
```

to:

```text
Real OSM geometry
       ↓
Directed NetworkX graph
       ↓
Synthetic pilgrim groups
       ↓
Route planner
       ↓
Deterministic movement engine
       ↓
Geographic position resolver
       ↓
Simulation State Manager
       ↓
1 Hz Simulation Loop
       ↓
WebSocket
       ↓
React
       ↓
Leaflet
       ↓
LIVE SYNTHETIC PILGRIM MOVEMENT
```

That is a legitimate end-to-end Digital Twin pipeline.

### And importantly, you have not cheated the architecture

The browser isn't pretending to move pilgrims.

The browser doesn't calculate:

```text
speed × time
```

The browser doesn't know the route graph.

The browser doesn't interpolate coordinates.

The backend owns the simulation.

That's exactly the separation we wanted.

**Push 2C-4B.**

After that, don't start the next phase immediately. First we'll decide what **2C-5** should actually accomplish, because this is the point where adding random "AI" or visual features could easily destroy the technically clean foundation you've built.

</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-13T22:01:20+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/scripts/phase2b_diagnostics.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/digital-twin/layers/BaseMapLayer.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/tests/test_graph.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/graph/graph_validator.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/fetch_osm_data.py (LANGUAGE_PYTHON)
Browser State:
  Page 91EFD520943CA7B08DB4713736A54416 (VARI OS) - http://localhost:5173/ [ACTIVE]
    Viewport: 1470x835, Page Height: 835
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-5 — DETERMINISTIC CROWD AGGREGATION & NETWORK LOAD ENGINE

Phase 2C-4B is COMPLETE and PUSHED.

The current system now has:

Phase 1       → real OSM road geometry
Phase 2A      → directed NetworkX road graph
Phase 2B      → deterministic synthetic pilgrim groups
Phase 2C-1    → route planning
Phase 2C-2    → deterministic movement engine
Phase 2C-3A   → authoritative simulation state
Phase 2C-3B   → geographic position resolver
Phase 2C-4A   → 1 Hz backend simulation loop + WebSocket
Phase 2C-4B   → React WebSocket live pilgrim visualization

DO NOT modify anything yet.

This phase is the foundation for future crowd-risk intelligence.

==================================================
PHASE OBJECTIVE
==================================================

Implement a deterministic backend-only:

CROWD AGGREGATION & NETWORK LOAD ENGINE

The purpose is to answer:

"How many simulated pilgrims and pilgrim groups are currently
occupying each directed road edge?"

The engine must derive crowd load ONLY from the authoritative
SimulationStateManager state.

No external data.
No AI.
No ML.
No LLM.
No prediction.
No fabricated road capacity.
No stampede prediction.

==================================================
CORE PRINCIPLE
==================================================

The SimulationStateManager remains the single source of truth.

The Crowd Aggregator is a READ-ONLY DERIVED-STATE computation.

Architecture:

SimulationStateManager
        │
        │ authoritative PilgrimGroup states
        ▼
Crowd Aggregator
        │
        ├── edge pilgrim count
        ├── edge group count
        ├── active edge count
        └── deterministic load metrics
        │
        ▼
Crowd State Snapshot

The aggregator MUST NOT mutate:

- PilgrimGroup
- current_edge_id
- progress
- route
- graph
- SimulationStateManager

==================================================
IMPORTANT DEFINITION
==================================================

For Phase 2C-5:

"Edge load" means the total number of simulated pilgrims whose
current_edge_id corresponds to that directed graph edge.

Example:

G001 → edge E1 → count 300
G002 → edge E1 → count 450
G003 → edge E1 → count 200

Then:

E1.pilgrim_count = 950
E1.group_count = 3

Do NOT infer physical pedestrian density yet.

Do NOT divide by road length and call the result "people/m²".

Do NOT infer road capacity.

Do NOT use OSM highway type as a capacity value.

==================================================
CURRENT EDGE ONLY
==================================================

A group contributes its full `count` to exactly ONE edge:

group.current_edge_id

Do NOT distribute the group between edges using `progress`.

Do NOT count future `route` edges.

Do NOT count destination zones.

Do NOT count geographic proximity.

The current edge is authoritative.

==================================================
EDGE ID VALIDATION
==================================================

Inspect the existing graph representation carefully.

Use the existing graph's authoritative edge identifiers.

Do not reconstruct edge IDs manually if an existing mapping/helper
already exists.

If a PilgrimGroup references an edge ID that does not exist in the
graph:

- do NOT create an edge
- do NOT silently attach it to another edge
- do NOT crash the entire simulation
- report it deterministically as an invalid/unresolved edge assignment

The behavior must be explicit and testable.

==================================================
STATUS HANDLING
==================================================

Inspect the existing PilgrimGroup status model:

moving
stopped
congested

Do NOT invent additional statuses.

For the first implementation, all groups with a valid current_edge_id
may contribute to edge load regardless of whether their status is
moving/stopped/congested.

However, expose enough information in the aggregate result to
distinguish:

- active groups
- stopped groups
- congested groups

Do not silently discard stopped/congested groups.

The aggregate should remain descriptive rather than predictive.

==================================================
REQUIRED AGGREGATION OUTPUT
==================================================

Design a Pydantic response model appropriate for the existing
backend architecture.

At minimum, provide:

CrowdEdgeLoad:

- edge_id
- pilgrim_count
- group_count
- moving_group_count
- stopped_group_count
- congested_group_count

And a CrowdSummary containing:

- total_pilgrims
- total_groups
- active_edge_count
- moving_group_count
- stopped_group_count
- congested_group_count
- unresolved_group_count

"active_edge_count" means the number of edges with at least one
valid assigned pilgrim group.

"unresolved_group_count" means groups whose current_edge_id cannot
be resolved to an existing graph edge.

Do NOT invent a percentage-based congestion value yet.

==================================================
DETERMINISM
==================================================

Given the same:

SimulationStateManager state
+
Graph

the Crowd Aggregator MUST always return exactly the same result.

No:

- randomness
- timestamps
- ML
- external API
- wall-clock dependence

Sort edge results deterministically by edge_id.

==================================================
ZERO-LOAD EDGES
==================================================

Do NOT return every one of the ~98,000 graph edges by default.

The primary aggregate result should contain only edges currently
occupied by at least one pilgrim group.

This keeps the state compact.

If a future map visualization requires the complete network,
that should be a separate operation.

==================================================
SIMULATION INTEGRATION
==================================================

Do NOT modify the movement engine.

Do NOT modify the route planner.

Do NOT modify the graph builder.

Do NOT modify OSM geometry.

Do NOT create a second simulation loop.

The aggregator should derive information from the existing
SimulationStateManager.

Prefer a clean service/module such as:

backend/app/simulation/crowd_aggregator.py

Use the existing repository architecture and naming conventions
where appropriate.

==================================================
API DESIGN
==================================================

Do NOT immediately expose the crowd aggregate through WebSocket
unless necessary for this phase.

First implement and test the aggregation engine independently.

If an API endpoint is useful for deterministic verification, propose
a minimal endpoint such as:

GET /api/simulation/crowd

BUT:

DO NOT implement it until the architecture is inspected and the
response schema is clear.

The existing WebSocket payload must NOT be changed in this phase
unless there is a compelling compatibility requirement.

==================================================
NO FRONTEND CHANGES
==================================================

Phase 2C-5 is BACKEND ONLY.

Do NOT modify:

- DigitalTwinMap.tsx
- PilgrimLayer.tsx
- RouteLayer.tsx
- frontend types
- frontend API utilities

The frontend visualization will be a later phase.

==================================================
NO "CONGESTION SCORE"
==================================================

This is critical.

DO NOT create arbitrary formulas such as:

load / 1000
load / road_length
count × speed
count / capacity

and label the result:

"congestion"

We do not currently have validated road capacity or pedestrian
density data.

For Phase 2C-5 we are calculating:

CROWD LOAD

not:

CROWD RISK

not:

CONGESTION PREDICTION

not:

STAMPEDE RISK

==================================================
FUTURE COMPATIBILITY
==================================================

The output should be designed so future phases can add:

- normalized congestion indicators
- flow rate
- speed variance
- bottleneck detection
- flood impact
- weather impact
- risk prediction
- resource recommendation

without rewriting the basic aggregation engine.

==================================================
TESTING REQUIREMENTS
==================================================

Create comprehensive unit tests.

At minimum test:

1. Empty simulation state.
2. One group on one edge.
3. Multiple groups on the same edge.
4. Groups on different edges.
5. Correct pilgrim count aggregation.
6. Correct group count aggregation.
7. Moving group classification.
8. Stopped group classification.
9. Congested group classification.
10. Mixed statuses on the same edge.
11. Invalid current_edge_id.
12. Multiple invalid groups.
13. No mutation of input PilgrimGroup objects.
14. No mutation of graph.
15. Deterministic output ordering.
16. Zero-load edges are excluded.
17. Large group count values remain correct.
18. All groups unresolved.
19. Combination of valid and unresolved groups.
20. Total summary values equal the edge-level aggregates where
    appropriate.

Use small synthetic NetworkX graphs in unit tests.

Do NOT use the full 48,967-node production graph for basic unit tests.

==================================================
DIAGNOSTICS
==================================================

Create a dedicated diagnostic script:

backend/scripts/phase2c5_diagnostics.py

It should:

1. Build/load the existing authoritative graph.
2. Generate the existing deterministic 85 pilgrim groups.
3. Route them using the existing Phase 2C-1 mechanism if required.
4. Obtain their authoritative current state.
5. Run the Crowd Aggregator.
6. Print:

- total groups
- total pilgrims
- occupied edge count
- top 10 occupied edges by pilgrim count
- maximum edge pilgrim count
- number of unresolved groups
- moving groups
- stopped groups
- congested groups

The diagnostic must use the existing seed/state conventions.

Do NOT fabricate output values.

==================================================
INTEGRATION SAFETY
==================================================

Before modifying anything:

Inspect:

- state_manager.py
- simulation_loop.py
- simulation.py
- schemas/simulation.py
- pilgrim_model.py
- graph_model.py
- graph_builder.py

Determine the correct integration point.

Do not duplicate existing functionality.

Do not create a second representation of pilgrim state.

==================================================
REGRESSION PROTECTION
==================================================

Phase 1 through Phase 2C-4B are frozen.

Do NOT modify:

- OSM raw data
- routes.json
- zones.json
- graph builder
- graph validator
- pilgrim generator
- pilgrim model
- route planner
- movement engine
- position resolver
- SimulationLoop
- WebSocket implementation
- PilgrimLayer

unless an unavoidable dependency is discovered.

If such a dependency is discovered:

STOP and report it instead of silently modifying previous phases.

==================================================
VALIDATION
==================================================

Run:

cd backend
source .venv/bin/activate
PYTHONPATH=. pytest -v

Then:

PYTHONPATH=. python3 scripts/phase2c5_diagnostics.py

Then:

cd frontend
npm run lint
npm run build

Finally:

git diff
git status

Confirm previous phases are unchanged.

==================================================
GIT
==================================================

DO NOT commit.

DO NOT push.

DO NOT stage files.

==================================================
FINAL REPORT
==================================================

Return:

1. Files created.
2. Files modified.
3. Exact aggregation algorithm.
4. Exact CrowdEdgeLoad schema.
5. Exact CrowdSummary schema.
6. Invalid edge behavior.
7. Status handling.
8. Determinism guarantees.
9. API changes, if any.
10. Test count.
11. Test results.
12. Diagnostic results.
13. Frontend status.
14. Lint result.
15. Build result.
16. Regression verification.
17. Explicit exclusions.

STOP after Phase 2C-5.

Do not proceed to any subsequent phase automatically.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-13T22:04:52+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/index.css (LANGUAGE_CSS)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/scripts/phase2c3b_diagnostics.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/simulation/position_resolver.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/simulation/generator.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/layout/Header.tsx (LANGUAGE_TSX)
Browser State:
  Page 91EFD520943CA7B08DB4713736A54416 (VARI OS) - http://localhost:5173/ [ACTIVE]
    Viewport: 1470x835, Page Height: 835
</ADDITIONAL_METADATA>
<USER_REQUEST>
push 
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-13T22:16:06+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/digital-twin/layers/BaseMapLayer.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/tests/test_graph.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/graph/graph_validator.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/fetch_osm_data.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/data/geospatial/fetch_routes.py (LANGUAGE_PYTHON)
Browser State:
  Page 91EFD520943CA7B08DB4713736A54416 (VARI OS) - http://localhost:5173/ [ACTIVE]
    Viewport: 1470x835, Page Height: 835
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-6 — DYNAMIC CROWD LOAD VISUALIZATION
STEP 1: ARCHITECTURE INSPECTION ONLY

Phase 2C-5 is COMPLETE and PUSHED.

Current frozen architecture:

Phase 1       → OSM geospatial foundation
Phase 2A      → directed NetworkX graph
Phase 2B      → synthetic pilgrim groups
Phase 2C-1    → route planner
Phase 2C-2    → deterministic movement engine
Phase 2C-3A   → simulation state manager/API
Phase 2C-3B   → geographic position resolver
Phase 2C-4A   → 1 Hz simulation loop + WebSocket
Phase 2C-4B   → React WebSocket live pilgrim visualization
Phase 2C-5    → deterministic crowd state aggregation

DO NOT MODIFY ANY FILES.
DO NOT CREATE ANY FILES.
DO NOT COMMIT.
DO NOT PUSH.

This is an inspection-only task.

==================================================
OBJECTIVE
==================================================

We are planning Phase 2C-6:

DYNAMIC CROWD LOAD VISUALIZATION

The goal is eventually:

authoritative pilgrim state
        ↓
Crowd Aggregator
        ↓
CrowdEdgeLoad
        ↓
WebSocket / appropriate state transport
        ↓
React
        ↓
OSM road network visualization

The visualization should communicate:

"Which real OSM road edges currently contain the highest
number of simulated pilgrims?"

It must NOT yet claim:

- physical pedestrian density
- road capacity
- percentage congestion
- stampede risk
- crowd danger prediction

This phase is visualization of measured/derived simulated load,
not prediction.

==================================================
1. INSPECT PHASE 2C-5
==================================================

Inspect:

backend/app/simulation/crowd_aggregator.py
backend/app/schemas/crowd.py
backend/tests/test_crowd_aggregator.py
backend/scripts/phase2c5_diagnostics.py

Report:

- exact CrowdEdgeLoad schema
- exact CrowdSummary schema
- how edge IDs map to NetworkX edges
- whether aggregation currently receives groups directly or
  SimulationStateManager state
- whether aggregation is computationally cheap enough to run
  once per simulation tick
- whether the aggregator mutates anything
- whether any caching currently exists

==================================================
2. INSPECT PHASE 2C-4A
==================================================

Inspect:

backend/app/simulation/simulation_loop.py
backend/app/api/simulation.py
backend/app/schemas/simulation.py

Determine:

- exact current WebSocket response schema
- exact broadcast flow
- where the simulation snapshot is generated
- where crowd aggregation could logically be calculated
- whether adding crowd state to the existing WebSocket snapshot would
  preserve atomicity
- whether crowd aggregation would unnecessarily increase message size
- whether the current WebSocket architecture can safely carry the
  additional edge-level crowd data

Do NOT modify the WebSocket yet.

==================================================
3. INSPECT PHASE 2C-4B
==================================================

Inspect:

frontend/src/components/digital-twin/layers/PilgrimLayer.tsx
frontend/src/components/digital-twin/DigitalTwinMap.tsx
frontend/src/types/simulation.ts
frontend/src/config/api.ts

Determine:

- how WebSocket state currently enters React
- how SimulationState is currently stored
- how often React receives updates
- how the current PilgrimLayer is mounted/unmounted
- whether the WebSocket connection belongs only to PilgrimLayer
- whether a separate crowd visualization layer can reuse the same
  WebSocket state without creating a second WebSocket connection

IMPORTANT:

We must NOT create:

WebSocket A → Pilgrims
WebSocket B → Crowd

unless there is a strong architectural reason.

Prefer one authoritative simulation WebSocket connection per
Digital Twin instance.

==================================================
4. INSPECT ROAD NETWORK VISUALIZATION
==================================================

Inspect:

frontend/src/components/digital-twin/layers/RouteLayer.tsx
frontend/src/components/digital-twin/DigitalTwinMap.tsx
frontend/src/data/geospatial/routes.json
any existing road-network layer/component
any existing map legend/control implementation

Determine:

- how OSM roads are rendered
- how each road feature is identified
- whether the frontend has access to the backend edge_id
- whether routes.json feature properties contain the same edge ID
- whether the backend edge_id can be directly matched to the
  frontend GeoJSON road feature
- how road styling is currently controlled
- whether zoom-dependent styling exists
- whether operational route colors could conflict with crowd-load
  visualization

DO NOT modify these files.

==================================================
5. CRITICAL EDGE-ID COMPATIBILITY CHECK
==================================================

This is mandatory.

Trace one real example end-to-end:

Backend:

CrowdEdgeLoad.edge_id

        ↓

NetworkX edge

        ↓

OSM way / graph edge

        ↓

routes.json

        ↓

GeoJSON feature

        ↓

Leaflet road feature

Determine whether the identifiers match exactly.

Example format may resemble:

E_R_OSM_<osm_way_id>_<u>_<v>

Do NOT assume the IDs match.

Report the exact mapping.

If they do NOT match, explain the cleanest deterministic mapping strategy.

DO NOT change the IDs in this inspection phase.

==================================================
6. VISUALIZATION OPTIONS ANALYSIS
==================================================

Evaluate these possible approaches:

OPTION A:
Change road stroke intensity/width based on pilgrim load.

OPTION B:
Overlay a separate colored line layer above the OSM road network.

OPTION C:
Use a heatmap.

OPTION D:
Use circles/markers at road centers.

Compare them specifically for this project.

Consider:

- 10,438 OSM road features
- potentially thousands of edges
- current Leaflet architecture
- existing zoom-dependent road visibility
- route layer semantics
- map readability
- performance
- ability to identify exact loaded road segments

Do NOT implement any option yet.

Recommend ONE approach.

My default preference is likely a dedicated road-segment overlay,
but verify this against the existing implementation rather than
assuming.

==================================================
7. PAYLOAD ARCHITECTURE ANALYSIS
==================================================

Compare:

OPTION A:

Existing WebSocket payload:

{
  simulation_time,
  tick,
  groups
}

becomes:

{
  simulation_time,
  tick,
  groups,
  crowd
}

OPTION B:

Keep WebSocket pilgrim payload unchanged and create a separate
crowd endpoint.

OPTION C:

Create a separate WebSocket channel for crowd state.

Evaluate:

- payload size
- update frequency
- consistency
- synchronization
- frontend complexity
- number of connections
- future scalability

Recommend ONE.

IMPORTANT:

We want pilgrim state and crowd state corresponding to the SAME
simulation tick.

Therefore the recommendation must explicitly consider atomicity.

==================================================
8. PERFORMANCE ANALYSIS
==================================================

Estimate:

- number of graph edges ≈ 98,621
- number of currently occupied edges ≈ number of active groups
- number of groups currently ≈ 85

Determine whether calculating CrowdEdgeLoad every 1-second simulation
tick is reasonable.

Remember:

The aggregator only returns occupied edges.

We do NOT want to serialize all 98,621 edges every second.

Estimate the expected WebSocket payload size for the current
85-group simulation.

Do NOT optimize prematurely.

==================================================
9. MAP SEMANTICS
==================================================

Recommend how crowd load should be visually represented.

For example:

low load
medium load
high load
highest load

BUT:

Do NOT assign arbitrary "congestion percentages".

Do NOT call these values:

- safe
- dangerous
- critical
- stampede risk

unless there is a defensible basis.

For this phase the terminology should remain:

"Pilgrim Load"

or:

"Simulated Pilgrim Load"

The visualization should communicate relative/current load,
not safety prediction.

==================================================
10. LEGEND / UI
==================================================

Inspect the existing legend and map controls.

Recommend:

- where "Simulated Pilgrim Load" should appear
- whether a toggle should be added
- whether crowd visualization should be independently enabled/disabled
- how it should coexist with:
  - Road Network
  - Routes
  - Zones
  - Synthetic Pilgrims

Do NOT modify UI yet.

==================================================
11. NO NEW FEATURES
==================================================

Explicitly exclude:

- AI
- ML
- LLM
- crowd prediction
- flood
- weather
- GPS
- CCTV
- road capacity
- pedestrian density
- stampede prediction
- emergency recommendation
- resource optimization
- route recalculation
- dynamic routing
- scenario simulation
- smooth animation

Phase 2C-6 is ONLY:

CURRENT SIMULATED PILGRIM LOAD VISUALIZATION.

==================================================
12. REGRESSION CHECK
==================================================

Confirm that Phase 1 through Phase 2C-5 are currently frozen.

Inspect git status.

Do not modify anything.

==================================================
FINAL OUTPUT
==================================================

Return an inspection report containing:

1. Phase 2C-5 architecture findings.
2. Current WebSocket architecture.
3. Current frontend state architecture.
4. Exact edge-ID mapping.
5. Road-layer architecture.
6. WebSocket payload analysis.
7. Performance analysis.
8. Visualization option comparison.
9. Recommended visualization approach.
10. Recommended data-transport approach.
11. Recommended React architecture.
12. Recommended legend/control design.
13. Potential integration risks.
14. Exact files that would need modification.
15. Exact files that should remain untouched.
16. Proposed Phase 2C-6 architecture diagram.
17. Proposed implementation sequence.

STOP.

Do NOT implement Phase 2C-6.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-13T22:22:03+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/.prettierrc.json (LANGUAGE_JSON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/main.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/tests/test_route_planner.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/tests/test_pilgrim_groups.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/App.tsx (LANGUAGE_TSX)
Browser State:
  Page 91EFD520943CA7B08DB4713736A54416 (VARI OS) - http://localhost:5173/ [ACTIVE]
    Viewport: 1470x835, Page Height: 835
</ADDITIONAL_METADATA>
<USER_REQUEST>
The Phase 2C-6 inspection is approved in principle, but revise the
implementation plan before writing any code.

IMPORTANT ARCHITECTURAL CORRECTIONS:

1. DO NOT implement a runtime endpoint that dynamically serves the
   entire edge geometry dataset on every request.

The inspection correctly discovered that backend graph edges:

E_R_OSM_<way_id>_<u>_<v>

do not directly match frontend routes.json:

R_OSM_<way_id>

because the backend graph splits OSM Ways into node-to-node directed
edges.

We need a clean static processed representation for frontend
visualization.

Evaluate creating:

frontend/src/data/geospatial/edge_geometry.json

or an equivalent processed static asset containing:

edge_id
geometry

derived directly from the authoritative Phase 2A graph.

The geometry must NOT be manually fabricated.

It must be generated from the existing graph edge geometry.

Do not duplicate or modify routes.json.

Do not change existing edge IDs.

Do not modify Phase 2A graph construction.

The frontend should load this static edge geometry once.

2. KEEP THE SINGLE WEBSOCKET ARCHITECTURE.

Lift the WebSocket state management out of PilgrimLayer.tsx.

Recommended architecture:

DigitalTwinMap
    ↓
useSimulationStream()
    ↓
SimulationState
    ├── groups → PilgrimLayer
    └── crowd  → CrowdLayer

Toggling PilgrimLayer or CrowdLayer must NOT create or destroy the
authoritative simulation WebSocket.

The WebSocket lifetime should follow the Digital Twin/map lifecycle.

3. EXTEND SimulationState WITH CROWD STATE.

Use a clean structure similar to:

SimulationState {
    simulation_time
    tick
    groups
    crowd
}

CrowdState should contain:

    edges: CrowdEdgeLoad[]
    summary: CrowdSummary

Do not duplicate the existing CrowdEdgeLoad definitions.

Reuse the backend schema concepts consistently.

4. MAINTAIN ATOMICITY.

Inside the existing SimulationLoop lock:

    manager.advance()
    pilgrim_snapshot = manager.get_state_snapshot()
    crowd_state = aggregate_crowd(...)

Then construct ONE SimulationStateResponse.

The resulting:

groups
+
crowd

must represent the exact same simulation tick.

Do not calculate crowd state outside the authoritative locked
simulation state transition.

5. KEEP THE CROWD AGGREGATION BACKEND-OWNED.

The frontend must NOT calculate:

crowd / length
crowd / capacity
crowd × speed
density
congestion
risk

The frontend receives already aggregated edge load.

6. KEEP THE VISUALIZATION TERMINOLOGY PRECISE.

Use:

"Simulated Pilgrim Load"

or:

"Relative Simulated Pilgrim Load"

Do NOT use:

"Congestion"
"Danger"
"Critical"
"Stampede Risk"
"Capacity"

because Phase 2C-6 does not contain a calibrated safety model.

7. DO NOT USE ABSOLUTE LOAD THRESHOLDS.

Do not define arbitrary thresholds such as:

0–100 = low
101–300 = medium
301–500 = high

Instead, if normalization is required for visualization, use the
current occupied-edge maximum:

relative_load = edge.pilgrim_count / max_current_edge_load

This is ONLY visual normalization.

Clearly document that it represents relative simulated load and not
physical capacity or safety risk.

8. CROWD LAYER.

Create:

frontend/src/components/digital-twin/layers/CrowdLayer.tsx

It should:

- consume crowd state from the shared simulation stream
- resolve edge_id against static edge geometry
- render only currently occupied edges
- use independent styling from RouteLayer
- never modify RouteLayer operational status colors
- never perform simulation calculations
- safely ignore unresolved edge geometry

Use an efficient Leaflet representation appropriate for the current
scale.

9. STATIC EDGE GEOMETRY.

Before implementation, determine the exact generation mechanism for:

edge_geometry.json

It must be derived from the existing NetworkX graph and preserve:

- edge_id
- directed geometry
- [lon, lat] source geometry convention

The frontend must convert correctly to Leaflet [lat, lon].

Do not manually fabricate any geometry.

10. WEBSOCKET PAYLOAD SIZE.

Only transmit the edge-level fields actually required by CrowdLayer
for visualization.

At minimum:

edge_id
pilgrim_count

Keep the richer CrowdEdgeLoad backend schema available for backend
logic and future decision intelligence.

Do not transmit 98,621 edges.

Transmit only currently occupied edges.

11. PRESERVE ALL PREVIOUS PHASES.

Phase 1 through Phase 2C-5 are frozen.

Do not modify:

- routes.json
- zones.json
- graph_builder.py
- graph_validator.py
- graph_model.py
- pilgrim_model.py
- generator.py
- route_planner.py
- movement_engine.py
- position_resolver.py
- existing simulation movement logic

unless an unavoidable integration dependency is discovered.

If such a dependency exists, STOP and report it.

12. BEFORE IMPLEMENTATION

Revise implementation_plan.md only.

Do not write implementation code yet.

The revised plan must specify:

- exact files to create
- exact files to modify
- static edge geometry generation strategy
- WebSocket schema changes
- shared React state architecture
- CrowdLayer architecture
- Leaflet rendering strategy
- normalization formula
- legend design
- performance strategy
- tests
- regression checks

Then STOP and ask for approval.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-13T22:35:23+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/i18n.ts (LANGUAGE_TYPESCRIPT)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/digital-twin/layers/PilgrimLayer.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/digital-twin/layers/RouteLayer.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/store/dashboardStore.ts (LANGUAGE_TYPESCRIPT)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/locales/en/translation.json (LANGUAGE_JSON)
Browser State:
  Page 91EFD520943CA7B08DB4713736A54416 (VARI OS) - http://localhost:5173/ [ACTIVE]
    Viewport: 1470x835, Page Height: 835
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-6 — REQUIRED PLAN CORRECTIONS BEFORE IMPLEMENTATION

I reviewed the Phase 2C-6 implementation plan.

The overall architecture is APPROVED, but make the following two
changes to implementation_plan.md BEFORE writing any implementation
code.

DO NOT implement Phase 2C-6 yet.
DO NOT create files.
DO NOT modify application code.
DO NOT commit.
DO NOT push.

Only revise the implementation plan and then stop for approval.

==================================================
CHANGE 1 — useSimulationStream.ts IS REQUIRED
==================================================

Current plan says:

frontend/src/hooks/useSimulationStream.ts
(Optional but recommended)

Change this to:

frontend/src/hooks/useSimulationStream.ts
(REQUIRED)

Reason:

The WebSocket must no longer belong to PilgrimLayer.

The required architecture is:

DigitalTwinMap
      │
      ▼
useSimulationStream()
      │
      ▼
SimulationState
   ┌──┴──────┐
   ▼         ▼
Pilgrim   Crowd
Layer     Layer

The hook must own:

- WebSocket creation
- WebSocket message handling
- JSON/state validation
- connection lifecycle
- cleanup
- StrictMode-safe teardown
- simulation state storage/update mechanism

DigitalTwinMap owns the lifecycle of the hook.

PilgrimLayer and CrowdLayer are presentation components only.

They must NOT independently create WebSocket connections.

The implementation plan must explicitly state:

"Exactly one authoritative simulation WebSocket connection exists
per mounted Digital Twin instance."

Layer toggling must NOT affect the WebSocket lifecycle.

Therefore:

PilgrimLayer OFF
→ PilgrimLayer unmounts
→ WebSocket remains connected

CrowdLayer OFF
→ CrowdLayer unmounts
→ WebSocket remains connected

Both layers OFF
→ WebSocket still remains connected as long as DigitalTwinMap
   remains mounted.

DigitalTwinMap unmount
→ useSimulationStream cleanup
→ WebSocket closes.

==================================================
CHANGE 2 — EXPLICIT UNRESOLVED EDGE GEOMETRY HANDLING
==================================================

Add a dedicated subsection to the CrowdLayer architecture.

The CrowdLayer will receive:

CrowdEdgeLoad.edge_id

and resolve:

edge_id
   ↓
edge_geometry.json
   ↓
geometry

If an edge_id exists in the WebSocket crowd state but does NOT exist
in edge_geometry.json, the frontend MUST:

1. NOT fabricate geometry.
2. NOT use routes.json as a fallback.
3. NOT search for the nearest OSM Way.
4. NOT approximate the location.
5. NOT create a marker at an arbitrary coordinate.
6. NOT crash the CrowdLayer.
7. Skip that individual edge deterministically.
8. Optionally emit a development-only diagnostic warning.

The missing geometry of one edge must never prevent valid crowd edges
from rendering.

Add this explicitly to the implementation plan.

==================================================
CHANGE 3 — ADD GEOMETRY RESOLUTION DIAGNOSTICS
==================================================

Extend the Phase 2C-6 validation requirements.

The implementation/diagnostic process must report:

- total edge geometries generated
- edge geometry JSON file size
- number of CrowdEdgeLoad entries received
- number of edge IDs successfully resolved to geometry
- number of unresolved edge IDs
- unresolved edge IDs, if any
- percentage of crowd edges successfully rendered/resolved

Do NOT fabricate these values.

These must come from actual execution.

The diagnostic should clearly distinguish:

"unresolved geometry"

from:

"unoccupied edge"

An unoccupied edge is expected and should NOT be considered an error.

==================================================
CHANGE 4 — STATIC GEOMETRY MUST REMAIN AUTHORITATIVE
==================================================

Keep the existing strategy:

backend NetworkX graph
        ↓
generate_edge_geometry.py
        ↓
edge_geometry.json
        ↓
frontend CrowdLayer

The geometry must be extracted directly from the authoritative
Phase 2A graph.

Do NOT:

- manually fabricate geometry
- modify routes.json
- reconstruct geometry from route_id
- create a runtime geometry API
- use nearest-road approximation
- modify Phase 2A graph construction

Preserve the existing graph geometry convention:

backend:
[lon, lat]

Leaflet:
[lat, lon]

The implementation plan must explicitly require this conversion.

==================================================
CHANGE 5 — KEEP EVERYTHING ELSE UNCHANGED
==================================================

Do NOT change the approved decisions regarding:

- atomic crowd + pilgrim state
- single WebSocket architecture
- sparse crowd payload
- occupied edges only
- CrowdLayer as a separate Polyline layer
- relativeLoad normalization
- no absolute crowd thresholds
- no physical density
- no road capacity
- no congestion prediction
- no stampede risk
- no AI/ML/LLM
- no frontend simulation calculations
- no changes to previous frozen phases

==================================================
REQUIRED FINAL PLAN
==================================================

After applying these corrections, update implementation_plan.md so
that it explicitly contains:

1. Required useSimulationStream hook.
2. DigitalTwinMap-owned WebSocket lifecycle.
3. PilgrimLayer as presentation-only.
4. CrowdLayer as presentation-only.
5. Explicit unresolved geometry behavior.
6. Geometry resolution diagnostics.
7. Static edge_geometry.json strategy.
8. [lon,lat] → [lat,lon] conversion.
9. Atomic WebSocket crowd + pilgrim state.
10. Sparse occupied-edge payload.
11. Relative simulated load normalization.
12. Existing regression protection.

Then STOP.

Do NOT implement anything.

Do NOT create files.

Do NOT commit.

Do NOT push.

Wait for my explicit approval before starting implementation.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-14T00:26:42+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/layout/Sidebar.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/locales/mr/translation.json (LANGUAGE_JSON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/services/.gitkeep (LANGUAGE_UNSPECIFIED)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/simulation/simulation_loop.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/api/simulation.py (LANGUAGE_PYTHON)
Browser State:
  Page 91EFD520943CA7B08DB4713736A54416 (VARI OS) - http://localhost:5173/ [ACTIVE]
    Viewport: 1470x835, Page Height: 835
</ADDITIONAL_METADATA>
Comments on artifact URI: file:///Users/bhavikesh/.gemini/antigravity-ide/brain/9be1aeae-ecc3-4449-98d2-734481b50fb4/implementation_plan.md

The user has approved this document.


<USER_REQUEST>

</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-14T00:40:07+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/schemas/.gitkeep (LANGUAGE_UNSPECIFIED)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/scripts/phase2c3a_diagnostics.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/common/StatusBadge.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/dashboard/KpiCard.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/graph/__init__.py (LANGUAGE_PYTHON)
Browser State:
  Page 91EFD520943CA7B08DB4713736A54416 (VARI OS) - http://localhost:5173/ [ACTIVE]
    Viewport: 1470x835, Page Height: 835
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-6 — FINAL CONSISTENCY CHECK ONLY

Do NOT modify implementation code yet.
Do NOT commit.
Do NOT push.

There is an inconsistency between the task tracker and walkthrough.

The task tracker currently shows:

[/] 2. Backend: Modify backend/app/schemas/simulation.py to include crowd state.

But the walkthrough claims:

"Extended the SimulationStateResponse to natively serialize the crowd
snapshot atomically inside the same simulation lock."

Verify the actual repository state.

Specifically inspect:

backend/app/schemas/simulation.py
backend/app/simulation/simulation_loop.py
backend/app/api/simulation.py
backend/tests/test_simulation_ws.py

Confirm:

1. Does SimulationStateResponse actually contain `crowd`?
2. Is the crowd field correctly typed using the existing Crowd schema?
3. Does the actual WebSocket response contain `crowd`?
4. Does crowd correspond to the same simulation tick as groups?
5. Does test_simulation_ws.py explicitly verify crowd exists?
6. Does the task tracker incorrectly show item 2 as incomplete?
7. Run the relevant WebSocket tests and report the actual result.

Also verify:

8. edge_geometry.json contains exactly 98,621 edge geometries.
9. Its actual file size.
10. Current live CrowdEdgeLoad entries.
11. Successful geometry resolutions.
12. Unresolved geometry count.
13. Frontend lint.
14. Frontend build.
15. Full backend pytest result.

IMPORTANT:
Do not change any implementation unless an actual defect is found.

If the implementation is correct and only the task checkbox is stale,
mark task 2 complete and report that this was a documentation/task-state
inconsistency.

Do NOT commit.
Do NOT push.

STOP after reporting the results.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-14T00:52:28+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/fetch_osm_data.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/data/geospatial/fetch_routes.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/.gitignore (LANGUAGE_UNSPECIFIED)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/simulation/route_planner.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/i18n.ts (LANGUAGE_TYPESCRIPT)
Browser State:
  Page 91EFD520943CA7B08DB4713736A54416 (VARI OS) - http://localhost:5173/ [ACTIVE]
    Viewport: 1470x835, Page Height: 835
</ADDITIONAL_METADATA>
<USER_REQUEST>
where are pilgrim and add hide on the lable
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-14T00:56:49+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/scripts/phase2a_diagnostics.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/graph/graph_builder.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/simulation/pilgrim_model.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/tests/test_movement_engine.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/layout/Sidebar.tsx (LANGUAGE_TSX)
Browser State:
  Page 91EFD520943CA7B08DB4713736A54416 (VARI OS) - http://localhost:5173/ [ACTIVE]
    Viewport: 1470x835, Page Height: 835
</ADDITIONAL_METADATA>
<USER_REQUEST>
push
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-14T00:59:54+05:30.

The user's current state is as follows:
Other open documents:
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/services/.gitkeep (LANGUAGE_UNSPECIFIED)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/simulation/simulation_loop.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/backend/app/api/simulation.py (LANGUAGE_PYTHON)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/components/dashboard/RecommendationPanel.tsx (LANGUAGE_TSX)
- /Users/bhavikesh/Documents/Hackathon/Varithon2026/frontend/src/index.css (LANGUAGE_CSS)
Browser State:
  Page 91EFD520943CA7B08DB4713736A54416 (VARI OS) - http://localhost:5173/ [ACTIVE]
    Viewport: 1470x835, Page Height: 835
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-6 — FINAL PRE-COMMIT VERIFICATION

Before starting any new phase, perform a final inspection of the
current Phase 2C-6 implementation.

DO NOT implement Phase 2C-7.
DO NOT create new feature code.
DO NOT modify backend simulation logic.
DO NOT commit or push.

==================================================
1. GIT STATE
==================================================

Run:

git status
git diff --stat
git diff

Identify every modified/untracked file.

Separate them into:

A. Required Phase 2C-6 changes
B. The recent Legend Hide/Show UI change
C. Unexpected/unrelated changes

Do not discard anything automatically.

==================================================
2. VERIFY LIVE DIGITAL TWIN
==================================================

Start the current backend and frontend if necessary.

Verify that the browser receives the CURRENT Phase 2C-6
SimulationState WebSocket payload containing:

simulation_time
tick
groups
crowd

Confirm:

- Pilgrim markers appear.
- Crowd load road segments appear.
- Both update as simulation ticks advance.
- No frontend mock data is being used.

==================================================
3. WEBSOCKET LIFECYCLE TEST
==================================================

Verify the shared WebSocket architecture.

Test:

A. Digital Twin opens
   → exactly one simulation WebSocket.

B. Pilgrim layer ON/OFF
   → WebSocket remains alive.

C. Crowd Load layer ON/OFF
   → WebSocket remains alive.

D. Both layers ON again
   → latest simulation state renders.

E. Digital Twin unmounts
   → WebSocket closes.

Do not create additional WebSocket connections.

==================================================
4. CROWD GEOMETRY VERIFICATION
==================================================

Verify:

edge_id
    ↓
edge_geometry.json
    ↓
CrowdLayer
    ↓
Leaflet Polyline

Confirm:

- 98,621 static edge geometries exist.
- Current occupied edges resolve correctly.
- No routes.json fallback exists.
- No nearest-road fallback exists.
- No fabricated coordinates exist.
- Unresolved edges are skipped safely.

==================================================
5. LEGEND CHANGE
==================================================

Inspect the recent Legend Hide/Show modification.

Verify:

- Legend can be hidden.
- "Show Legend" restores it.
- Leaflet controls remain usable.
- Layer toggles remain usable.
- No map layout regression exists.
- No unrelated styling has changed.

If the legend change is correct, classify it as an intended
Phase 2C-6 UI enhancement.

==================================================
6. TESTS
==================================================

Run:

cd backend
source .venv/bin/activate
PYTHONPATH=. pytest -v

Then:

cd ../frontend
npm run lint
npm run build

Report exact results.

==================================================
7. REGRESSION CHECK
==================================================

Confirm no changes have leaked into:

Phase 1
Phase 2A
Phase 2B
Phase 2C-1
Phase 2C-2
Phase 2C-3A
Phase 2C-3B
Phase 2C-4A
Phase 2C-4B
Phase 2C-5

Do not modify those phases.

==================================================
8. FINAL REPORT
==================================================

Return:

1. Git status.
2. Modified files.
3. Phase 2C-6 files.
4. Legend change assessment.
5. WebSocket lifecycle result.
6. Pilgrim rendering result.
7. Crowd rendering result.
8. Geometry resolution result.
9. Backend test result.
10. Frontend lint result.
11. Frontend build result.
12. Regression result.
13. Whether the repository is safe to commit.

STOP.

Do not commit.
Do not push.
Do not start Phase 2C-7.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-14T18:34:34+05:30.
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-7 — TEMPORAL CROWD STATE / PREDICTION FOUNDATION
STEP 1: INSPECTION ONLY

Phase 2C-6 is COMPLETE, COMMITTED, PUSHED, and the working tree is
clean.

DO NOT modify any application code.
DO NOT create files.
DO NOT commit.
DO NOT push.
DO NOT implement Phase 2C-7.

We are now investigating the architecture required for the next
capability.

==================================================
OBJECTIVE
==================================================

We need to determine how the current Digital Twin can retain a
deterministic temporal history of crowd state.

Current pipeline:

OSM network
    ↓
Directed graph
    ↓
Pilgrim groups
    ↓
Routing
    ↓
Movement
    ↓
Position resolution
    ↓
Simulation state
    ↓
Crowd aggregation
    ↓
WebSocket
    ↓
Digital Twin

Phase 2C-6 currently provides the current crowd state only.

We now need to determine whether and how we should retain previous
crowd states so that future prediction/intelligence can be based on
actual temporal simulation data.

==================================================
1. INSPECT CURRENT SIMULATION STATE
==================================================

Inspect:

backend/app/simulation/state_manager.py
backend/app/simulation/simulation_loop.py
backend/app/schemas/simulation.py
backend/app/schemas/crowd.py
backend/app/simulation/crowd_aggregator.py

Determine:

- what state exists only for the current tick
- whether previous ticks are retained anywhere
- whether simulation tick numbers are deterministic
- whether simulation_time is deterministic
- whether crowd aggregation can be reproduced from a past state
- whether the current state manager is suitable for maintaining a
  bounded history

DO NOT modify anything.

==================================================
2. DETERMINE REQUIRED TEMPORAL DATA
==================================================

Determine what minimum information should be retained per tick.

Evaluate whether we need:

- tick
- simulation_time
- edge_id
- pilgrim_count
- group_count
- moving_group_count
- stopped_group_count
- congested_group_count

Do NOT add fields merely because they might be useful.

For every proposed field, explain why it is necessary.

==================================================
3. MEMORY ANALYSIS
==================================================

Estimate memory requirements for:

A. 60 seconds of history
B. 5 minutes
C. 15 minutes
D. 1 hour

Use the current system:

- approximately 85 groups
- occupied edges only
- approximately 1 tick/second

Do not store all 98,621 graph edges for every tick.

History must be sparse and contain occupied edges only.

==================================================
4. HISTORY ARCHITECTURE OPTIONS
==================================================

Compare:

OPTION A:
In-memory bounded deque inside SimulationStateManager.

OPTION B:
Separate in-memory TemporalStateManager.

OPTION C:
Database persistence.

OPTION D:
Redis/external state store.

Evaluate each against:

- hackathon complexity
- determinism
- performance
- integration complexity
- reliability
- future ML/prediction use
- current project scope

Recommend ONE.

Do not implement it.

==================================================
5. IMPORTANT DISTINCTION
==================================================

Clearly distinguish:

CURRENT STATE

from:

HISTORICAL STATE

from:

PREDICTION

The system currently has:

CURRENT STATE:
"What is happening now?"

Phase 2C-7 may provide:

HISTORICAL STATE:
"What happened during previous ticks?"

It must NOT yet claim:

PREDICTION:
"What will happen next?"

unless a real prediction model is later implemented and evaluated.

==================================================
6. FUTURE ML READINESS
==================================================

Evaluate whether the proposed temporal representation would allow
future features such as:

- current pilgrim load
- previous load
- load change
- load growth rate
- edge occupancy duration
- moving/stopped proportions
- neighboring edge loads
- time-of-day features

Do not implement ML.

Do not train a model.

Do not generate fake accuracy.

This phase is only about creating a trustworthy temporal data
foundation.

==================================================
7. API / WEBSOCKET IMPACT
==================================================

Determine whether historical state should be:

- sent to the frontend
- exposed through REST
- kept backend-only

The Digital Twin currently needs current state.

Do NOT unnecessarily send historical data through the WebSocket.

Recommend the smallest architecture that preserves future analytical
use without bloating the live stream.

==================================================
8. DETERMINISM
==================================================

Verify that any history mechanism can be tested deterministically.

For example:

tick 100
→ record state A

tick 101
→ record state B

tick 102
→ record state C

History should remain ordered and bounded.

Determine behavior when the history reaches capacity.

==================================================
9. RESET / LIFECYCLE
==================================================

Determine what should happen to temporal history when:

- simulation starts
- simulation resets
- simulation manager is recreated
- application restarts

Do not assume database persistence unless the architecture actually
requires it.

==================================================
10. PRIVACY / DATA BOUNDARY
==================================================

Confirm that the temporal history contains only synthetic simulation
data.

No:

- GPS telemetry
- personal information
- real pilgrim identities
- CCTV data
- device identifiers

==================================================
11. NO PREDICTION YET
==================================================

Explicitly exclude:

- ML models
- neural networks
- LSTM
- XGBoost
- Random Forest
- regression
- forecasting
- AI
- LLM
- confidence scores
- accuracy claims

We first need trustworthy temporal data.

==================================================
12. FINAL OUTPUT
==================================================

Return an inspection report containing:

1. Current state architecture.
2. Current absence/presence of temporal history.
3. Minimum temporal schema recommendation.
4. Memory estimates for 60s / 5m / 15m / 1h.
5. Comparison of history architecture options.
6. Recommended architecture.
7. API/WebSocket impact.
8. Determinism strategy.
9. Reset/lifecycle behavior.
10. Future ML-readiness analysis.
11. Exact files that would eventually need modification.
12. Exact files that should remain untouched.
13. Proposed Phase 2C-7 architecture diagram.
14. Proposed implementation sequence.

STOP.

Do not implement anything.
Wait for explicit approval.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-14T19:11:20+05:30.
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-7 — TEMPORAL CROWD STATE / PREDICTION FOUNDATION
STEP 1: INSPECTION ONLY

Phase 2C-6 is COMPLETE, COMMITTED, PUSHED, and the working tree is
clean.

DO NOT modify any application code.
DO NOT create files.
DO NOT commit.
DO NOT push.
DO NOT implement Phase 2C-7.

We are now investigating the architecture required for the next
capability.

==================================================
OBJECTIVE
==================================================

We need to determine how the current Digital Twin can retain a
deterministic temporal history of crowd state.

Current pipeline:

OSM network
    ↓
Directed graph
    ↓
Pilgrim groups
    ↓
Routing
    ↓
Movement
    ↓
Position resolution
    ↓
Simulation state
    ↓
Crowd aggregation
    ↓
WebSocket
    ↓
Digital Twin

Phase 2C-6 currently provides the current crowd state only.

We now need to determine whether and how we should retain previous
crowd states so that future prediction/intelligence can be based on
actual temporal simulation data.

==================================================
1. INSPECT CURRENT SIMULATION STATE
==================================================

Inspect:

backend/app/simulation/state_manager.py
backend/app/simulation/simulation_loop.py
backend/app/schemas/simulation.py
backend/app/schemas/crowd.py
backend/app/simulation/crowd_aggregator.py

Determine:

- what state exists only for the current tick
- whether previous ticks are retained anywhere
- whether simulation tick numbers are deterministic
- whether simulation_time is deterministic
- whether crowd aggregation can be reproduced from a past state
- whether the current state manager is suitable for maintaining a
  bounded history

DO NOT modify anything.

==================================================
2. DETERMINE REQUIRED TEMPORAL DATA
==================================================

Determine what minimum information should be retained per tick.

Evaluate whether we need:

- tick
- simulation_time
- edge_id
- pilgrim_count
- group_count
- moving_group_count
- stopped_group_count
- congested_group_count

Do NOT add fields merely because they might be useful.

For every proposed field, explain why it is necessary.

==================================================
3. MEMORY ANALYSIS
==================================================

Estimate memory requirements for:

A. 60 seconds of history
B. 5 minutes
C. 15 minutes
D. 1 hour

Use the current system:

- approximately 85 groups
- occupied edges only
- approximately 1 tick/second

Do not store all 98,621 graph edges for every tick.

History must be sparse and contain occupied edges only.

==================================================
4. HISTORY ARCHITECTURE OPTIONS
==================================================

Compare:

OPTION A:
In-memory bounded deque inside SimulationStateManager.

OPTION B:
Separate in-memory TemporalStateManager.

OPTION C:
Database persistence.

OPTION D:
Redis/external state store.

Evaluate each against:

- hackathon complexity
- determinism
- performance
- integration complexity
- reliability
- future ML/prediction use
- current project scope

Recommend ONE.

Do not implement it.

==================================================
5. IMPORTANT DISTINCTION
==================================================

Clearly distinguish:

CURRENT STATE

from:

HISTORICAL STATE

from:

PREDICTION

The system currently has:

CURRENT STATE:
"What is happening now?"

Phase 2C-7 may provide:

HISTORICAL STATE:
"What happened during previous ticks?"

It must NOT yet claim:

PREDICTION:
"What will happen next?"

unless a real prediction model is later implemented and evaluated.

==================================================
6. FUTURE ML READINESS
==================================================

Evaluate whether the proposed temporal representation would allow
future features such as:

- current pilgrim load
- previous load
- load change
- load growth rate
- edge occupancy duration
- moving/stopped proportions
- neighboring edge loads
- time-of-day features

Do not implement ML.

Do not train a model.

Do not generate fake accuracy.

This phase is only about creating a trustworthy temporal data
foundation.

==================================================
7. API / WEBSOCKET IMPACT
==================================================

Determine whether historical state should be:

- sent to the frontend
- exposed through REST
- kept backend-only

The Digital Twin currently needs current state.

Do NOT unnecessarily send historical data through the WebSocket.

Recommend the smallest architecture that preserves future analytical
use without bloating the live stream.

==================================================
8. DETERMINISM
==================================================

Verify that any history mechanism can be tested deterministically.

For example:

tick 100
→ record state A

tick 101
→ record state B

tick 102
→ record state C

History should remain ordered and bounded.

Determine behavior when the history reaches capacity.

==================================================
9. RESET / LIFECYCLE
==================================================

Determine what should happen to temporal history when:

- simulation starts
- simulation resets
- simulation manager is recreated
- application restarts

Do not assume database persistence unless the architecture actually
requires it.

==================================================
10. PRIVACY / DATA BOUNDARY
==================================================

Confirm that the temporal history contains only synthetic simulation
data.

No:

- GPS telemetry
- personal information
- real pilgrim identities
- CCTV data
- device identifiers

==================================================
11. NO PREDICTION YET
==================================================

Explicitly exclude:

- ML models
- neural networks
- LSTM
- XGBoost
- Random Forest
- regression
- forecasting
- AI
- LLM
- confidence scores
- accuracy claims

We first need trustworthy temporal data.

==================================================
12. FINAL OUTPUT
==================================================

Return an inspection report containing:

1. Current state architecture.
2. Current absence/presence of temporal history.
3. Minimum temporal schema recommendation.
4. Memory estimates for 60s / 5m / 15m / 1h.
5. Comparison of history architecture options.
6. Recommended architecture.
7. API/WebSocket impact.
8. Determinism strategy.
9. Reset/lifecycle behavior.
10. Future ML-readiness analysis.
11. Exact files that would eventually need modification.
12. Exact files that should remain untouched.
13. Proposed Phase 2C-7 architecture diagram.
14. Proposed implementation sequence.

STOP.

Do not implement anything.
Wait for explicit approval.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-14T19:27:12+05:30.
</ADDITIONAL_METADATA>
<USER_REQUEST>
1. Explicit TemporalCrowdSnapshot schema.
2. Define and implement tick-0 history behavior.
3. Treat memory numbers as estimates; measure actual serialized size.
4. Add a server-side maximum to GET /history.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-14T19:29:09+05:30.
</ADDITIONAL_METADATA>
<USER_REQUEST>
Tick 0 must be recorded exactly once.

The initial crowd snapshot must be generated from the same authoritative
initial SimulationStateManager state.

The append operation must occur under the SimulationLoop's existing
asyncio.Lock.

Starting/restarting the loop must not append duplicate tick-0 records.

Tick 0 must represent the state BEFORE the first movement advance.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-14T19:30:30+05:30.
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-7 — FINAL CLAIM/TEST VERIFICATION

Do NOT modify application code.
Do NOT commit.
Do NOT push.

Review the completed Phase 2C-7 implementation and specifically verify
the Tick-0 lifecycle claim.

The walkthrough currently states:

"Tick 0 is strictly recorded exactly once, even upon loop restarts
or crashes."

This needs precise verification because TemporalStateManager is
in-memory and its deque is destroyed when the Python process exits.

Inspect:

backend/app/simulation/temporal_manager.py
backend/app/simulation/simulation_loop.py
backend/tests/test_temporal_state.py
backend/app/api/simulation.py

Determine:

1. Does get_latest_tick() prevent duplicate Tick-0 insertion during
   multiple executions/restarts of the same TemporalStateManager
   instance?

2. What happens after the entire Python/FastAPI process restarts?

3. Is historical data intentionally lost after process restart?

4. Do tests actually verify the behavior they claim?

5. Does any code claim persistence across process crashes when no
   persistent storage exists?

If the implementation is correct but the wording overclaims
cross-process persistence, DO NOT change application code.

Instead:
- correct walkthrough/task documentation terminology
- state clearly that Tick 0 is recorded exactly once per
  TemporalStateManager history lifecycle
- state clearly that in-memory history is reset when the application
  process restarts

Also verify:

6. Full pytest result.
7. Tick-0 duplicate test result.
8. History ordering.
9. maxlen=3600 eviction.
10. API default limit=60.
11. API maximum limit=900.

Do not make unrelated changes.

STOP after reporting.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-14T19:34:45+05:30.
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-7 — CLEAN COMMIT PREPARATION

Do NOT implement anything new.
Do NOT modify application behavior.
Do NOT start Phase 2C-8.

Current git status shows:

Modified:
- app/api/simulation.py
- app/schemas/crowd.py
- app/simulation/simulation_loop.py

Untracked:
- app/simulation/temporal_manager.py
- tests/test_temporal_state.py
- scratch_ws.py

Perform an inspection-only cleanup check.

1. Confirm that the first five files are the intended Phase 2C-7 changes.

2. Inspect `scratch_ws.py`.

Determine whether it is merely a temporary diagnostic/scratch script
from the previous WebSocket testing.

3. If `scratch_ws.py` is temporary and unrelated to Phase 2C-7:
   DO NOT add it to git.
   DO NOT commit it.
   Report that it should be removed or ignored.

4. Verify that no other unexpected files are present.

5. Run:

cd backend
source .venv/bin/activate
PYTHONPATH=. pytest -v

6. Run:

git diff --stat
git diff

7. Confirm that the Phase 2C-7 diff contains only:
   - TemporalCrowdSnapshot changes
   - TemporalStateManager
   - SimulationLoop temporal history integration
   - GET /api/simulation/history
   - temporal history tests
   - required documentation/task changes, if any

8. Confirm that no Phase 1–2C-6 implementation has been unintentionally
modified.

IMPORTANT:
Do NOT git add.
Do NOT commit.
Do NOT push.
Do NOT modify code.

STOP and return the inspection results.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-14T19:46:36+05:30.
</ADDITIONAL_METADATA>
<USER_REQUEST>
Good. The inspection is clean, and **Phase 2C-7 is ready to commit**.

One correction: Antigravity says the working tree is "completely clean," but the earlier `git status` clearly showed `scratch_ws.py` plus the five intended changes. What it actually established is that the **diff is cleanly scoped**; it did not remove `scratch_ws.py`.

So before committing:

```bash
rm backend/scratch_ws.py

git status
```

Then explicitly stage only the five Phase 2C-7 files:

```bash
git add backend/app/api/simulation.py \
        backend/app/schemas/crowd.py \
        backend/app/simulation/simulation_loop.py \
        backend/app/simulation/temporal_manager.py \
        backend/tests/test_temporal_state.py
```

Review:

```bash
git status
git diff --cached --stat
git diff --cached
```

If that looks correct:

```bash
git commit -m "feat: Phase 2C-7 - Temporal Crowd State Foundation"
git push origin digital_twin
```

Then verify:

```bash
git status
git log -1 --oneline
```

You want:

```text
working tree clean
branch up to date with origin/digital_twin
```

### One thing I would NOT do

Don't add `scratch_ws.py` to `.gitignore just for this one file. It's a disposable diagnostic script, not a project artifact. Remove it.

After the push, **2C-7 is officially frozen**.

Then we'll design the next phase. And I agree with the direction we established: **don't immediately train a model**. First we should inspect the temporal data and design the feature/prediction layer properly.

</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-14T19:53:02+05:30.
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-8 — TEMPORAL FEATURE ENGINEERING & PREDICTION FOUNDATION
STEP 1: INSPECTION ONLY

Phase 2C-7 is COMPLETE, COMMITTED, PUSHED, and FROZEN.

Commit:
2950e0e feat: Phase 2C-7 - Temporal Crowd State Foundation

The working tree is clean.

DO NOT modify application code.
DO NOT create implementation files.
DO NOT train any ML model.
DO NOT install ML libraries.
DO NOT commit.
DO NOT push.
DO NOT modify frozen Phase 1–2C-7 logic.

We are now performing an architecture and data-quality inspection
for Phase 2C-8.

==================================================
OBJECTIVE
==================================================

Determine whether the current deterministic simulation produces
meaningful temporal signal that can support a future crowd-load
prediction system.

The current architecture is:

OSM road network
    ↓
Directed graph
    ↓
Synthetic pilgrim groups
    ↓
Route planner
    ↓
Movement engine
    ↓
Geographic position resolver
    ↓
Simulation state
    ↓
Crowd aggregator
    ↓
TemporalStateManager
    ↓
Historical crowd snapshots

We now need to determine:

1. What temporal features can be derived?
2. What prediction target should eventually be predicted?
3. Does the current simulation actually contain enough variation
   to make that prediction meaningful?
4. How should a future ML dataset be constructed without temporal
   leakage?
5. What simple non-ML baseline should be established before
   introducing a model?

==================================================
1. INSPECT THE TEMPORAL DATA
==================================================

Inspect:

backend/app/simulation/temporal_manager.py
backend/app/schemas/crowd.py
backend/app/simulation/crowd_aggregator.py
backend/app/simulation/simulation_loop.py
backend/app/simulation/state_manager.py

Determine exactly:

- How historical snapshots are stored.
- Whether history contains tick 0.
- Maximum available history.
- Number of occupied edges per tick.
- Number of groups.
- Total pilgrims.
- How frequently edge occupancy changes.
- Whether edge loads actually vary over time.
- Whether the current simulation generates enough temporal variation.

Do not assume variation exists.
Measure it.

==================================================
2. DATA PROFILE
==================================================

Run the simulation long enough to produce a meaningful temporal
sample using the existing deterministic simulation.

Do NOT modify the simulation algorithm.

Collect/profile the existing history.

Report:

- number of ticks observed
- number of unique edges observed
- minimum occupied edges per tick
- maximum occupied edges per tick
- mean occupied edges per tick
- minimum edge pilgrim count
- maximum edge pilgrim count
- mean edge pilgrim count
- total network pilgrim count over time
- number of edges whose load changed
- percentage of occupied edges with changing load
- number of edges with zero temporal variation
- number of edges with meaningful temporal variation

If possible, report distributions rather than only averages.

==================================================
3. TEMPORAL FEATURES

Determine which features can be derived WITHOUT changing the
simulation state.

Evaluate:

CURRENT FEATURES:

- current pilgrim_count
- current group_count
- moving_group_count
- stopped_group_count
- congested_group_count

LAG FEATURES:

- load_t_minus_1
- load_t_minus_5
- load_t_minus_15
- load_t_minus_30
- load_t_minus_60

CHANGE FEATURES:

- absolute_load_change
- percentage_load_change
- load_growth_rate
- moving_group_change
- stopped_group_change

TREND FEATURES:

- rolling mean
- rolling maximum
- rolling minimum
- rolling standard deviation
- slope/trend over a recent window

OCCUPANCY FEATURES:

- edge occupancy duration
- consecutive occupied ticks
- time since last occupation

FLOW FEATURES:

- moving / total group ratio
- stopped / total group ratio
- congested / total group ratio

Do not implement these features yet.

Determine which are actually supported by the existing data.

==================================================
4. IMPORTANT: EDGE IDENTITY

Investigate whether historical snapshots contain enough information
to correctly associate observations for the same physical directed
edge across time.

Verify:

tick
    +
edge_id
    +
crowd metrics

are sufficient to construct an edge-level time series.

Also determine how an edge that is absent from a sparse snapshot
should be represented.

Example:

tick 10:
edge A → 100

tick 11:
edge A absent

tick 12:
edge A → 120

Determine whether absence means:

load = 0

and whether this interpretation is safe and deterministic.

Do NOT modify the data model.

==================================================
5. NEIGHBORING EDGE FEATURES

Inspect the existing NetworkX graph.

Determine whether future features could incorporate:

- upstream neighboring edge load
- downstream neighboring edge load
- adjacent edge load
- number of occupied neighboring edges
- aggregate neighboring load

Do not calculate them yet.

Determine whether graph topology is sufficient to derive them
deterministically.

==================================================
6. PREDICTION TARGET

This is critical.

Do NOT assume the prediction target.

Evaluate possible targets:

A. Predict edge pilgrim_count at t+1

B. Predict edge pilgrim_count at t+5

C. Predict edge pilgrim_count at t+15

D. Predict whether an edge's load will increase

E. Predict future load growth rate

F. Predict future occupancy

For each:

- What exactly would the target mean?
- Can the current simulation generate it?
- Is it measurable?
- Is it useful to the Digital Twin?
- Is there sufficient signal?

Recommend ONE primary target and explain why.

==================================================
7. TEMPORAL LEAKAGE ANALYSIS

This is mandatory.

Design how a future training dataset would be constructed without
future information leaking into features.

Example:

For prediction at tick T+15:

Features may use:

T
T-1
T-5
T-15
...

But MUST NOT use:

T+1
T+5
T+15
or any information generated after T.

Explain:

- feature timestamp
- prediction timestamp
- target timestamp
- dataset row construction
- handling of missing history
- handling of newly appearing edges

Do not implement the dataset.

==================================================
8. TRAIN / VALIDATION / TEST STRATEGY

Do NOT use random train/test splitting.

Evaluate a chronological split such as:

EARLY PERIOD
    ↓
Training

MIDDLE PERIOD
    ↓
Validation

LATEST PERIOD
    ↓
Test

Explain why chronological evaluation is necessary for temporal
simulation data.

Also determine the minimum amount of simulation history required
before a valid training row can be created.

==================================================
9. BASELINE BEFORE ML

Before any ML model is considered, define deterministic baselines.

Evaluate:

BASELINE A:
Next load = current load

BASELINE B:
Next load = recent rolling mean

BASELINE C:
Linear trend extrapolation

Determine which baseline is most appropriate for the recommended
prediction target.

Do not implement it yet.

The eventual ML model must outperform the baseline to justify its
existence.

==================================================
10. SYNTHETIC DATA LIMITATION

This is extremely important.

The current pilgrim population is synthetic and generated with a
fixed deterministic seed.

Determine:

- What patterns are artifacts of the synthetic generator?
- Whether a model trained on this simulation would generalize to
  real pilgrimage crowds.
- Whether the current data is appropriate for demonstrating the
  engineering pipeline versus claiming real-world predictive
  accuracy.

Explicitly distinguish:

ENGINEERING DEMONSTRATION

from:

REAL-WORLD PREDICTION VALIDATION.

Do not make real-world accuracy claims.

==================================================
11. MODEL SELECTION — INSPECTION ONLY

Do NOT train anything.

Compare candidate model families at a high level:

- persistence / naive baseline
- linear regression
- Random Forest
- Gradient Boosting / XGBoost
- LSTM / temporal neural network

Evaluate them against:

- dataset size
- feature dimensionality
- temporal structure
- interpretability
- training complexity
- inference latency
- hackathon suitability
- risk of overfitting synthetic data

Recommend a model family only if the data inspection supports using
one.

It is completely acceptable to conclude that ML is not justified yet.

==================================================
12. REAL-TIME INFERENCE ARCHITECTURE

Determine how a future prediction component would fit into:

SimulationLoop
       ↓
Current Crowd State
       ↓
Feature Builder
       ↓
Prediction Model
       ↓
Prediction Result
       ↓
WebSocket / API
       ↓
Digital Twin

Do NOT implement it.

Determine:

- whether prediction should run every tick
- whether prediction should run every N ticks
- whether prediction should be asynchronous
- whether prediction belongs inside or outside asyncio.Lock
- how prediction failure should affect the simulation

The authoritative simulation must NEVER depend on the prediction
model.

==================================================
13. FAILURE ISOLATION

Design the future boundary so that:

Prediction failure
    ≠
Simulation failure

The simulation must continue deterministically if:

- model unavailable
- feature generation fails
- prediction times out
- invalid prediction returned
- model file missing

Do not implement this yet.

==================================================
14. NO AI CLAIMS

Explicitly exclude:

- LLM
- generative AI
- neural networks unless justified by data
- fabricated confidence scores
- fabricated accuracy
- fake prediction outputs
- hard-coded "predictions"
- rule-based systems presented as ML

If the inspection shows that a simple statistical baseline is
better than ML, say so.

==================================================
15. REQUIRED OUTPUT

Return a comprehensive Phase 2C-8 inspection report containing:

1. Temporal data profile.
2. Actual measured temporal variation.
3. Supported feature candidates.
4. Edge time-series construction strategy.
5. Sparse-edge handling strategy.
6. Neighbor feature feasibility.
7. Prediction target comparison.
8. Recommended prediction target.
9. Temporal leakage prevention strategy.
10. Chronological train/validation/test strategy.
11. Recommended deterministic baseline.
12. Synthetic-data limitations.
13. Model-family comparison.
14. Recommended model family, if justified.
15. Real-time inference architecture.
16. Failure-isolation architecture.
17. Exact files that would eventually change.
18. Exact files that must remain frozen.
19. Proposed Phase 2C-8 architecture diagram.
20. Recommended implementation sequence.

IMPORTANT:

This is an INSPECTION ONLY phase.

Do not modify code.
Do not create files.
Do not install dependencies.
Do not train models.
Do not commit.
Do not push.

STOP after producing the report and wait for explicit approval.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-14T19:54:45+05:30.
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-8 — FINAL ARCHITECTURE REVIEW
INSPECTION REPORT ONLY

You previously completed the Phase 2C-8 inspection and generated the
Phase 2C-8 Inspection Report.

DO NOT implement Phase 2C-8.
DO NOT modify application code.
DO NOT create implementation files.
DO NOT install ML dependencies.
DO NOT train any model.
DO NOT create a prediction model.
DO NOT commit.
DO NOT push.

I want the COMPLETE inspection report returned now so it can be
independently reviewed before implementation approval.

==================================================
1. RETURN THE ACTUAL REPORT
==================================================

Provide the complete Phase 2C-8 Inspection Report, not a summary.

It must contain the actual measurements obtained from the temporary
profiling run.

Do not replace measured values with statements such as:

"strong temporal variation"
"high variance"
"good predictive signal"

unless the numerical measurements supporting those claims are shown.

==================================================
2. TEMPORAL PROFILE — SHOW THE NUMBERS
==================================================

The profiling experiment reportedly ran for:

180 ticks
= 3 minutes

Return the actual measured values for:

- total ticks
- unique edges observed
- occupied edges per tick
- min occupied edges
- max occupied edges
- mean occupied edges
- total pilgrims per tick
- min edge load
- max edge load
- mean edge load
- median edge load if available
- number of edges whose load changed
- percentage of edges whose load changed
- number of edges with zero temporal variance
- number of edges with non-zero temporal variance
- maximum absolute load change between consecutive ticks
- mean absolute load change
- maximum load growth rate
- distribution of edge load changes if available

IMPORTANT:

Do not equate:

"maximum load ≈ 500"

with:

"strong temporal predictive signal".

Those are different measurements.

==================================================
3. VERIFY THE "85 OCCUPIED EDGES" CLAIM
==================================================

The current simulation contains approximately 85 groups.

Do NOT assume:

85 groups = 85 occupied edges.

Verify whether multiple groups ever occupy the same edge.

Report:

- minimum distinct occupied edges
- maximum distinct occupied edges
- mean distinct occupied edges
- maximum groups sharing a single edge
- number of ticks where edge sharing occurred

If exactly 85 occupied edges occurred at every tick, provide the
measured evidence.

==================================================
4. TEMPORAL VARIATION ANALYSIS
==================================================

For representative occupied edges, show examples like:

edge_id | tick | load

and demonstrate actual movement such as:

tick 10 → 120
tick 11 → 135
tick 12 → 151
...

Also show at least one edge with:

- high variation
- low variation
- zero variation, if such an edge exists

Determine whether the system contains genuine temporal variation
rather than simply moving fixed-size groups between edges.

==================================================
5. PREDICTION TARGET ANALYSIS
==================================================

The current recommendation is:

"Absolute Load at T+15"

Do NOT accept this recommendation automatically.

Compare:

A. Absolute load at T+1
B. Absolute load at T+5
C. Absolute load at T+15
D. Load change at T+15
E. Load growth rate at T+15
F. Probability of load increase at T+15

For each target explain:

- exact mathematical definition
- available target samples
- temporal horizon
- usefulness to the Digital Twin
- difficulty
- expected baseline difficulty
- whether the simulation actually produces enough variation

Then recommend ONE primary target.

==================================================
6. BASELINE REQUIREMENT
==================================================

Before recommending ML, explicitly calculate or explain the expected
performance of deterministic baselines.

At minimum evaluate:

Baseline A:
prediction(T+15) = current load(T)

Baseline B:
prediction(T+15) = recent rolling mean

Baseline C:
linear trend extrapolation

If the inspection can safely calculate baseline metrics without
modifying application code, report them.

Otherwise explain exactly how they should be calculated later.

IMPORTANT:

An ML model must eventually demonstrate improvement over a baseline.

Do not recommend ML merely because the dataset exists.

==================================================
7. FEATURE ANALYSIS
==================================================

For each proposed feature classify it as:

SUPPORTED
PARTIALLY SUPPORTED
NOT SUPPORTED

Evaluate:

Current:
- current load
- group count
- moving groups
- stopped groups
- congested groups

Lag:
- load t-1
- load t-5
- load t-15
- load t-30
- load t-60

Change:
- absolute load change
- percentage load change
- growth rate

Trend:
- rolling mean
- rolling max
- rolling min
- rolling std
- recent slope

Occupancy:
- consecutive occupied ticks
- time since last occupation

Topology:
- upstream load
- downstream load
- neighboring edge load
- number of occupied neighbors

Do not invent features that cannot be derived from the current
architecture.

==================================================
8. CRITICAL SPARSE-EDGE RULE
==================================================

Explicitly establish:

If an edge is absent from a sparse CrowdStateSnapshot at tick T,
does that mean:

load(edge,T) = 0

?

If yes, explain why this is mathematically safe.

If no, explain the correct representation.

This decision must be explicit before any future dataset is created.

==================================================
9. TEMPORAL LEAKAGE
==================================================

Show an exact example training row.

For example:

Prediction timestamp:
T = 100

Target:
load(edge, T+15)

Allowed features:
T
T-1
T-5
T-15
T-30
T-60

Forbidden information:
T+1
T+5
T+15
anything generated after T

Show exactly how one row would be constructed.

==================================================
10. TRAIN / VALIDATION / TEST
==================================================

Explain the chronological split.

Example:

earliest period → training
middle period   → validation
latest period   → test

Do NOT use random train/test splitting.

Also determine:

- minimum history required
- minimum number of training samples
- how newly appearing edges are handled
- how missing history is handled

==================================================
11. SYNTHETIC DATA LIMITATION
==================================================

Be explicit about the distinction:

ENGINEERING DEMONSTRATION
versus
REAL-WORLD PREDICTIVE VALIDATION.

The simulation uses synthetic pilgrim groups.

Determine what conclusions are legitimately supported.

Do NOT claim:

"real-world crowd prediction accuracy"

from this dataset.

==================================================
12. MODEL FAMILY COMPARISON
==================================================

Compare:

- persistence baseline
- linear regression
- Random Forest
- Gradient Boosting / XGBoost
- LSTM / temporal neural network

Evaluate:

- data size
- temporal structure
- feature dimensionality
- interpretability
- training complexity
- inference latency
- overfitting risk
- hackathon suitability

Do not train anything.

Recommend ML only if justified by the measured data.

It is acceptable — and preferable — to conclude:

"ML is not justified yet."

==================================================
13. REAL-TIME ARCHITECTURE
==================================================

Analyze:

SimulationLoop
      ↓
Current Crowd State
      ↓
Temporal History
      ↓
Feature Builder
      ↓
Prediction
      ↓
Prediction Result
      ↓
API/WebSocket
      ↓
Digital Twin

Determine:

- whether prediction should execute every tick
- whether prediction should execute every N ticks
- whether it should run asynchronously
- whether it may execute inside asyncio.Lock
- how prediction failure is isolated

CRITICAL:

The authoritative simulation must NEVER depend on prediction.

If prediction crashes, times out, or returns invalid output:

simulation continues normally.

==================================================
14. FUTURE IMPLEMENTATION BOUNDARY
==================================================

Identify exact files that would eventually be modified.

Separate them into:

A. Feature engineering
B. Dataset generation
C. Model training
D. Model inference
E. API/WebSocket integration
F. Frontend visualization

Also identify files that must remain frozen:

Phase 1
Phase 2A
Phase 2B
Phase 2C-1
Phase 2C-2
Phase 2C-3A
Phase 2C-3B
Phase 2C-4A
Phase 2C-4B
Phase 2C-5
Phase 2C-6
Phase 2C-7

==================================================
15. FINAL RECOMMENDATION
==================================================

End with exactly one of these conclusions:

OPTION A:
"Data is sufficient for a prediction proof-of-concept."

OPTION B:
"Data is insufficient for ML; improve the simulator/data generation
before modeling."

OPTION C:
"Data supports feature engineering and deterministic baselines, but
ML should be deferred."

Explain the decision using the measured evidence.

==================================================
16. STRICT STOP CONDITION
==================================================

This remains INSPECTION ONLY.

Do not:

- modify Python files
- modify TypeScript files
- modify schemas
- create feature-engineering code
- create datasets
- train models
- install scikit-learn
- install XGBoost
- install PyTorch
- install TensorFlow
- create model files
- change WebSocket payloads
- change simulation behavior
- commit
- push

Return the complete report and STOP.

Wait for explicit approval before implementing anything.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-15T08:51:09+05:30.
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-8 — FINAL ARCHITECTURE REVIEW
INSPECTION REPORT ONLY

You previously completed the Phase 2C-8 inspection and generated the
Phase 2C-8 Inspection Report.

DO NOT implement Phase 2C-8.
DO NOT modify application code.
DO NOT create implementation files.
DO NOT install ML dependencies.
DO NOT train any model.
DO NOT create a prediction model.
DO NOT commit.
DO NOT push.

I want the COMPLETE inspection report returned now so it can be
independently reviewed before implementation approval.

==================================================
1. RETURN THE ACTUAL REPORT
==================================================

Provide the complete Phase 2C-8 Inspection Report, not a summary.

It must contain the actual measurements obtained from the temporary
profiling run.

Do not replace measured values with statements such as:

"strong temporal variation"
"high variance"
"good predictive signal"

unless the numerical measurements supporting those claims are shown.

==================================================
2. TEMPORAL PROFILE — SHOW THE NUMBERS
==================================================

The profiling experiment reportedly ran for:

180 ticks
= 3 minutes

Return the actual measured values for:

- total ticks
- unique edges observed
- occupied edges per tick
- min occupied edges
- max occupied edges
- mean occupied edges
- total pilgrims per tick
- min edge load
- max edge load
- mean edge load
- median edge load if available
- number of edges whose load changed
- percentage of edges whose load changed
- number of edges with zero temporal variance
- number of edges with non-zero temporal variance
- maximum absolute load change between consecutive ticks
- mean absolute load change
- maximum load growth rate
- distribution of edge load changes if available

IMPORTANT:

Do not equate:

"maximum load ≈ 500"

with:

"strong temporal predictive signal".

Those are different measurements.

==================================================
3. VERIFY THE "85 OCCUPIED EDGES" CLAIM
==================================================

The current simulation contains approximately 85 groups.

Do NOT assume:

85 groups = 85 occupied edges.

Verify whether multiple groups ever occupy the same edge.

Report:

- minimum distinct occupied edges
- maximum distinct occupied edges
- mean distinct occupied edges
- maximum groups sharing a single edge
- number of ticks where edge sharing occurred

If exactly 85 occupied edges occurred at every tick, provide the
measured evidence.

==================================================
4. TEMPORAL VARIATION ANALYSIS
==================================================

For representative occupied edges, show examples like:

edge_id | tick | load

and demonstrate actual movement such as:

tick 10 → 120
tick 11 → 135
tick 12 → 151
...

Also show at least one edge with:

- high variation
- low variation
- zero variation, if such an edge exists

Determine whether the system contains genuine temporal variation
rather than simply moving fixed-size groups between edges.

==================================================
5. PREDICTION TARGET ANALYSIS
==================================================

The current recommendation is:

"Absolute Load at T+15"

Do NOT accept this recommendation automatically.

Compare:

A. Absolute load at T+1
B. Absolute load at T+5
C. Absolute load at T+15
D. Load change at T+15
E. Load growth rate at T+15
F. Probability of load increase at T+15

For each target explain:

- exact mathematical definition
- available target samples
- temporal horizon
- usefulness to the Digital Twin
- difficulty
- expected baseline difficulty
- whether the simulation actually produces enough variation

Then recommend ONE primary target.

==================================================
6. BASELINE REQUIREMENT
==================================================

Before recommending ML, explicitly calculate or explain the expected
performance of deterministic baselines.

At minimum evaluate:

Baseline A:
prediction(T+15) = current load(T)

Baseline B:
prediction(T+15) = recent rolling mean

Baseline C:
linear trend extrapolation

If the inspection can safely calculate baseline metrics without
modifying application code, report them.

Otherwise explain exactly how they should be calculated later.

IMPORTANT:

An ML model must eventually demonstrate improvement over a baseline.

Do not recommend ML merely because the dataset exists.

==================================================
7. FEATURE ANALYSIS
==================================================

For each proposed feature classify it as:

SUPPORTED
PARTIALLY SUPPORTED
NOT SUPPORTED

Evaluate:

Current:
- current load
- group count
- moving groups
- stopped groups
- congested groups

Lag:
- load t-1
- load t-5
- load t-15
- load t-30
- load t-60

Change:
- absolute load change
- percentage load change
- growth rate

Trend:
- rolling mean
- rolling max
- rolling min
- rolling std
- recent slope

Occupancy:
- consecutive occupied ticks
- time since last occupation

Topology:
- upstream load
- downstream load
- neighboring edge load
- number of occupied neighbors

Do not invent features that cannot be derived from the current
architecture.

==================================================
8. CRITICAL SPARSE-EDGE RULE
==================================================

Explicitly establish:

If an edge is absent from a sparse CrowdStateSnapshot at tick T,
does that mean:

load(edge,T) = 0

?

If yes, explain why this is mathematically safe.

If no, explain the correct representation.

This decision must be explicit before any future dataset is created.

==================================================
9. TEMPORAL LEAKAGE
==================================================

Show an exact example training row.

For example:

Prediction timestamp:
T = 100

Target:
load(edge, T+15)

Allowed features:
T
T-1
T-5
T-15
T-30
T-60

Forbidden information:
T+1
T+5
T+15
anything generated after T

Show exactly how one row would be constructed.

==================================================
10. TRAIN / VALIDATION / TEST
==================================================

Explain the chronological split.

Example:

earliest period → training
middle period   → validation
latest period   → test

Do NOT use random train/test splitting.

Also determine:

- minimum history required
- minimum number of training samples
- how newly appearing edges are handled
- how missing history is handled

==================================================
11. SYNTHETIC DATA LIMITATION
==================================================

Be explicit about the distinction:

ENGINEERING DEMONSTRATION
versus
REAL-WORLD PREDICTIVE VALIDATION.

The simulation uses synthetic pilgrim groups.

Determine what conclusions are legitimately supported.

Do NOT claim:

"real-world crowd prediction accuracy"

from this dataset.

==================================================
12. MODEL FAMILY COMPARISON
==================================================

Compare:

- persistence baseline
- linear regression
- Random Forest
- Gradient Boosting / XGBoost
- LSTM / temporal neural network

Evaluate:

- data size
- temporal structure
- feature dimensionality
- interpretability
- training complexity
- inference latency
- overfitting risk
- hackathon suitability

Do not train anything.

Recommend ML only if justified by the measured data.

It is acceptable — and preferable — to conclude:

"ML is not justified yet."

==================================================
13. REAL-TIME ARCHITECTURE
==================================================

Analyze:

SimulationLoop
      ↓
Current Crowd State
      ↓
Temporal History
      ↓
Feature Builder
      ↓
Prediction
      ↓
Prediction Result
      ↓
API/WebSocket
      ↓
Digital Twin

Determine:

- whether prediction should execute every tick
- whether prediction should execute every N ticks
- whether it should run asynchronously
- whether it may execute inside asyncio.Lock
- how prediction failure is isolated

CRITICAL:

The authoritative simulation must NEVER depend on prediction.

If prediction crashes, times out, or returns invalid output:

simulation continues normally.

==================================================
14. FUTURE IMPLEMENTATION BOUNDARY
==================================================

Identify exact files that would eventually be modified.

Separate them into:

A. Feature engineering
B. Dataset generation
C. Model training
D. Model inference
E. API/WebSocket integration
F. Frontend visualization

Also identify files that must remain frozen:

Phase 1
Phase 2A
Phase 2B
Phase 2C-1
Phase 2C-2
Phase 2C-3A
Phase 2C-3B
Phase 2C-4A
Phase 2C-4B
Phase 2C-5
Phase 2C-6
Phase 2C-7

==================================================
15. FINAL RECOMMENDATION
==================================================

End with exactly one of these conclusions:

OPTION A:
"Data is sufficient for a prediction proof-of-concept."

OPTION B:
"Data is insufficient for ML; improve the simulator/data generation
before modeling."

OPTION C:
"Data supports feature engineering and deterministic baselines, but
ML should be deferred."

Explain the decision using the measured evidence.

==================================================
16. STRICT STOP CONDITION
==================================================

This remains INSPECTION ONLY.

Do not:

- modify Python files
- modify TypeScript files
- modify schemas
- create feature-engineering code
- create datasets
- train models
- install scikit-learn
- install XGBoost
- install PyTorch
- install TensorFlow
- create model files
- change WebSocket payloads
- change simulation behavior
- commit
- push

Return the complete report and STOP.

Wait for explicit approval before implementing anything.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-15T08:54:53+05:30.
</ADDITIONAL_METADATA>
<USER_REQUEST>
PHASE 2C-8A — DETERMINISTIC FEATURE ENGINEERING & PERSISTENCE BASELINE

IMPLEMENTATION PHASE

Phase 2C-8 inspection is complete and has been reviewed.

The approved conclusion is:

OPTION C:
"Data supports feature engineering and deterministic baselines,
but ML should be deferred."

The current simulation produces temporal variation, but the observed
data is a rigid deterministic step-function caused by cohesive
synthetic pilgrim groups moving between graph edges.

Therefore, we will NOT implement ML yet.

==================================================
CORE OBJECTIVE
==================================================

Implement the smallest trustworthy temporal feature-engineering and
deterministic prediction-baseline layer on top of the existing
Phase 2C-7 temporal history.

The implementation must allow us to answer:

"For a given directed edge at time T, what would a simple persistence
baseline predict its absolute pilgrim load will be at T+15?"

Then objectively measure how good that baseline actually is.

This phase is about DATA + BASELINE VALIDATION.

It is NOT an ML phase.

==================================================
HARD ARCHITECTURAL BOUNDARIES
==================================================

DO NOT:

- train an ML model
- install scikit-learn
- install XGBoost
- install PyTorch
- install TensorFlow
- create an ML model file
- create an LSTM
- create a Random Forest
- create an XGBoost model
- create a neural network
- create a prediction engine
- create a frontend prediction layer
- modify the WebSocket payload
- modify the simulation tick behavior
- modify movement
- modify routing
- modify crowd aggregation semantics
- modify temporal storage semantics
- introduce a database
- introduce Redis
- introduce external APIs
- fabricate prediction confidence
- fabricate accuracy

Do NOT make the authoritative simulation depend on this phase.

The existing simulation must continue operating exactly as before.

==================================================
FROZEN ARCHITECTURE
==================================================

The following phases are frozen and MUST NOT be modified:

Phase 1
Phase 2A
Phase 2B
Phase 2C-1
Phase 2C-2
Phase 2C-3A
Phase 2C-3B
Phase 2C-4A
Phase 2C-4B
Phase 2C-5
Phase 2C-6
Phase 2C-7

Especially preserve:

- graph construction
- graph edge IDs
- route planner
- movement engine
- position resolver
- SimulationStateManager semantics
- SimulationLoop tick behavior
- CrowdAggregator
- TemporalStateManager
- WebSocket schema
- frontend simulation streaming

==================================================
1. INSPECT BEFORE MODIFYING
==================================================

Before writing code, inspect:

backend/app/schemas/crowd.py
backend/app/simulation/temporal_manager.py
backend/app/simulation/crowd_aggregator.py
backend/app/simulation/simulation_loop.py
backend/app/api/simulation.py

Also inspect the existing tests around:

- temporal history
- crowd aggregation
- simulation state
- simulation loop

Confirm the exact current schema and APIs.

Do not assume field names.

Do not duplicate existing functionality.

==================================================
2. FEATURE ENGINEERING MODULE
==================================================

Create a narrowly scoped module:

backend/app/ml/features.py

This module must be PRESENTATION/ANALYTICS logic only.

It must NOT mutate:

- SimulationStateManager
- TemporalStateManager
- NetworkX graph
- PilgrimGroup
- CrowdStateSnapshot

It should consume historical temporal snapshots and derive
deterministic feature values.

==================================================
3. PRIMARY FEATURE SET
==================================================

Implement only features that are directly supported by the current
temporal history.

At minimum support:

CURRENT:

- current_load

LAGS:

- load_t_minus_1
- load_t_minus_5
- load_t_minus_15

CHANGES:

- absolute_load_change_1
- absolute_load_change_5
- absolute_load_change_15

OCCUPANCY:

- consecutive_occupied_ticks

Do NOT automatically implement every feature proposed in the
Phase 2C-8 inspection report.

Keep the first implementation minimal.

==================================================
4. SPARSE EDGE SEMANTICS
==================================================

This rule is authoritative:

If an edge is absent from a sparse CrowdStateSnapshot at tick T:

    load(edge, T) = 0

This must be handled deterministically by the feature builder.

Example:

tick 10:
edge A = 100

tick 11:
edge A absent

tick 12:
edge A = 100

Therefore:

load(T10) = 100
load(T11) = 0
load(T12) = 100

Do NOT treat missing edge data as unknown/null.

Do NOT interpolate missing values.

Do NOT fabricate values.

==================================================
5. NO PERCENTAGE GROWTH FEATURE
==================================================

Do NOT implement:

(new - old) / old

as a production feature.

The current data contains frequent transitions from:

0 → N
N → 0

which makes naïve percentage growth undefined or misleading.

Use absolute load change instead.

==================================================
6. EDGE TIME-SERIES CONSTRUCTION
==================================================

The feature builder must be able to construct a deterministic
time series for a specific edge:

(edge_id, tick) → load

The implementation must correctly handle:

- occupied edge
- absent edge
- newly appearing edge
- previously occupied edge becoming empty
- multiple historical ticks
- insufficient history

Do not modify TemporalStateManager to achieve this.

==================================================
7. INSUFFICIENT HISTORY
==================================================

If the requested lag does not exist:

DO NOT fabricate the value.

Choose one deterministic behavior:

- return None for that feature, OR
- reject the feature row as insufficient history.

Use the approach that best matches the existing Pydantic/schema
conventions.

Document the decision.

==================================================
8. PRIMARY PREDICTION TARGET
==================================================

The target is:

ABSOLUTE EDGE PILGRIM LOAD AT T+15

For edge E at tick T:

X = information available at or before T

y = load(E, T+15)

The target MUST NOT be included in the feature vector.

==================================================
9. PERSISTENCE BASELINE
==================================================

Create:

backend/app/ml/baseline.py

Implement exactly one baseline first:

Persistence:

prediction(E, T+15) = load(E, T)

This is a deterministic baseline.

It is NOT ML.

It has no training process.

It has no learned parameters.

It must return the current observed edge load as the prediction.

==================================================
10. BASELINE EVALUATION
==================================================

Create an evaluation utility, for example:

backend/app/ml/evaluation.py

or another appropriately scoped module if the existing architecture
suggests a better location.

Implement deterministic evaluation of the persistence baseline.

At minimum calculate:

- MAE
- RMSE

Definitions:

MAE:
mean absolute error between predicted and actual T+15 load.

RMSE:
square root of mean squared error.

Also report:

- number of evaluated samples
- number of skipped samples
- prediction horizon
- number of unique edges evaluated

Do not report:

- accuracy percentage
- confidence
- precision/recall
- F1
- "AI accuracy"

Those metrics are inappropriate for the primary continuous
absolute-load target.

==================================================
11. IMPORTANT: DO NOT CLAIM BASELINE PERFORMANCE BEFORE MEASURING
==================================================

The Phase 2C-8 inspection suggested that persistence should be strong.

That is a hypothesis.

Do NOT hard-code or claim:

"Persistence is 95% accurate"

or any other performance number.

Actually calculate:

MAE
RMSE

from the deterministic simulation data.

If persistence performs poorly, report that honestly.

If persistence performs extremely well, report that honestly.

==================================================
12. OFFLINE / DIAGNOSTIC EVALUATION
==================================================

Do NOT add baseline computation to the real-time SimulationLoop.

Do NOT execute baseline inference every simulation tick.

Do NOT add baseline predictions to the WebSocket payload.

Instead, create:

backend/scripts/phase2c8a_diagnostics.py

This diagnostic should:

1. Construct/use the existing deterministic simulation.
2. Generate sufficient temporal history.
3. Build valid feature/target rows.
4. Run the persistence baseline.
5. Calculate MAE and RMSE.
6. Print deterministic summary statistics.
7. Clearly identify skipped rows caused by insufficient history.

The diagnostic must not alter the authoritative simulation architecture.

==================================================
13. TRAINING / VALIDATION / TEST
==================================================

Do NOT train anything.

Do NOT create a machine-learning dataset intended for model fitting.

However, structure the evaluation code so that chronological
evaluation is possible later.

The conceptual rule is:

Features at T
        ↓
Prediction
        ↓
Target at T+15

Never use information after T inside X.

Do NOT use random train/test splitting.

==================================================
14. TEMPORAL LEAKAGE TESTS
==================================================

Add explicit tests proving:

- T+15 target is not included in feature construction.
- future ticks cannot enter the feature vector.
- sparse missing edges resolve to zero.
- insufficient history is handled deterministically.
- persistence prediction equals current load exactly.

==================================================
15. TESTS
==================================================

Create:

backend/tests/test_phase2c8a_features.py

Cover at minimum:

1. current load extraction
2. t-1 lag
3. t-5 lag
4. t-15 lag
5. absolute load change
6. sparse edge → zero
7. newly appearing edge
8. edge becoming empty
9. insufficient history
10. persistence prediction
11. target at T+15
12. future leakage prevention
13. MAE calculation
14. RMSE calculation
15. deterministic repeated evaluation

Use small synthetic temporal snapshots for unit tests.

Do NOT depend exclusively on the full 48,967-node graph for unit
tests.

==================================================
16. TEMPORAL HISTORY CAPACITY LIMITATION
==================================================

Current Phase 2C-7 history is bounded.

Do NOT modify the 900-tick temporal history merely to support this
phase.

Do NOT change:

maxlen=900

unless an explicit requirement makes it impossible.

The previously discussed 30/15/15 minute ML split is NOT being
implemented now.

That split belongs to a future offline dataset-generation phase,
if ML is ever justified.

==================================================
17. NO REAL-TIME PREDICTION ENGINE
==================================================

Do NOT create:

backend/app/simulation/prediction_engine.py

Do NOT create:

backend/app/api/prediction.py

Do NOT create:

frontend/src/components/digital-twin/PredictionLayer.tsx

Those are deferred.

There is currently no ML model to serve.

==================================================
18. NO FRONTEND CHANGES
==================================================

Frontend must remain untouched.

Do NOT:

- add prediction markers
- add prediction heatmaps
- add prediction WebSocket fields
- add prediction controls
- add prediction legend entries

This phase is backend analytics foundation only.

==================================================
19. ARCHITECTURAL ISOLATION
==================================================

The dependency direction must remain:

Simulation
    ↓
Temporal History
    ↓
Feature Builder
    ↓
Baseline Evaluation

NOT:

Feature Builder
    ↓
Simulation

NOT:

Baseline
    ↓
SimulationLoop

NOT:

Prediction
    ↓
Movement

The simulation remains the authoritative source of truth.

==================================================
20. DETERMINISM
==================================================

The entire Phase 2C-8A implementation must be deterministic.

No:

- random sampling
- timestamps from wall clock
- random seeds inside feature generation
- stochastic inference
- external APIs
- network calls

Given identical temporal snapshots, the feature builder and baseline
must produce identical outputs.

==================================================
21. DIAGNOSTIC OUTPUT
==================================================

phase2c8a_diagnostics.py must report at minimum:

Temporal history:
- ticks available
- unique edges
- candidate rows
- valid rows
- skipped rows

Features:
- number of features per row
- missing-feature count

Baseline:
- prediction horizon = 15 ticks
- MAE
- RMSE
- evaluated samples
- unique edges evaluated

Also report:

- minimum target load
- maximum target load
- mean target load

Do NOT call these "accuracy".

==================================================
22. REQUIRED DOCUMENTATION
==================================================

Update walkthrough/task documentation only if necessary.

Clearly state:

- this is NOT ML
- persistence is a deterministic baseline
- no model was trained
- no model dependencies were installed
- the current synthetic data is an engineering dataset
- real-world prediction claims are not supported

Do not overclaim.

==================================================
23. GIT SAFETY
==================================================

Before finishing:

Run:

git status
git diff --stat
git diff

Verify that no frozen phase files were unintentionally changed.

If unrelated changes exist, STOP and report them.

Do not commit.

Do not push.

==================================================
24. REQUIRED VALIDATION
==================================================

Run:

cd backend
source .venv/bin/activate
PYTHONPATH=. pytest -v

Then:

python3 scripts/phase2c8a_diagnostics.py

Then:

cd frontend
npm run lint
npm run build

Frontend should remain unchanged, but still verify that the entire
repository builds cleanly.

==================================================
25. REQUIRED FINAL REPORT
==================================================

Return:

1. Files created.
2. Files modified.
3. Features implemented.
4. Persistence baseline implementation.
5. Evaluation methodology.
6. Actual MAE.
7. Actual RMSE.
8. Number of evaluated rows.
9. Number of skipped rows.
10. Temporal leakage tests.
11. Sparse-edge behavior.
12. Determinism verification.
13. Full pytest result.
14. Frontend lint result.
15. Frontend build result.
16. Git diff scope verification.
17. Explicit confirmation that NO ML model was trained.
18. Explicit confirmation that NO ML dependencies were installed.
19. Explicit confirmation that WebSocket and SimulationLoop behavior
    remain unchanged.
20. Explicit confirmation that frontend files remain unchanged.

==================================================
STOP CONDITION
==================================================

After implementation and verification:

STOP.

Do NOT proceed to:

- XGBoost
- Random Forest
- LSTM
- model training
- prediction engine
- prediction API
- prediction visualization
- Phase 2C-9

Wait for explicit approval.

The goal of Phase 2C-8A is only:

TEMPORAL HISTORY
      ↓
DETERMINISTIC FEATURES
      ↓
PERSISTENCE BASELINE
      ↓
MEASURED MAE/RMSE

Nothing beyond that.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-15T09:11:51+05:30.
</ADDITIONAL_METADATA>
<USER_REQUEST>
MASTER ROADMAP RECOVERY — POST PHASE 2C-8A
INSPECTION ONLY

Phase 2C-8A has now been implemented and verified.

Current confirmed state:

- Phase 2C-8A feature engineering is complete.
- Persistence baseline is complete.
- No ML model was trained.
- No ML dependencies were installed.
- 71 backend tests passed, 3 skipped.
- Frontend lint passed.
- Frontend build passed.
- SimulationLoop is unchanged.
- WebSocket payload is unchanged.
- Frontend is unchanged.
- Phase 1 through Phase 2C-8A are considered frozen unless the
  master roadmap explicitly requires otherwise.

The persistence baseline result was:

MAE = 47.61
RMSE = 128.82

These are baseline measurements on the deterministic synthetic
simulation only. Do NOT interpret them as real-world predictive
accuracy.

==================================================
OBJECTIVE
==================================================

We have spent substantial time inside Phase 2C.

Before implementing anything else, I want you to recover and inspect
the ORIGINAL MASTER PROJECT ROADMAP.

Do NOT invent new phases.

Do NOT extend Phase 2C.

Do NOT propose implementation yet.

Do NOT modify application code.

Do NOT create files.

Do NOT install dependencies.

Do NOT train ML.

Do NOT commit.

Do NOT push.

==================================================
1. FIND THE MASTER ROADMAP
==================================================

Search the repository for the original project planning documents.

Look for files such as:

- implementation_plan.md
- README.md
- task.md
- walkthrough.md
- project specification
- architecture documentation
- phase documentation
- roadmap documentation

Search the entire repository if necessary.

Identify which document represents the ORIGINAL master roadmap,
rather than a temporary phase-specific implementation plan.

If multiple roadmap documents exist, report all of them and explain
which one appears authoritative based on their contents.

==================================================
2. DO NOT RECONSTRUCT THE ROADMAP FROM MEMORY
==================================================

Do NOT invent missing phases.

Do NOT infer phase names simply from the current implementation.

Use the actual repository documentation.

Preserve the original terminology and phase numbering exactly.

==================================================
3. EXTRACT THE ORIGINAL PHASE STRUCTURE
==================================================

Return the complete roadmap in this format:

Phase 1
- Objective:
- Deliverables:
- Status:

Phase 2
- Objective:
- Deliverables:
- Status:

Phase 3
- Objective:
- Deliverables:
- Status:

...

Continue until the final phase in the original roadmap.

Do not omit future phases.

==================================================
4. MAP CURRENT IMPLEMENTATION TO THE ROADMAP
==================================================

Create a table:

| Original Phase | Original Objective | Current Status | Evidence |
|----------------|--------------------|----------------|----------|

For each phase classify it as exactly one of:

COMPLETE
PARTIALLY COMPLETE
NOT STARTED
UNCLEAR

Do NOT mark a phase complete merely because related functionality
exists somewhere in the repository.

Use the original roadmap's acceptance criteria where available.

==================================================
5. SPECIAL REVIEW OF PHASE 2
==================================================

Determine exactly how much of the original Phase 2 roadmap has been
completed.

Map:

Phase 2A
Phase 2B
Phase 2C
and every original Phase 2 sub-phase

against the actual roadmap.

Do NOT create new sub-phases.

Identify whether Phase 2 was originally intended to end before the
current temporal/prediction work.

==================================================
6. CHECK FOR WORK IMPLEMENTED AHEAD OF SCHEDULE
==================================================

Determine whether any current functionality was implemented earlier
than the original roadmap intended.

Examples to inspect:

- temporal history
- crowd aggregation
- WebSocket streaming
- feature engineering
- prediction baseline

If functionality was implemented ahead of schedule, report it.

Do NOT recommend deleting it.

==================================================
7. IDENTIFY THE NEXT MAJOR PHASE
==================================================

Based strictly on the ORIGINAL roadmap, identify:

NEXT MAJOR PHASE:
<exact original phase name/number>

Original objective:
<exact objective>

Why it is next:
<evidence from roadmap>

Dependencies already satisfied:
<list>

Dependencies still missing:
<list>

Do NOT choose the next phase based on what you personally think
would be interesting.

Use the roadmap.

==================================================
8. DO NOT START IMPLEMENTATION
==================================================

Even if the next phase is obvious:

DO NOT write code.

DO NOT modify task.md.

DO NOT modify implementation_plan.md.

DO NOT create an implementation plan for the next phase yet.

DO NOT create tests.

DO NOT install anything.

STOP after the roadmap analysis.

==================================================
9. IMPORTANT ARCHITECTURAL CONSTRAINT
==================================================

The following existing functionality should be treated as frozen
unless the ORIGINAL roadmap explicitly requires modification:

- Phase 1 OSM data
- Phase 2A graph
- Phase 2B pilgrim model
- Phase 2C routing
- Phase 2C movement
- Phase 2C simulation state
- Phase 2C geographic positioning
- Phase 2C WebSocket
- Phase 2C crowd aggregation
- Phase 2C crowd visualization
- Phase 2C temporal history
- Phase 2C-8A deterministic features
- Persistence baseline

If the next major phase requires changes to these components,
identify the dependency, but do not modify them.

==================================================
10. FINAL REPORT
==================================================

Return ONLY an inspection report containing:

1. Master roadmap document(s) discovered.
2. Original complete phase structure.
3. Current phase-by-phase status.
4. Evidence for completed phases.
5. Any phases implemented ahead of schedule.
6. Exact next major phase.
7. Dependencies satisfied.
8. Dependencies remaining.
9. Any conflict between current implementation and original roadmap.
10. Recommended next action.

Then STOP.

No implementation.
No code changes.
No commit.
No push.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-15T10:32:37+05:30.
</ADDITIONAL_METADATA>
